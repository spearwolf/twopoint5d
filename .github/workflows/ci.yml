name: CI

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: ./.github/workflows/prepare_workspace.yml

      - run: yarn test
        name: Run tests
