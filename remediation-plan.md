# Remediation-Plan — twopoint5d (Display & Frame-Loop)

Quelle: ./audit.html vom 2026-09-23 · Branch: main · erstellt: 2026-09-24
Baseline: `pnpm run ci` ✓ (clean, lint, build, typecheck, checkPkgTypes, checkNameableTypes, lintPkg, test:scripts, test:coverage, test:browser)
Arbeitsverzeichnis: /tmp/claude-1000/-home-spw-spaceland-twopoint5d/a354229b-228b-496c-9c2e-82b9e47afc5f/scratchpad (Diffs und Verify-Logs, außerhalb der Versionierung)
Paketdetails: docs/remediation/paket-<N>.md — je Paket eine Datei, angelegt von dessen Zug 0
Scope: 33 Findings der Domäne »Display & Frame-Loop« (4 medium, 18 low, 11 info — Teil-Findings TEST-004/TEST-042 nur mit ihrem Display-Anteil) · ausgenommen: alle übrigen Domänen, acknowledged
Scope-Regel: alles, was `packages/twopoint5d/src/display/**` betrifft, samt dessen Tests, Doku, CHANGELOG-Einträgen und den Display-Demos der Lookbook, jede Severity einschließlich info — gilt auch für Befunde, die erst im Lauf auffallen. Ziel ist eine stabile, issue-freie Display-Basis.
Kaltstarts: 6 Pakete (Paket 1 in Zug 0 geteilt, 1b neu, 5 aus einem vorbestehenden Befund, den Paket 3 gemeldet hat) × mindestens 3 Agenten ≈ 18, je Nachrunde zwei mehr · 6,6 Findings je Paket
Stand (2026-09-24): Lauf abgeschlossen · Pakete 1–8 und 1b committet, keines blockiert · Abschluss-Verify `pnpm run ci` grün auf bd80d232 · »Offene Befunde« vollständig verteilt: im Lauf behoben oder als neues Finding in ./audit.html gebucht

Diese Datei führt einen Lauf des Skills `js-ts-audit-remediation` und hält
seinen Stand. Wer hier weiterarbeitet: diesen Skill laden, die eingetragenen
Hashes gegen `git log --oneline` halten, beim obersten Paket ohne `[x]`
einsteigen. Der Lauf ist erst fertig, wenn auch »Offene Befunde« leer ist.
Statusmarken: `[ ]` offen · `[~]` Detailplan steht, Umsetzung läuft · `[x]`
erledigt · `[r]` committet, Review wird nachgezogen · `[!]` blockiert.

## Entscheidungen
- Scope: Kern-Findings unter `src/display/` plus die domänennahen Findings in Tests, Doku, CHANGELOG und Display-Demos (SEC-002, Display-Anteile von TEST-004/TEST-042, TEST-024, READ-017, DOC-014, DOC-023, MEM-006, CONS-045) (2026-09-24)
- API-065 jetzt umsetzen: Interna (`postFixID`, `globalStylesID`, `OnRAF`, `ISetAnimationLoop`) aus den öffentlichen Exporten nehmen, `public-api.ts` mit benannten Exporten; `renderer`, `frameLoop`, `frameNo` nur lesbar (Getter/readonly). Breaking Change mit CHANGELOG- und Migration-Guide-Eintrag; eigene Aufrufer (Lookbook, Tests) werden mitgezogen (2026-09-24)
- IMPL-009: `elementIsInsideViewport` per IntersectionObserver auf den Canvas verdrahten, **opt-in** über Option/Attribut, Default aus; beim `dispose()` getrennt. Kein Verhaltenswechsel für bestehende Konsumenten (2026-09-24)
- MEM-018: `dispose()` nimmt bei einem übergebenen Canvas alles zurück, was das Display gesetzt hat — Klassen, Inline-Größe, Fullscreen-Klasse —, sodass »not modified« in `resource-lifecycle.md` wörtlich stimmt (2026-09-24)
- BUG-117: Die TSDoc gilt — `MaxResolution` klemmt in Device-Pixeln (`w × pixelRatio`, bei `pixelZoom > 0` gegen `w`); Browser-Test prüft `canvas.width` mit emuliertem dpr (2026-09-24)
- READ-001 betrifft in diesem Lauf nur `Display.ts`; der `StageRenderer.ts`-Anteil gehört zur Stage-Domäne und bleibt im Audit offen (2026-09-24)
- TEST-004 und TEST-042 werden nur mit ihrem Display-Anteil behoben (Pause/Restart, visibilitychange, Event-Reihenfolge, start/stop-Race, Chronometer, Dispose während Init, dpr-Clamp); die Anteile zu styleUtils, PanControl2D, Sprites und Stage bleiben im Audit offen (2026-09-24)
- Paket 5 neu geschnitten (Rückfrage aus Zug 2 von B): Ziel ist, dass die Freigabe eines `WebGPURenderer` den rAF der Seite unter Firefox/WebGPU laufen lässt. Weg: `renderer.dispose()` nach dem Drain begrenzt um zwei Animation Frames aufschieben, in allen drei Konstruktionspfaden, mit Browser-Test, der vorher rot ist; `Folge von: Paket 2` entfällt. Der neue Zug 0 prüft gegen den Basis-Commit aa6dcc4e, ob der Stillstand vorbestand; im Scope ist er so oder so, nach der Scope-Regel (2026-09-24, Nutzer)
- `FrameLoop`: `frameNo`, `now`, `deltaTime`, `measuredFps` werden wie die Felder von `Display` nur lesbar (Getter). Breaking Change unter `[Unreleased]` mit CHANGELOG- und Migration-Guide-Eintrag, eigene Aufrufer werden mitgezogen (2026-09-24, Nutzer)
- Drain der Befund-Queue: die Einträge `→ Scope` gehen in Paket 5 (Test-Hygiene der Dispose/Resize-Browser-Tests) und Paket 6 (Quelltext, API, TSDoc); die Einträge `→ Audit` werden im Abschluss ins Audit gebucht (2026-09-24, Orchestrator nach der Scope-Regel)
- Drain-Runde 3: die kleinen Reviewer-Befunde der Display-Domäne, die keine Runde ausgelöst haben, gehen als Paket 8 in diesen Lauf statt ins Audit — der Nutzer hat ausdrücklich verlangt, dass auch neu auftretende Issues der Domäne in diesem Lauf behoben werden. Ausgenommen sind der rAF-Tick von three während der Pause und das Restrisiko der 2000-ms-Frist; beide liegen außerhalb der Reichweite des Laufs und gehen ins Audit (2026-09-24, Orchestrator nach Nutzervorgabe)

## Konventionen
Gelten für jede Zeile, die in diesem Lauf entsteht — Code, Kommentare,
Dokumentation, CHANGELOG, Migrations-Hinweise, Commit-Messages:
- Inline-Kommentare sind erwünscht, wo sie erklären, *warum* etwas so ist.
- Keine Finding-IDs, auch nicht in der Commit-Message. Sie gehören diesem einen
  Audit, sind danach tot, und die Commit-Message überdauert den Lauf. Sie leben
  in diesem Plan und sonst nirgends; die Verbindung zwischen Finding und Commit
  trägt das Feld `Hash:` unter dem Paket — in genau der Richtung, in der jemand
  sie später sucht. Eine Commit-Message sagt in eigenen Worten, was sie ändert.
- Kein Rückblick auf den Vorzustand: kein »früher«, kein »statt bisher«, kein
  »im Zuge des Audits umgestellt«. Der Test: Ergibt der Satz für jemanden Sinn,
  der den Vorzustand nie gesehen hat? Dann bleibt er. Braucht er ihn, gehört er
  in die Commit-Message — die Historie ist bereits konserviert.
- Projektspezifisch: `AGENTS.md` im Repo-Root vor der ersten Änderung lesen.
  `pnpm run ci` ist das Verify-Gate. CHANGELOG-Einträge folgen dem Skill
  `updating-changelog` (Keep a Changelog 1.1.0, nur unter `[Unreleased]`).
  Commit-Messages im Stil von `git log` (Conventional Commits, englisch).
  Niemals `pnpm publishNpmPkg` oder `scripts/publishNpmPkg.mjs` ausführen.

## Vorbestehende Fehler
- keine — Baseline vollständig grün

## Offene Befunde
Nebenbefunde aus den Paketen: was auch ohne diesen Lauf falsch war. Jeder
Eintrag wird beschlossen, bevor der Lauf endet — Paket oder Rückgabe ins Audit.
Ein leerer Abschnitt ist Abschlussbedingung, kein Zufall. Das Urteil am Ende
der Zeile misst den Eintrag an der Scope-Regel oben: `→ Scope`, `→ Audit`,
`→ Rückfrage`.
- [x] `packages/twopoint5d/src/display/Chronometer.ts:145-160` — `start(time)` setzt `#currentTime` ohne Klemme: nach `stop(1000)` schiebt `start(990)` die Zeit zurück, `time` wird kleiner; das `Display` füttert beide mit `performance.now()` und trifft es nicht, andere Nutzer des öffentlichen `Chronometer` schon (Paket 1, low) → Scope · → Paket 6 (Drain, 2026-09-24) · jetzt `:146-158`, dieselbe Ursache in `stop()` `:125-131` mit aufgenommen, im Detailplan von Paket 6 (Schritt 1) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/Display.ts:785` — `get pause()` prüft nur `state === PAUSED`: ein gestopptes, nie gestartetes Display (Zustand `NEW`, `pausedByUser` gesetzt) meldet `pause === false`, auch nach einem gewonnenen `start(); stop()` (Paket 1, low) → Scope · → Paket 6 (Drain, 2026-09-24) · jetzt `:1035-1037`, im Detailplan von Paket 6 (Schritt 2) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/Display.ts:344` — TSDoc von `resizePollIntervalMs` sagt »matching the legacy behavior«, ein Rückblick auf einen Vorzustand (Paket 1, info) → Scope · deckungsgleich mit DOC-064 → Paket 2 (Zug 0, 2026-09-24), schließt mit dessen Commit · geschlossen mit a33a6961 (abgehakt in Zug 0 von Paket 5: `Display.ts` enthält »legacy« nicht mehr)
- [x] `packages/twopoint5d/src/display/Display.ts:625` — verwaister Kommentar `// TODO check if this is still needed` über `...params` im Standard-`makeRenderer` (Paket 1, info) → Scope · deckungsgleich mit READ-019 → Paket 4 (Zug 0 von Paket 2, 2026-09-24), schließt mit dessen Commit · geschlossen mit 78ee4952
- [x] `packages/twopoint5d/src/display/Display.ts:218` — Klassen-TSDoc »Lifecycle«, Punkt 2: »`await display.start()` — awaits renderer init, fires `OnDisplayInit` (once), then `OnDisplayStart`« beschreibt nur den Start bei sichtbarem Tab und Canvas; die TSDoc von `start()` nennt die Pause-Fälle richtig (Paket 1, info) → Scope · → Paket 6 (Drain, 2026-09-24) · jetzt `:380-381`, im Detailplan von Paket 6 (Schritt 5) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/DisplayStateMachine.ts:131-162` — ein Init-Listener, der synchron `stateMachine.start()` ruft, bekommt `Restart` direkt nach `Init`, ohne vorherigen Start; `Display` löst das nicht aus (`start()` läuft erst nach einem `await` weiter) (Paket 1, info) → Scope · → Paket 6 (Drain, 2026-09-24) · unverändert; dieselbe Ursache beim Restart-Listener (über `Display` erreichbar) mit aufgenommen, im Detailplan von Paket 6 (Schritt 3) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d-testing/test/display-resize.test.js:100` und `:413` — `sizeRef`/`altRef` werden ohne `finally` aus dem DOM genommen; wirft eine Assertion vorher, bleibt der Container hängen (Paket 1b, low) → Scope · → Paket 5 (Drain, 2026-09-24) · jetzt `:101-111`/`:446-462`, im Detailplan von Paket 5 (Schritt 9) · geschlossen mit d38cc402
- [x] `packages/twopoint5d-testing/test/display-resize.test.js:103` — Testtitel nennt `resizeToCallback`, die Option heißt `resizeTo` (Paket 1b, info) → Scope · → Paket 5 (Drain, 2026-09-24) · jetzt `:114`, im Detailplan von Paket 5 (Schritt 9) · geschlossen mit d38cc402
- [x] `packages/twopoint5d-testing/test/display-dispose.test.js:28` und ab `:248` — Backend-Typannotation und WebGPU/WebGL-Unterscheidung doppelt (`expectLiveBackend` und Sonde) (Paket 1b, info) → Scope · → Paket 5 (Drain, 2026-09-24) · jetzt `:24-30`/`:370-375`, im Detailplan von Paket 5 (Schritt 6e) · geschlossen mit d38cc402
- [x] `packages/twopoint5d/src/display/Display.ts:376` — Klassen-TSDoc »Resize model«: »`OnDisplayResize` is emitted **exactly once** per frame«; ein manueller `resize()` zwischen zwei Frames emittiert zusätzlich (ebenso `:447`) (Paket 2, low) → Scope · → Paket 6 (Drain, 2026-09-24) · jetzt `:477-484` und `:544-550`, im Detailplan von Paket 6 (Schritt 6) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/Display.ts:468` — TSDoc von `pixelZoom`, Kommentarzeile über 100 Zeichen (Paket 2, info) → Scope · jetzt `:530`, gleiche Ursache wie DOC-033 → Paket 4 (Zug 0, 2026-09-24), geschlossen mit 78ee4952
- [x] `packages/twopoint5d/CHANGELOG.md:248` — `[Unreleased]`-Eintrag »fix `Stylesheets.installRule()`«, zweiter Satz »The sheet no longer grows by a rule per `Display`…« blickt auf einen Vorzustand zurück (aus 1813d635, vor diesem Lauf) (Paket 3, klein) → Scope · gleiche Ursache wie DOC-023 → Paket 4 (Zug 0, 2026-09-24), geschlossen mit 78ee4952
- [ ] `packages/twopoint5d-testing/test/stage-pipeline.test.js:23` — Titel verspricht »runs once per frame«, geprüft wird nur `runs > 0` (Paket 1b, low) → Audit
- [ ] `packages/twopoint5d-testing/test/pan-control-switch-off.test.js:6` — Kommentar spricht von W, S, A und D, die Datei hat nur `KEY_NORTH` (Paket 1b, info) → Audit
- [ ] `packages/twopoint5d-testing/test/pan-control-keys.test.js:32` — Inline-Literal `{x: 0, y: 0, pixelRatio: 1}` statt `makeState()` (Paket 1b, info) → Audit
- [ ] `packages/twopoint5d-testing/test/map2d-tile-upload.test.js:24`, `map2d-visibility-helpers.test.js:49` — bauen inline dieselbe Kamera wie `makeCamera` in `map2d-placement.test.js` (Paket 1b, info) → Audit
- [x] `packages/twopoint5d/src/display/FrameLoop.ts:197-200` — `frameNo`, `now`, `deltaTime` und `measuredFps` von `FrameLoop` sind öffentlich schreibbare Instanzfelder, dieselbe Art wie die drei, die Paket 4 an `Display` nur lesbar gemacht hat (Paket 4, low) → Scope · Nutzer 2026-09-24: nur lesbar machen → Paket 6 · unverändert, im Detailplan von Paket 6 (Schritt 4) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/FrameLoop.ts:47` — `RAF#frameNo` und die `measure*`-Felder des modulinternen `RAF` sind schreibbar (Paket 4, info) → Scope · → Paket 6 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 6 (Schritt 4) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/FrameLoop.spec.ts:371` — einzeilige TSDoc mit 109 Zeichen, über der Breite der Umgebung; der awk-Check aus Paket 4 kennt `/**` am Zeilenanfang nicht (Paket 4, info) → Scope · → Paket 6 (Drain, 2026-09-24) · unverändert; dieselbe Ursache in `FixedFrameLoop.ts:285` (Folge von Paket 4) mit aufgenommen, im Detailplan von Paket 6 (Schritt 7) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/src/display/Display.ts:557-567` — TSDoc von `width`/`height` sagt »recalculated for each frame«; bei gesetztem `resizePollIntervalMs` stimmt das nicht (gleiche Ursache wie der Eintrag `Display.ts:376`) (Paket 4, info) → Scope · → Paket 6 (Drain, 2026-09-24) · jetzt `:597-609`; »each frame« an neun weiteren TSDoc-Stellen (`Display.ts`, `types.ts`) mit aufgenommen, im Detailplan von Paket 6 (Schritt 6) · geschlossen mit a96d0ca3
- [x] `packages/twopoint5d/CHANGELOG.md:1799` — Überschrift im Migration Guide »The event keys of `FrameLoop` carry the library namespace« spricht von mehreren Schlüsseln, veröffentlicht ist nur `FrameLoop.OnFrame` (Paket 4, info) → Scope · → Paket 6 (Drain, 2026-09-24) · unverändert, unter `[Unreleased]`, im Detailplan von Paket 6 (Schritt 8) · geschlossen mit a96d0ca3
- [ ] `packages/twopoint5d-testing/test/pan-control-stylesheet-root.test.js:42` — `puts the rule in the stylesheet of the document when no root is named` sucht irgendeine `.PanControl2D-`-Regel im Sheet des Dokuments; Regeln früherer Tests bleiben dort stehen, der Test ist grün, auch wenn der Control selbst keine Regel anlegt (Paket 4, low) → Audit
- [x] `packages/twopoint5d/src/display/Display.ts:63-68` — Kommentar über `drainSubmittedWork()` nennt »submitted work still in flight« als Auslöser des Firefox-Stillstands; die Sonden aus Paket 5 zeigen ihn auch bei leerer Queue (Paket 5, info) → Scope · Nutzer 2026-09-24 → Paket 5 · im Detailplan (Schritt 3) · geschlossen mit d38cc402
- [x] `packages/twopoint5d/src/display/Display.ts:1445-1447` — `renderer.dispose()` im Zweig `!handBack` trifft auch einen übernommenen `WebGPURenderer`, dessen Canvas im Dokument bleibt; nach einem `render()` steht unter Firefox/WebGPU danach der rAF (Paket 5, high geschätzt) → Scope · Nutzer 2026-09-24 → Paket 5 · vorbestehend (Basis-Sonde gegen aa6dcc4e rot), dritter Konstruktionspfad des Hauptbefunds, im Detailplan (Schritt 4, Test 7a) · geschlossen mit d38cc402
- [x] `packages/twopoint5d-testing/test/display-dispose.test.js:180-183` — Begründung für `getContext()` statt `render()` nennt »work still in flight« als Auslöser, nicht zutreffend (Paket 5, info) → Scope · Nutzer 2026-09-24 → Paket 5 · im Detailplan (Schritt 6c) · geschlossen mit d38cc402
- [x] `packages/twopoint5d-testing/test/display-resize.test.js:218-225` — baut das Größen-Referenzelement von Hand, obwohl dieselbe Datei in `:34-44` `makeSizeRef()` dafür hat (Paket 5, info) → Scope · nicht in Paket 6 (Zug 0, 2026-09-24): Test-Hygiene einer Datei, die Paket 6 nicht ändert, andere Ursache → Drain-Runde des Abschlusses · → Paket 7 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 7 (Schritt 4.1) · geschlossen mit e1b9f2ff
- [x] `packages/twopoint5d-testing/test/display-resize.test.js:85`, `:195`, `:488` — `off(display, OnDisplayResize, …)` steht nicht in einem `finally`; harmlos, weil `dispose()` im `afterEach` alle Listener löst (Paket 5, info) → Scope · nicht in Paket 6 (Zug 0, 2026-09-24), wie der Eintrag darüber → Drain-Runde des Abschlusses · → Paket 7 (Drain, 2026-09-24) · unverändert; dieselbe Ursache in `:501` (anonymer Listener) und `:232`/`:424` (`new Display` vor dem `try`) mit aufgenommen, im Detailplan von Paket 7 (Schritte 4.2–4.6) · geschlossen mit e1b9f2ff
- [x] `packages/twopoint5d/src/display/FixedFrameLoop.ts:82` — Klassen-TSDoc nennt das Ereignis `OnRenderFrame`, es heißt `OnDisplayRenderFrame` (Paket 6, info) → Scope · → Paket 7 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 7 (Schritt 2) · geschlossen mit e1b9f2ff
- [x] `packages/twopoint5d/src/display/DisplayStateMachine.ts:18` — `state` ist ein öffentlich schreibbares Feld, dieselbe Art wie die `FrameLoop`-Felder; die Klasse ist nicht über `public-api.ts` exportiert, also nur intern erreichbar (Paket 6, info) → Scope · → Paket 7 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 7 (Schritt 1) · geschlossen mit e1b9f2ff
- [x] `packages/twopoint5d/src/display/Chronometer.ts:15` — Kommentar an `#currentTime` »set by calling the `update()` method« ist unvollständig: `start()`, `reset()` und der Konstruktor setzen es ebenfalls (Paket 6, info) → Scope · → Paket 7 (Drain, 2026-09-24) · unverändert; dieselbe Ursache an `#lostTime`, `#recentlyLostTime`, `get lostTime()` (vorbestehend) und `#pausedAt` (Folge von Paket 6) mit aufgenommen, im Detailplan von Paket 7 (Schritt 3) · geschlossen mit e1b9f2ff
- [x] `packages/twopoint5d/src/display/Display.ts` (`#emit(OnDisplayInit)` im Init-Handler, Stand 6a4bcacf `:677`) — ein `OnDisplayInit`-Listener, der synchron wirft, lässt `start()` rejecten, während `#initMustBeCalled` schon `false` und der Zustand `NEW` ist; ein zweites `start()` emittiert dann `OnDisplayRestart` statt `OnDisplayInit`, und spätere Listener sowie späte Subscriber bekommen `OnDisplayInit` nie (Reviewer Paket 1, low) → Scope · → Paket 8 (Drain, 2026-09-24) · unverändert: `Display.ts:943`, Ursache `DisplayStateMachine.ts:157-164` (Flag vor dem `emit`, vorbestehend), im Detailplan von Paket 8 (Schritte 1–3) · geschlossen mit bd80d232
- [x] `packages/twopoint5d-testing/test/display-dispose.test.js:257-269` — der Test »a dispose() before the renderer is ready leaves the frame loop empty« kann auf dem Dispose-Pfad nicht mehr scheitern, weil das Display erst in `start()` abonniert; der Titel verspricht mehr, als er prüft (Reviewer Paket 1, low) → Scope · → Paket 8 (Drain, 2026-09-24) · jetzt `:302-336`, Titelverweis in `:62`, im Detailplan von Paket 8 (Schritt 4) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/CHANGELOG.md:140`, `:896-897` — »throws a TypeError at runtime« gilt nur für Strict-Mode-Code (Reviewer Paket 4, low) → Scope · → Paket 8 (Drain, 2026-09-24) · jetzt `:140`/`:898-899`; dieselbe Wendung in `:141`/`:921-922` (Folge von Paket 6) mit aufgenommen, im Detailplan von Paket 8 (Schritt 5) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/CHANGELOG.md:244` — `[Unreleased]`-Eintrag »fix a Display that is disposed before its renderer is ready …« nennt einen Mechanismus, den es nicht mehr gibt (Reviewer Paket 1, info) → Scope · → Paket 8 (Drain, 2026-09-24) · jetzt `:256`, im Detailplan von Paket 8 (Schritt 6) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/CHANGELOG.md:12` — Added-Eintrag zu `pauseOutsideViewport` sagt zweimal »as a hidden tab does« (Reviewer Paket 1, info) → Scope · → Paket 8 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 8 (Schritt 6) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/src/display/Display.ts` (TSDoc von `start()`, Stand 6a4bcacf `:1062`) — überzähliges Komma nach dem Gedankenstrich »waits for the renderer —, the display goes« (Reviewer Paket 1, info) → Scope · → Paket 8 (Drain, 2026-09-24) · jetzt `:1373`; dasselbe `—,` an neun weiteren Stellen aus den Paketen 2, 4 und 6 (`Display.ts`, `Stylesheets.ts:105`, `docs/architecture.md:26`, `CHANGELOG.md:325`) mit aufgenommen, im Detailplan von Paket 8 (Schritt 9) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/src/display/Display.ts` (Klassen-TSDoc »Lifecycle« Punkt 3, Stand a33a6961 `:299`) — eine Zeile (~93 Zeichen) bricht anders um als ihre Nachbarn (~75) (Reviewer Paket 2, info) → Scope · → Paket 8 (Drain, 2026-09-24) · an der Fundstelle gegenstandslos (d38cc402 hat Punkt 3 neu umbrochen); dieselbe Ursache in Punkt 2, Punkt 4 und »Resize model« (Zeilen bis 89 Zeichen) mit aufgenommen, im Detailplan von Paket 8 (Schritt 8) · geschlossen mit bd80d232
- [x] `packages/twopoint5d-testing/test/display-adopt-renderer.test.js:73-74` — Kommentar »once the GPU has run dry and the page has drawn two more frames« ohne »under WebGPU«; unter WebGL wird nicht gewartet (Reviewer Paket 5, info) → Scope · → Paket 8 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 8 (Schritt 7) · geschlossen mit bd80d232
- [x] `packages/twopoint5d/src/display/Display.ts:110-119` — Kommentar über `waitForTwoAnimationFrames()` begründet das Warten nur mit übergebenem Canvas und übernommenem Renderer; die Funktion wartet auch im Host-Pfad, dessen Canvas wieder in ein Dokument gelangen kann (Sonde P4) (Reviewer Paket 5, info) → Scope · → Paket 8 (Drain, 2026-09-24) · unverändert, im Detailplan von Paket 8 (Schritt 7) · geschlossen mit bd80d232
- [ ] `packages/twopoint5d/src/display/Display.ts` (Pause-Pfad) mit `three/src/renderers/common/Animation.js:69-91` — ein pausiertes Display führt keinen twopoint5d-Code pro Frame mehr aus, aber three 0.185.1 startet in `renderer.init()` eine eigene rAF-Schleife, die bis `renderer.dispose()` weiterläuft; `setAnimationLoop(null)` nimmt nur den Callback (Abweichung Paket 1, low, improvement) → Audit · außerhalb der Reichweite dieses Laufs, hängt an three
- [ ] `packages/twopoint5d/src/display/Display.ts` (`SUBMITTED_WORK_TIMEOUT_MS = 2000`, `drainSubmittedWork()`) — eine Seite, die länger als die Frist verborgen bleibt, präsentiert den Canvas erst beim Sichtbarwerden, dann ist das Device schon zerstört; headless nicht prüfbar (Restrisiko Paket 5, info) → Audit
- [ ] `packages/twopoint5d/src/display/Display.ts` (Start-Handler `:947-958`) mit `DisplayStateMachine.ts` (`#initOrRestartThenStart()`, `state = RUNNING` vor `emit(Start)`) — ein `OnDisplayStart`-Listener, der synchron wirft: das Display läuft (`RUNNING`, am Frame-Loop), `start()` rejected, spätere Listener hören `OnDisplayStart` nie, und weil `emit()` abbricht, bevor der Wert retained ist, auch späte Subscriber nicht (Zug 0 Paket 8, low, vorbestehend — aa6dcc4e setzt `RUNNING` ebenso vor dem `emit`) → Rückfrage · nicht in Paket 8: andere Ursache als der werfende Init-Listener; ein Fix verlangt eine Designentscheidung (Frame-Loop und Chronometer zurücknehmen oder eine Pause emittieren) und wäre die vierte Drain-Runde derselben Fläche → Audit mit der Frage (Orchestrator, 2026-09-24)
- [ ] `packages/twopoint5d/src/stage/fitIntoRectangle.ts:189`, `packages/twopoint5d/src/utils/isPowerOf2.ts:3` — überzähliges Komma hinter dem schließenden Gedankenstrich (`—,`), dieselbe Gewohnheit, die Paket 8 in der Display-Domäne behebt (Zug 0 Paket 8, info, vorbestehend: 69e27081, a25e9275) → Audit · außerhalb der Display-Domäne

## Pakete

### [x] 1. Lifecycle und Frame-Loop: start/stop/pause, Event-Reihenfolge, rAF-Anbindung
- Findings: ASYNC-005 (medium), ASYNC-002 (medium), PERF-022 (low), IMPL-009 (info), BUG-118 (low), TEST-004 (low, Display-Anteil), TEST-042 (medium, Display-Anteil ohne dpr-Clamp)
- Ziel: Das Display startet, pausiert, stoppt und meldet seine Events in der dokumentierten Reihenfolge — auch bei überlappenden Aufrufen —, hängt nur am rAF-Treiber, solange es läuft, pausiert auf Wunsch außerhalb des Viewports, und diese Pfade sind durch eine Display-Spec und Browser-Tests abgesichert.
- Bereich: `packages/twopoint5d/src/display/` (Display.ts, Chronometer.ts, types.ts, neu Display.spec.ts), `packages/twopoint5d-testing/test/display-lifecycle.test.js` (neu), `packages/twopoint5d/CHANGELOG.md`
- Detail: docs/remediation/paket-1.md
- Geteilt in Zug 0 (2026-09-24): TEST-024 und READ-017 → Paket 1b. Die Helfer-Extraktion ist Transkription über rund zwanzig Testdateien, dieses Paket Nebenläufigkeit und öffentliche API — zwei Modell- und Effort-Profile, ein Implementierer für beides füllte seinen Kontext mit Testdateien, bevor er an die Race-Logik kommt.
- Abgegeben an Paket 2: der dpr-Clamp-Anteil von TEST-042 (siehe dort).
- Hängt ab von: —
- Hash: 6a4bcacf
- Ergebnis: 3 Runden · ASYNC-005, ASYNC-002, PERF-022, IMPL-009, BUG-118, TEST-004 (Display-Anteil) und TEST-042 (Display-Anteil ohne dpr-Clamp) behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstests vor dem Fix rot (11 failed / 28 passed): u. a. `Display > start() > emits init before start, both before its promise resolves, and replays them to a late listener in that order`, `… a stop() while start() waits for the renderer keeps the display from starting`, `Display > frame loop > keeps the display on its frame loop only while it runs`, `Chronometer > an update() with an earlier time lets no time pass while running`; Runde 1 behob eine Folge des synchronen Init-Handlers (Pause aus Init-/Restart-Listener verpuffte), Regressionstests `DisplayStateMachine > a pause set by an init listener holds the state machine in the pause` u. a. vor dem Fix rot (5 failed / 22 passed) · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (6)
- Folgen: keine offen — `CHANGELOG.md:139` (Aufzählung der `createRenderer`-Schlüssel) in Runde 1 mitgezogen
- Schnittstellen: `DisplayParameters.pauseOutsideViewport?: boolean` neu (Konstruktor-Option, erreicht `createRenderer` nicht) · ein `Display` meldet sich erst beim Start an `display.frameLoop` an und bei jeder Pause ab: `frameLoop.subscriptionCount` ist vor dem ersten Start und in der Pause `0` · `OnDisplayInit` geht synchron vor `OnDisplayStart` hinaus · `DisplayStateMachine.start()`: pausiert ein Listener von `Init`/`Restart`, folgt `Pause` statt `Start` (privater Zähler `#transitions`) · `Chronometer#update()` klemmt `deltaTime` auf `>= 0` · neue Specs `src/display/Display.spec.ts` (Stubs für `window`, `document`, `getComputedStyle`, `IntersectionObserver`, Renderer; `vi.mock` auf `./Stylesheets.js`) · für Paket 1b: `packages/twopoint5d-testing/test/display-lifecycle.test.js` trägt lokale Kopien von `makeContainer` und `disposeDisplay` (`FIXTURE_ID = 'display-lifecycle-fixture'`) und die lokalen Helfer `animationFrames`, `recordEvents`, `nextEvent`

### [x] 1b. Gemeinsame Test-Helfer der Browser-Suite
- Findings: TEST-024 (low), READ-017 (info)
- Ziel: Die Browser-Suite holt ihre Fixture-Helfer aus einem gemeinsamen Modul `test/helpers/fixtures.js`, keine Testdatei trägt mehr eine eigene Kopie davon, jedes Display geht mit demselben Teardown ab, und der Kommentar in `display-resize.test.js` liest sich ohne Vorzustand.
- Bereich: `packages/twopoint5d-testing/test/helpers/fixtures.js` (neu), 22 Testdateien der Browser-Suite (jede mit eigener Kopie eines Helfers oder mit nacktem `dispose()` im Teardown — auch `display-lifecycle.test.js` aus Paket 1), `eslint.config.mjs` (Browser-Globals für das Modul), `AGENTS.md` (ein Satz zum gemeinsamen Modul)
- Detail: docs/remediation/paket-1b.md
- Aufgenommen in Zug 0 (2026-09-24): die Kopien von `makeMap`/`makeRectMap` in den drei map2d-Tests — dieselbe Ursache wie TEST-024, dieselben Dateien; stehen gelassen verfehlten sie das Ziel in genau den Dateien, die das Paket ändert.
- Hängt ab von: 1 (dessen neuer Browser-Test bringt zwei weitere Kopien mit, die hier eingesammelt werden)
- Aus Paket 1 geteilt (2026-09-24). Steht vor Paket 2, damit die neuen Display-Tests der Pakete 2–4 die gemeinsamen Helfer schon vorfinden.
- Abgleich (Zug 0 von 1b, gegen 6a4bcacf): `start()` im Teardown gegenstandslos seit 7ec25649; offen sind die Extraktion (inzwischen 15 Kopien `makeContainer`, 12 `disposeDisplay`) und der zweite Teardown-Vertrag — fünf Dateien rufen im `afterEach` nacktes `dispose()`. READ-017 unverändert, jetzt `display-resize.test.js:467-468`.
- Hash: cf1304c7
- Ergebnis: 1 Runde · TEST-024 und READ-017 behoben, vom Reviewer mit Fundstelle bestätigt · kein Bugfix-Paket: Testzahlen vorher = nachher (Chromium 164 passed/1 skipped, Firefox 160/5, `it(` 165) · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (7)
- Folgen: keine
- Schnittstellen: neues Modul `packages/twopoint5d-testing/test/helpers/fixtures.js` mit `makeContainer({width, height, id})`, `disposeDisplay(display)`, `pointer(type, {x, y, buttons, pointerId, pointerType, target})`, `key(type, init)`, `makeState()`, `bufferOf(attr)`, `readBack(renderer, attr)`, `quadDescription`, `instancedDescription`, `makeMap(visibilitor)` → `{map2d, tileSprites, tileRenderer}` · neue Browser-Tests der Pakete 2–4 importieren von dort statt zu kopieren · `eslint.config.mjs` gibt `test/helpers/*.js` die Browser-Globals

### [x] 2. Resize-Pipeline und Canvas-Ownership
- Findings: BUG-117 (medium), BUG-119 (low), BUG-120 (low), MEM-018 (low), READ-001 (low, nur Display.ts), DOC-030 (low), DOC-041 (info), DOC-063 (low), DOC-064 (low), DOC-065 (low), TEST-042 (medium, Display-Anteil: nur der dpr-Clamp)
- Ziel: `resize()` liest sich als Pipeline, klemmt in Device-Pixeln, validiert Callback-Ergebnisse, wendet `styleImageRendering` beim nächsten `resize()` an, auch ohne Größenänderung, und `dispose()` gibt einen übergebenen Canvas so zurück, wie das Display ihn vorgefunden hat — mit TSDoc, die dem Verhalten entspricht.
- Bereich: `packages/twopoint5d/src/display/Display.ts`, `types.ts`, `Display.spec.ts`, `docs/resource-lifecycle.md`, `CHANGELOG.md`, `packages/twopoint5d-testing/test/display-resize.test.js`, `display-dispose.test.js`, `display-constructor.test.js` (Renderer-Stub)
- Detail: docs/remediation/paket-2.md
- Aufgenommen in Zug 0 (2026-09-24): DOC-065 aus Paket 4 — BUG-119 schreibt genau die TSDoc von `styleImageRendering` neu, deren abgebrochenen Satz DOC-065 meint. Der Nebenbefund `Display.ts:344` aus Paket 1 ist deckungsgleich mit DOC-064 und schließt mit ihm.
- Ziel präzisiert in Zug 0 (2026-09-24): `styleImageRendering` wirkt beim nächsten `resize()` statt über einen Setter — so wertet das Resize-Modell alles aus, und ein Wechsel löst weder `setDrawingBufferSize()` noch `OnDisplayResize` aus. MEM-018 nimmt auch die Drawing-Buffer-Attribute `width`/`height` zurück: nur mit ihnen stimmt »not modified« wörtlich, wie die Entscheidung es verlangt.
- Abgegeben an Paket 3 (Zug 0, 2026-09-24): die Randbemerkung aus READ-001 zum `styleSheetRoot` (siehe dort).
- Übernommen aus Paket 1 (2026-09-24): der Browser-Test zum Device-Pixel-Clamp bei dpr > 1 aus dem Display-Anteil von TEST-042 — derselbe Test, den die Entscheidung zu BUG-117 verlangt; in Paket 1 geschrieben, prüfte er das Verhalten, das erst dieses Paket richtigstellt.
- Hinweis aus Paket 1: `dispose()` bleibt dort zeichengleich; wer ihn hier ändert (MEM-018), zieht das wörtliche Zitat in `docs/resource-lifecycle.md` (Abschnitt um Zeile 161) mit.
- Hängt ab von: —
- Hash: a33a6961
- Ergebnis: 2 Runden · BUG-117, BUG-119, BUG-120, MEM-018, READ-001 (Display.ts), DOC-030, DOC-041, DOC-063, DOC-064, DOC-065 und TEST-042 (dpr-Clamp) behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstests vor dem Fix rot — Spec 5 failed / 18 passed: `Display > resize() > clamps the drawing buffer to MaxResolution in device pixels`, `… rounds a clamped size down, so the drawing buffer stays within MaxResolution at a fractional pixel ratio`, `… holds the CSS size to MaxResolution while pixelZoom is above 0`, `… applies styleImageRendering without a size change, also within resizePollIntervalMs`, `… treats a resizeTo result without two finite numbers as no size`; Browser 4 failed: `clamps the drawing buffer to Display.MaxResolution in device pixels at a device pixel ratio of 2`, `applies styleImageRendering on the next frame without a size change`, `gives a canvas that was handed in back as it found it`, `gives a canvas that was handed in its own classes, styles and attributes back, the fullscreen class included` · Abweichung vom Detailplan: das Attribut `data-engine`, das three auf den Canvas schreibt, geht bei `dispose()` mit zurück · Runde 1 behob zwei wichtige Review-Befunde (`resize()`-TSDoc zur Emission, Rückblick-Wendungen im CHANGELOG) · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (2)
- Folgen: `packages/twopoint5d/src/display/Display.ts:1293` (`#giveBackCallersCanvas()` in `dispose()`) — landet `dispose()` mitten in der WebGPU-Init, schreibt three `WebGPUBackend.init()` (`three/src/renderers/webgpu/WebGPUBackend.js:347`) `data-engine` nach dem synchronen Zurückgeben erneut auf den übergebenen Canvas, und niemand nimmt es zurück; die »not modified«-Aussage in `dispose()`-TSDoc und `docs/resource-lifecycle.md` §1 gilt in diesem Fall nicht. Ungetestet. → Paket 3 (Renderer-Init/-Freigabe), siehe dort
- Schnittstellen: `ResizeDisplayToFn` gibt `[width: number, height: number] | undefined` zurück; ein Paar mit nicht endlichem Wert gilt als keine Größe · `Display` ruft am Renderer nur `setDrawingBufferSize(width, height, pixelRatio)` — ein Stub oder ein eigener `createRenderer` braucht die Methode, `setPixelRatio()`/`setSize()` ruft er nicht mehr · `resize()` = `#applyImageRendering()` (vor dem Poll-Intervall, außerhalb des Hashs) → `#resolveSizeSource()` → `#applyFullscreenClass()` → `#measureSizeSource()` → `#applyMeasuredSize()` · private Felder `#fullscreenClassName`, `#fullscreenClassApplied`, `#canvasClassName`, `#callersCanvasBefore` · modulweit `readCanvasState()`/`restoreCanvasState()` mit `CANVAS_STATE_ATTRIBUTES` (`class`, `style`, `touch-action`, `width`, `height`, `data-engine`) · Konstruktor liest den Zustand eines übergebenen Canvas vor `makeRenderer` und behält ihn nur, wenn `renderer.domElement === canvas`; `dispose()` ruft `#giveBackCallersCanvas()` synchron vor `#releaseRenderer()` · der `typeof document`-Guard im Konstruktor ist entfernt · `docs/resource-lifecycle.md` §4 zitiert `dispose()` zeichengleich — wer ihn ändert, zieht das Zitat mit

### [x] 3. Stylesheets und Renderer-Ressourcen
- Findings: MEM-017 (medium), SEC-002 (low), MEM-019 (low), ASYNC-006 (info), READ-020 (low), DOC-031 (info)
- Ziel: Stylesheets überstehen nicht eingehängte ShadowRoots und Reconnects über Constructable Stylesheets, nehmen nur validierte Werte an, und Renderer-Init/-Freigabe hinterlässt weder Listener noch ein unbegrenztes Warten.
- Bereich: `packages/twopoint5d/src/display/Stylesheets.ts`, `Display.ts` (Konstruktor, `styleSheetRoot`, Renderer-Freigabe), `types.ts` (TSDoc), `Display.spec.ts`, `packages/twopoint5d/src/controls/PanControl2D.ts` (Cursor-Regel), `docs/resource-lifecycle.md` §1, `docs/architecture.md`, `CHANGELOG.md`, Browser-Tests `stylesheets.test.js`, `pan-control-cursor.test.js`, `display-constructor.test.js`, `display-dispose.test.js`, neu `display-stylesheets.test.js`
- Detail: docs/remediation/paket-3.md
- Übernommen aus Paket 2 (Zug 0, 2026-09-24): die Randbemerkung aus READ-001 — die Fullscreen-Regel wird in den `styleSheetRoot` des ersten Fullscreen-Frames installiert, eine spätere Neuzuweisung von `Display#styleSheetRoot` erreicht sie nicht (die Canvas-Regel aus dem Konstruktor ebenso wenig). Dieselbe Frage wie MEM-017: wo die Regeln eines Displays liegen. Kein eigenes Finding; READ-001 wird in Paket 2 an seiner Empfehlung gemessen.
- Randbemerkung entschieden in Zug 0 (2026-09-24): `Display#styleSheetRoot` wird ein Accessor-Paar, dessen Setter die Regeln des Displays im neuen Root installiert — nicht brechend; `readonly` wäre eine API-Änderung, die keine Entscheidung deckt, und ein Feld, dessen Schreiben nichts bewirkt, eine Falle.
- Folge aus Paket 2, triagiert in Zug 0 (2026-09-24): gegenstandslos. three 0.185.1 schreibt `data-engine` nur im Renderer-Konstruktor (`Backend.getDomElement()`, `Backend.js:707`) und beim ersten Lesen von `WebGPUBackend#context` (`WebGPUBackend.js:347`), das nur `beginRender()`, `copyFramebufferToTexture()` und `getContext()` auslösen — weder die Init (`WebGPUBackend.js:197-297`) noch die Freigabe. Zwei Absicherungstests in `display-dispose.test.js` halten es im Browser fest; sind sie rot, meldet der Implementierer eine Folge, statt zu beheben.
- Hinweis aus Paket 2: der Konstruktor liest vor `makeRenderer` den Zustand eines übergebenen Canvas (`readCanvasState()`), und `dispose()` schreibt ihn zurück, samt der Klasse aus `Stylesheets.addRule` (`#canvasClassName`). Wer das Aufräum-`try` um den Konstruktor zieht (MEM-017) oder die Klassenvergabe umbaut, hält beides mit. — Eingearbeitet: das `try` beginnt nach `#callersCanvasBefore`, `#waitForRenderer` und `frameLoop`, der `catch` ruft `dispose()`.
- Hängt ab von: —
- Hash: e9c29c0b
- Ergebnis: 2 Runden · MEM-017, SEC-002, MEM-019, ASYNC-006, READ-020, DOC-031 und die Randbemerkung aus READ-001 (`Display#styleSheetRoot`) behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstests vor dem Fix rot — Spec 3 failed / 24 passed (`Display > release > releases the renderer after a bounded wait when the queue never answers, with one warning`, `… once the device reports itself lost, without waiting for the queue`, `Display > constructor > releases a renderer it has taken over when the constructor fails after taking it`); Browser: `stylesheets.test.js` 6/10 (u. a. `installs a rule in a shadow root whose host is not in the document yet`, `keeps its rules when the shadow host moves to another place in the document`, `escapes the class name in the selector of its rule`), `pan-control-cursor.test.js` 3/8, `display-stylesheets.test.js` 5/0, `display-constructor.test.js` 2/3, `display-dispose.test.js` 1/26 (`takes the webglcontextlost listener of a failed WebGL init off the canvas`) · Runde 1 behob den wichtigen Review-Befund (Constructable Sheet aus dem Modul-Realm → `NotAllowedError` bei einem Root im iframe), Regressionstests `Stylesheets > installs a rule in the document of an iframe` und `… in a shadow root inside an iframe` vor dem Fix rot (2/16), dazu `a retainRule() that throws leaves the control on the rule it holds` und `gives its cursor rule back to the sheet it took it from when the styleSheetRoot element moves` (bei zurückgedrehtem Setter rot, 2/11) · Abweichungen: `restoreCanvasState()` liest `style` vor `removeAttribute` (Chromium ließ `style=""` stehen — Folge von Paket 2, mitbehoben); die `data-engine`-Absicherung liest `renderer.getContext()` statt `render()` · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (1)
- Folgen:
  - `packages/twopoint5d/src/display/Display.ts:1386` (`dispose()` ruft `#giveBackCallersCanvas()` vor `#releaseRenderer()`) — Folge von Paket 2: unter Firefox/WebGPU setzt die Rückgabe `width`/`height` eines übergebenen Canvas synchron zurück, während noch GPU-Arbeit läuft; danach `GPUInternalError: Buffer with '' label has been destroyed`, und die Seite bekommt keinen `requestAnimationFrame` mehr. Vom Implementierer beobachtet (mit Host-Container grün, mit übergebenem Canvas rot, ausgelöst durch `render()` vor `dispose()`), ohne Test; vor Paket 2 hat `dispose()` `width`/`height` nicht angefasst. → triagiert in Zug 0 von Paket 4 (2026-09-24): echte Folge von Paket 2, geschnitten als Paket 5 · neu eingeordnet in Zug 0 von Paket 5 (2026-09-24): **vorbestehend**, keine Folge — Auslöser ist `renderer.dispose()` auf einem Canvas im Dokument, nicht die Rückgabe aus Paket 2; die Basis-Sonde gegen aa6dcc4e ist in Firefox 155 rot (siehe Paket 5)
  - `packages/twopoint5d/src/display/Stylesheets.ts:22-24` (`newSheetFor()`) — Folge von Paket 3 selbst: der Kommentar verspricht »A document without a window falls back on the constructor of this realm«; der Fallback wirft immer `NotAllowedError` (ein Dokument adoptiert nur ein Sheet, dessen constructor document es selbst ist — `createHTMLDocument()`, `DOMParser`, `template.content.ownerDocument`), und das nicht adoptierbare Sheet bleibt im Cache `sheets`. Kommentar ehrlich machen oder den Fall vor dem Cache mit klarer Meldung abweisen. → triagiert in Zug 0 von Paket 4 (2026-09-24): Symptom von Paket 3, Nachtragspaket mit Paket 4 zusammengelegt (abweisen mit klarer Meldung)
  - `packages/twopoint5d/src/display/Stylesheets.ts` `getGlobalSheet()` — Folge von Paket 3: es gibt kein globales Sheet mehr, sondern eines je Dokument oder ShadowRoot; der Name ist eine API-Frage für Paket 4 (API-065), ebenso der unbenutzte Export `globalStylesID` (`:2`). → triagiert in Zug 0 von Paket 4 (2026-09-24): Symptom von Paket 3, in Paket 4 (`getSheet()` neu, `getGlobalSheet()` als `@deprecated`-Alias, `globalStylesID` gelöscht)
- Schnittstellen: `Stylesheets` legt keine `<style>`-Elemente an: je Scope (Dokument oder ShadowRoot, modulintern `scopeOf(root)` über `root.getRootNode()`) ein Constructable Stylesheet aus dem `CSSStyleSheet` des Fensters dieses Dokuments (`newSheetFor(scope)`), bei jedem `getGlobalSheet()` in `scope.adoptedStyleSheets` (neu) eingetragen; `sheets` ist eine `WeakMap<Document | ShadowRoot, CSSStyleSheet>`; der Selektor läuft durch `CSS.escape()` · `globalStylesID` exportiert, aber unbenutzt · `Display#styleSheetRoot` ist ein Accessor-Paar (privat `#styleSheetRoot`); der Setter installiert Container-, Canvas- und Fullscreen-Regel über `#installRules(root)` im neuen Root, nach `dispose()` wirkungslos · Modulkonstanten `CONTAINER_RULE_CSS`, `CANVAS_RULE_CSS`, `FULLSCREEN_RULE_CSS`, `SUBMITTED_WORK_TIMEOUT_MS = 2000`, Modulfunktion `dropContextLostListener(renderer)` · `drainSubmittedWork()` wartet auf Queue, `device.lost` oder 2000 ms (dann `console.warn`) · der Konstruktor fasst alles nach `#callersCanvasBefore`, `#waitForRenderer` und `frameLoop` in ein `try`, dessen `catch` `dispose()` ruft und neu wirft · `PanControl2D#cursorPanStyle` nimmt nur Werte mit `CSS.supports('cursor', v)`, sonst `console.warn` und der alte Wert bleibt; als Konstruktor-Option greift dann `'none'`; die Regel geht an den Root zurück, für den sie retained wurde (`#cursorPanRuleRoot`, modulintern `pinnedRootOf()`) · `Display.spec.ts`: Renderer-Stub in `makeRenderer(init, backend)`, `makeDisplay()` nimmt `backend` als dritten Parameter · neue Browser-Datei `display-stylesheets.test.js`; `stylesheets.test.js` hat den Helfer `makeIframeDocument()`

### [x] 4. Öffentliche Display-API, Typen, Doku und Demos
- Findings: API-065 (low), TYPE-013 (low), DOC-033 (info), READ-019 (low), DOC-014 (low), DOC-023 (low), MEM-006 (info), CONS-045 (info)
- Abgegeben an Paket 2 (Zug 0 von Paket 2, 2026-09-24): DOC-065 — die TSDoc von `styleImageRendering` schreibt dort BUG-119 neu.
- Hinweis aus Paket 2: READ-019 ist deckungsgleich mit dem Nebenbefund `Display.ts:625` aus Paket 1 und schließt ihn mit. Die TSDoc von `frameNo` steht nach Paket 2 richtig (0 vor dem ersten Frame); API-065 macht `frameNo` nur lesbar und nimmt sie an den Getter mit.
- Ziel: Die Display-Oberfläche exportiert nur Gewolltes mit echten Signaturen und nur lesbarem Zustand, `Stylesheets` nennt sein Sheet nach dem, was es ist, und weist ein Dokument ohne Fenster mit klarer Meldung ab, Doku, README- und CHANGELOG-Einträge stimmen, und die Multi-Display-Demo räumt auf.
- Bereich: `packages/twopoint5d/src/display/public-api.ts` (+ neu `public-api.spec.ts`), `Display.ts`, `FrameLoop.ts`, `FixedFrameLoop.ts`, `Stylesheets.ts`, `Chronometer.ts` (Kommentar), die Specs dazu, Browser-Tests `stylesheets.test.js`, `pan-control-cursor.test.js`, `pan-control-stylesheet-root.test.js`, `packages/twopoint5d/README.md`, `CHANGELOG.md`, `docs/resource-lifecycle.md` §4, `docs/architecture.md` §1, `apps/lookbook/src/pages/demos/display-multi.astro`
- Detail: docs/remediation/paket-4.md
- Hinweis aus Zug 0 von Paket 3 (2026-09-24): nach Paket 3 benutzt `Stylesheets.ts` `globalStylesID` nicht mehr (kein `<style>`-Element, die Regeln liegen in adoptierten Constructable Stylesheets) — API-065 kann es löschen statt nur aus den Exporten zu nehmen; `postFixID` bleibt intern in Benutzung. `Display#styleSheetRoot` ist nach Paket 3 ein Accessor-Paar mit Setter, nicht Teil der »nur lesbar«-Liste der Entscheidung.
- Aufgenommen in Zug 0 (2026-09-24): die beiden `Stylesheets`-Folgen von Paket 3 — `newSheetFor()` weist ein Dokument ohne Fenster mit klarer Meldung ab, bevor etwas im Cache landet; `getGlobalSheet()` bekommt den Namen `getSheet()`, der alte bleibt ein Release lang `@deprecated`-Alias. Das Nachtragspaket für beide ist mit diesem zusammengelegt: dieselbe Funktion, die dieses Paket umbenennt, ruft `newSheetFor()` und füllt den Cache. Dazu die Nebenbefunde `Display.ts:530` und `CHANGELOG.md:248` aus der Queue (gleiche Ursache wie DOC-033 bzw. DOC-023) und neu `README.md:60` (gleiche Ursache wie DOC-014) sowie die überlangen Kommentarzeilen `Chronometer.ts:51`, `Display.spec.ts:437`, `Stylesheets.ts:181`.
- Folge von: Paket 3 — nur für die beiden `Stylesheets`-Folgen (zweite Generation); die Audit-Findings dieses Pakets sind erste Generation
- Entschieden in Zug 0 (2026-09-24): `OnRAF` und `ISetAnimationLoop` verlassen auch die öffentlichen Signaturen, sonst schlägt `checkNameableTypes` an — der `FrameLoop`-Konstruktor schreibt die Form `{setAnimationLoop(…)}` aus, `[OnRAF]` wird ein privates `#onRAF`. Beide waren in 0.21.2 nicht exportiert: sie verschwinden nur aus dem unveröffentlichten »export 30 types«-Eintrag, `Removed` gilt nur `postFixID` und `globalStylesID`. Auch die Typen aus `types.ts` stehen in `public-api.ts` benannt; `docs/architecture.md` §1 zieht mit.
- Hängt ab von: 3 (Stylesheets-Interna müssen feststehen, bevor die Exporte bereinigt werden)
- Hash: 78ee4952
- Ergebnis: 1 Runde · API-065, TYPE-013, DOC-033, READ-019, DOC-014, DOC-023, MEM-006, CONS-045, die beiden `Stylesheets`-Folgen von Paket 3 und die aufgenommenen Nebenbefunde (`Display.ts:530`, `CHANGELOG.md:248`, `README.md:60`, überlange Kommentarzeilen) behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstests vor dem Umbau rot: `display public API > exports the classes and functions of the display module and nothing else` und `Display > read-only state > renderer, frameLoop and frameNo are accessors without a setter` (2 failed / 27 passed), `FixedFrameLoop > onTick() and onRender() hand back the function that takes the handler off again` im Typecheck (TS2322 `unknown` → `UnsubscribeFunc`), Browser `Stylesheets > refuses a root in a document without a window, on every call, with a message that says so` (`NotAllowedError` statt der eigenen Meldung) · Commit-Message nach Review präzisiert (`globalStylesID` gelöscht, `TypeError` im Strict Mode) · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (6)
- Folgen: keine
- Schnittstellen: `display/public-api.ts` exportiert benannt: `Chronometer`, `Display`, `DisplayEventListener` (Typ), `FixedFrameLoop`, `FixedFrameLoopRenderProps`/`FixedFrameLoopTickProps` (Typen), `FrameLoop`, `isWebGLRenderer`, `isWebGPURenderer`, `Stylesheets`, `getContentAreaSize`, dazu die Typen `CreateRendererParameters`, `DisplayEventProps`, `DisplayParameters`, `DisplayRendererParameters`, `ResizeDisplayToFn` — ein neuer Export muss dort eingetragen werden · `postFixID` modulintern, `globalStylesID` gelöscht · `OnRAF`, `ISetAnimationLoop` nicht exportiert; `FrameLoop`-Konstruktor `(maxFps = 0, renderer?: {setAnimationLoop(callback: ((now: number) => unknown) | null): unknown})`; privates `#onRAF`; `RAF.attach(loop, onRAF)`/`detach(loop)` mit `Map<FrameLoop, UnsubscribeFunc>` · `Display#renderer`, `#frameLoop`, `#frameNo` sind Getter auf dem Prototyp über private `#renderer`, `#frameLoop`, `#frameNo`; `dispose()` schreibt `this.#renderer = undefined` (Zitat in `resource-lifecycle.md` §4 zieht mit) · `FixedFrameLoop#onTick`/`#onRender` sind Pfeilfelder mit Rückgabe `UnsubscribeFunc` · `Stylesheets.getSheet(root = document.head)` neu, `getGlobalSheet()` `@deprecated`-Alias; `newSheetFor()` wirft für ein Dokument ohne Fenster (`… without a window …`), bevor etwas im Cache landet

### [x] 5. Die Freigabe eines WebGPU-Renderers lässt den rAF der Seite laufen
- Findings: — (kein Audit-Finding) · Hauptbefund `packages/twopoint5d/src/display/Display.ts:1442-1461` — nach `render()` und `dispose()` zerstört die Freigabe (`renderer.dispose()` direkt nach `drainSubmittedWork()`) das Device eines WebGPU-Canvas, der im Dokument hängt; unter Firefox 155 folgt `GPUInternalError: Buffer with '' label has been destroyed`, und die Seite bekommt keinen `requestAnimationFrame` mehr (high, geschätzt) · dazu die sechs Queue-Einträge mit `→ Paket 5`
- Ziel: Nach `render()` und `dispose()` eines Displays mit `WebGPURenderer` läuft der `requestAnimationFrame` der Seite unter Firefox/WebGPU weiter — in allen drei Konstruktionspfaden (Host-Container, übergebener Canvas, übernommener Renderer) —, belegt durch Browser-Tests, die vor dem Fix rot sind; die dazugehörigen Kommentare und Testbegründungen nennen die tatsächliche Ursache, und die Test-Hygiene der Dispose- und Resize-Browser-Tests aus der Befund-Queue ist behoben.
- Weg: `#releaseRenderer()` wartet nach `drainSubmittedWork()` unter WebGPU auf zwei verschachtelt angeforderte Animation Frames des Fensters, in dem der Canvas sitzt, höchstens 2000 ms, vor der Verzweigung nach `handBack`; `dispose()`, die synchrone Canvas-Rückgabe und der WebGL-Pfad bleiben (Sonde P5 grün mit synchroner Rückgabe).
- Einordnung (Zug 0, 2026-09-24): **vorbestehend** — Basis-Sonde gegen aa6dcc4e in Firefox 155/WebGPU rot für übergebenen Canvas und übernommenen Renderer, Host grün (`paket-5.basisprobe-*.log` im Arbeitsverzeichnis); in 0.21.2 rief `dispose()` `renderer.dispose()` synchron. `Folge von: Paket 2` entfällt, keine Generation. Pre-existing high außerhalb des Audits: durch die Entscheidung vom 2026-09-24 schon im Scope, kein Halt.
- Nebenbefunde: `Display.ts:63-68`, `Display.ts:1445-1447`, `display-dispose.test.js:180-183`, `display-resize.test.js:101-111`/`:446-462` und `:114`, `display-dispose.test.js:24-30`/`:370-375`, siehe »Offene Befunde«
- Bereich: `packages/twopoint5d/src/display/Display.ts` (neu `ANIMATION_FRAMES_TIMEOUT_MS`, `waitForTwoAnimationFrames()`; `#releaseRenderer()`, Kommentare, TSDoc), `Display.spec.ts`, Browser-Tests `helpers/fixtures.js` (`whenReleased`, `whenPageAnimates` neu dort), `display-dispose.test.js`, `display-adopt-renderer.test.js`, `display-resize.test.js`, `packages/twopoint5d/src/stage/README.md`, `docs/architecture.md`, `CHANGELOG.md:255` (in place) · `docs/resource-lifecycle.md` bleibt (`dispose()` zeichengleich)
- Detail: docs/remediation/paket-5.md
- Vorgeschichte: erster Schnitt (Folge von Paket 2, `width`/`height` in die Freigabe) in Zug 2 von B durch die Ursachenprobe widerlegt, Rückfrage, Neuschnitt durch den Nutzer (siehe »Entscheidungen«, 2026-09-24); Sondentabelle und alter Verlauf in der Paketdatei. `paket-5.regressiontests.patch` prüft den verworfenen Weg und wird nicht angewendet.
- Hängt ab von: 4 (beide ändern `Display.ts`)
- Hash: d38cc402
- Ergebnis: 1 Runde (zweiter Anlauf der Runde 1 nach dem Neuschnitt; der erste Report `paket-5.impl-1.json` gehört zum verworfenen Schnitt) · Hauptbefund in allen drei Konstruktionspfaden und die sechs Queue-Einträge behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstests vor dem Fix rot — Spec 2 failed / 29 passed: `Display > release > releases a WebGPU renderer once the page has drawn two animation frames after the queue has run dry`, `… releases the renderer after a bounded wait when the page draws no frame, without a warning, and takes its frame request back`; Browser in Firefox 155 (WebGPU) mit `'no frame'` und `GPUInternalError: Buffer with '' label has been destroyed`: `the page keeps its animation frames when a display on a canvas that was handed in is disposed right after a render()` (`display-dispose.test.js`), `the page keeps its animation frames when the display is disposed right after a render() while the canvas of the renderer stays in the document` (`display-adopt-renderer.test.js`); Chromium (WebGL2) grün · klein offen: siehe Paketdatei
- Nebenbefunde: → Queue (2)
- Folgen: keine
- Schnittstellen: `Display.ts` modulintern neu `ANIMATION_FRAMES_TIMEOUT_MS = 2000` und `waitForTwoAnimationFrames(renderer)` (wartet nur mit Device und Fenster des Canvas-Dokuments, zwei verschachtelte rAF, Schranke ohne Warnung); `#releaseRenderer()` ruft sie nach `drainSubmittedWork()` vor der Verzweigung nach `handBack` · `Display.ts` ab Zeile 106 um rund 40 Zeilen verschoben (für Zug 0 von Paket 6) · `fixtures.js` exportiert neu `whenReleased(renderer)` und `whenPageAnimates()` → `'frames' | 'no frame'` · `display-dispose.test.js` hat den modulweiten Helfer `backendOf(renderer)` · `Display.spec.ts` `describe('release')` hat den Helfer `pageWithFrames()` → `{view, frame}`

### [x] 6. Offene Befunde der Display-Domäne: Chronometer, Pause-Zustand, FrameLoop-Felder, TSDoc
- Nebenbefund: alle Einträge unter »Offene Befunde« mit `→ Paket 6` — `Chronometer.ts:145-160`, `Display.ts:785` (`get pause()`), `Display.ts:218` (Lifecycle-TSDoc), `DisplayStateMachine.ts:131-162`, `Display.ts:376`/`:447` und `:557-567` (Resize-TSDoc), `FrameLoop.ts:197-200` (öffentliche Felder nur lesbar), `FrameLoop.ts:47` (`RAF`-Felder), `FrameLoop.spec.ts:371`, `CHANGELOG.md:1799`
- Ziel: Chronometer, Pause-Zustand und State-Machine verhalten sich in den Randfällen so, wie ihre TSDoc es sagt, `FrameLoop` exponiert seinen Zustand nur lesbar, und TSDoc, Spec-Kommentare und CHANGELOG der Display-Domäne stimmen mit dem Code überein.
- Bereich: `packages/twopoint5d/src/display/` (Chronometer.ts, Display.ts, DisplayStateMachine.ts, FrameLoop.ts, FixedFrameLoop.ts — eine Kommentarzeile, types.ts — TSDoc, dazu die Specs Chronometer, Display, DisplayStateMachine, FrameLoop), `packages/twopoint5d/CHANGELOG.md` (nur `[Unreleased]`) · keine Browser-Tests, keine Aufrufer der `FrameLoop`-Felder in Lookbook oder Tests (per `grep` geprüft)
- Detail: docs/remediation/paket-6.md
- Aufgenommen in Zug 0 (2026-09-24), je dieselbe Ursache wie ein Queue-Eintrag des Pakets: `Chronometer#stop(time)` ohne Klemme (`Chronometer.ts:125-131`); die Restart-Verschachtelung der State-Machine (`pause = true; pause = false` in einem `OnDisplayRestart`-Listener emittiert zwei `OnDisplayRestart`) — ein `start()` aus einem Listener von `Init` oder `Restart` tut nichts, `#transitions` entfällt, `DisplayStateMachine.spec.ts:185-230` zieht mit; »each frame« trotz `resizePollIntervalMs` in `Display.ts:420-424`, `:432`, `:455`, `:638-639`, `:658-659`, `:676` und `types.ts:63`, `:73`, `:79`; `FixedFrameLoop.ts:285` (101 Zeichen).
- Folge von: Paket 4 — nur für `FixedFrameLoop.ts:285` (in 78ee4952 entstanden, Symptom derselben Ursache wie `FrameLoop.spec.ts:371`, das Nachtragspaket für eine Zeile ist mit diesem zusammengelegt; zweite Generation). Die übrigen Teile sind vorbestehend (gegen aa6dcc4e nachgesehen).
- Restplan geprüft (Zug 0, 2026-09-24): Paket 6 ist das letzte; nichts zu verschieben. Nach seinem Commit bleiben in »Offene Befunde« die zwei Test-Hygiene-Einträge aus Paket 5 (→ Scope) und fünf Einträge → Audit für den Abschluss.
- Hängt ab von: 5 (beide ändern `Display.ts`)
- Hash: a96d0ca3
- Ergebnis: 2 Runden · alle zehn Queue-Einträge des Pakets und die in Zug 0 aufgenommenen Stellen behoben, vom Reviewer mit Fundstelle bestätigt; dazu `Display.ts:665` (`resizeToElement`) und `types.ts:16` (`ResizeDisplayToFn`), dieselbe Ursache wie Schritt 6, in Runde 1 · Regressionstests vor dem Fix rot — `Chronometer.spec.ts` 3 failed / 23 passed (`a start() with a time before the stop lets no time run backwards` u. a.), `DisplayStateMachine.spec.ts` 1/10 (`an init listener that calls start() gets no restart: init, then start`), `Display.spec.ts` 4/31 (`a display stopped before its first start answers pause = true, and false after pause = false`, `a stop() while start() waits keeps pause at true`, `a disposed display that never ran answers pause = true`, `a pause = true and a pause = false inside a restart listener restart the display once` — dieser mit `RangeError: Maximum call stack size exceeded`), `FrameLoop.spec.ts` 1/25 (`frameNo, now, deltaTime and measuredFps are accessors without a setter`); Runde 1: `lets frameNo, now, deltaTime and measuredFps read what the last frame carried`, bei vertauschten Gettern rot · Abweichung: Migration-Guide-Absatz »A disposed display refuses to be used« zur `pause`-Aussage mitgezogen · klein offen: keine
- Nebenbefunde: → Queue (3)
- Folgen: keine gemeldet · nachgetragen in Zug 0 von Paket 7 (2026-09-24): `packages/twopoint5d/src/display/Chronometer.ts:26` — der Kommentar an `#pausedAt` (»Wall-clock timestamp captured by the most recent `stop()`«) ist beim Klemmen von `stop()` (`:134`) nicht mitgezogen; Symptom, das Nachtragspaket ist mit Paket 7 zusammengelegt
- Schnittstellen: `FrameLoop#frameNo`, `#now`, `#deltaTime`, `#measuredFps` sind Getter auf dem Prototyp über private Felder (Breaking Change unter `[Unreleased]`, CHANGELOG und Migration Guide) · `RAF` (modulintern) hat nur noch private Felder und `#measureFps()` · `Display#pause` = `isPaused || pausedByUser`, `true` vor dem ersten Start nach `stop()`/`pause = true` und nach `dispose()` · `DisplayStateMachine`: `#transitions` entfernt, privates `#emittingInitOrRestart`; `start()` aus einem Listener von `Init`/`Restart` tut nichts · `Chronometer#start(time)`/`#stop(time)` klemmen gegen die zuletzt gesehene Zeit

### [x] 7. Restbefunde der Display-Domäne: Test-Hygiene display-resize, TSDoc, State-Feld
- Nebenbefund: alle Einträge unter »Offene Befunde« mit `→ Paket 7` — `display-resize.test.js:218-225` (`makeSizeRef()` nutzen), `display-resize.test.js:85/:195/:488` (`off()` in `finally`), `FixedFrameLoop.ts:82` (Ereignisname in der TSDoc), `DisplayStateMachine.ts:18` (`state` nur lesbar, modulintern), `Chronometer.ts:15` (Kommentar an `#currentTime`)
- Ziel: Die letzten Nebenbefunde der Display-Domäne sind behoben — die Resize-Browser-Tests nutzen ihre eigenen Helfer und räumen Listener deterministisch ab, `DisplayStateMachine#state` ist nur lesbar, und TSDoc und Kommentare in `FixedFrameLoop` und `Chronometer` stimmen mit dem Code.
- Bereich: `packages/twopoint5d-testing/test/display-resize.test.js`, `packages/twopoint5d/src/display/FixedFrameLoop.ts`, `DisplayStateMachine.ts`, `DisplayStateMachine.spec.ts` (Regressionstest), `Chronometer.ts` (nur Kommentare) · kein CHANGELOG-Eintrag: die State-Machine ist modulintern, der Rest TSDoc, Kommentare, Tests
- Detail: docs/remediation/paket-7.md
- Aufgenommen in Zug 0 (2026-09-24), je dieselbe Ursache wie ein Eintrag des Pakets: `display-resize.test.js:232`/`:424` (`new Display` vor dem `try`, das die Referenz in `document.body` abräumt) und `:501` (anonymer Listener ohne `off()`), beide vorbestehend; `Chronometer.ts:20`, `:23`, `:63` (`#lostTime`, `#recentlyLostTime`, TSDoc von `get lostTime()` — `lostTime` trägt auch den Überschuss aus `maxDeltaTime`), vorbestehend; `Chronometer.ts:26` (`#pausedAt`), Folge von Paket 6.
- Folge von: Paket 6 — nur für `Chronometer.ts:26` (Symptom, das Nachtragspaket für eine Kommentarzeile ist mit diesem zusammengelegt; zweite Generation). Die übrigen Teile sind vorbestehend (gegen aa6dcc4e bzw. per `git blame` nachgesehen).
- Entschieden in Zug 0 (2026-09-24): `state` wird Getter über privates `#state` wie an `FrameLoop` und `Display`, mit Regressionstest vor dem Umbau rot; die Statics der State-Machine bleiben schreibbar (Konstanten, kein Instanzzustand, `readonly` wirkte nur im Typ); `off()` wandert in `finally`, statt gestrichen zu werden, wie Paket 5 es in derselben Datei gehalten hat.
- Restplan geprüft (Zug 0, 2026-09-24): Paket 7 ist das letzte; nichts zu verschieben. Nach seinem Commit bleiben in »Offene Befunde« nur die fünf Einträge → Audit für den Abschluss.
- Hängt ab von: —
- Hash: e1b9f2ff
- Ergebnis: 1 Runde · alle fünf Queue-Einträge des Pakets und die in Zug 0 aufgenommenen Stellen behoben, vom Reviewer mit Fundstelle bestätigt · Regressionstest `state is an accessor without a setter` (`DisplayStateMachine.spec.ts`) vor dem Umbau rot, 1 failed / 11 passed · Abweichung: der Test schreibt `['state']` statt `.state` (TS4111 unter `noPropertyAccessFromIndexSignature`) · klein offen: keine
- Nebenbefunde: keine
- Folgen: keine
- Schnittstellen: `DisplayStateMachine#state` ist ein Getter über privates `#state` (modulintern, kein CHANGELOG)

### [x] 8. Letzte Reviewer-Befunde der Display-Domäne: Init-Listener, der wirft, Testtitel, CHANGELOG, Kommentare
- Nebenbefund: alle Einträge unter »Offene Befunde« mit `→ Paket 8` — Init-Handler in `Display.ts` (werfender `OnDisplayInit`-Listener), `display-dispose.test.js:257-269`, `CHANGELOG.md:12/:140/:244/:896-897`, TSDoc von `start()` und Lifecycle-Punkt 3 in `Display.ts`, `display-adopt-renderer.test.js:73-74`, `Display.ts:110-119`
- Ziel: Ein `OnDisplayInit`-Listener, der wirft, lässt `start()` mit seinem Fehler rejecten und das Display ungestartet, und das nächste `start()` emittiert wieder `OnDisplayInit` an alle Listener (Test zuerst); Testtitel, CHANGELOG-Einträge, Kommentare, TSDoc-Umbruch und Interpunktion der Display-Domäne sagen genau, was der Code tut.
- Detail: docs/remediation/paket-8.md
- Bereich: `packages/twopoint5d/src/display/DisplayStateMachine.ts` (`#initOrRestart`), `DisplayStateMachine.spec.ts`, `Display.ts` (Kommentar, Klassen-TSDoc, TSDoc von `start()`, Interpunktion), `Display.spec.ts`, `Stylesheets.ts` (ein Komma), `packages/twopoint5d/docs/architecture.md` (ein Komma), `packages/twopoint5d/CHANGELOG.md` (nur `[Unreleased]`), `packages/twopoint5d-testing/test/display-dispose.test.js`, `display-adopt-renderer.test.js`
- Ziel präzisiert und Weg entschieden in Zug 0 (2026-09-24): `DisplayStateMachine#initOrRestart` löscht `#initMustBeCalled` erst, nachdem jeder Init-Listener durch ist; ein Wurf lässt das Display in `NEW`, das nächste `start()` emittiert `Init` erneut, auch an die Listener vor dem Wurf (TSDoc und CHANGELOG sagen das). Kein `emitStrict()`: die Bibliothek dispatcht überall mit `emit()`, und eine geschützte Variante verteuert jedes `emit()` im Prozess der Konsumenten. Der Dispose-Test ruft `start()` vor dem `dispose()` und prüft so den Wächter nach `await #waitForRenderer`.
- Aufgenommen in Zug 0 (2026-09-24), je dieselbe Ursache wie ein Eintrag des Pakets: die Strict-Mode-Wendung auch in `CHANGELOG.md:141`/`:921-922`; `—,` an neun weiteren Stellen (`Display.ts:451`, `:590`, `:607`, `:618`, `:1049`, `:1056`, `Stylesheets.ts:105`, `docs/architecture.md:26`, `CHANGELOG.md:325`); der Umbruch der Klassen-TSDoc in Punkt 2, Punkt 4 und »Resize model« (jede Zeile höchstens 80 Zeichen). Lifecycle Punkt 3 selbst ist an der Fundstelle gegenstandslos.
- Folge von: Pakete 1, 2, 4, 5 und 6 — je nur für die Stellen, die deren Diff geschrieben oder stehen gelassen hat (zweite Generation). Die Kommastelle `Stylesheets.ts:105` stammt aus dem Teil von Paket 4, der Folge von Paket 3 war; gezählt wird nach der Ursache (eine Interpunktionsgewohnheit, erstmals in Paket 1), nicht nach der Zeile — keine dritte Generation. Der Init-Befund ist im Ursprung vorbestehend.
- Restplan geprüft (Zug 0, 2026-09-24): Paket 8 ist das letzte; nichts zu verschieben. Nach seinem Commit bleiben in »Offene Befunde« der neue Eintrag zum werfenden `OnDisplayStart`-Listener (→ Scope, braucht eine weitere Drain-Runde) und acht Einträge → Audit für den Abschluss.
- Hängt ab von: —
- Hash: bd80d232
- Ergebnis: 1 Runde · alle neun Queue-Einträge des Pakets und die in Zug 0 aufgenommenen Stellen behoben, vom Reviewer mit Fundstelle bestätigt (Lifecycle Punkt 3 an der Fundstelle gegenstandslos, Umbruch in Punkt 2, Punkt 4 und »Resize model« erledigt) · Regressionstests vor dem Fix rot, 2 failed / 48 passed — `an init listener that throws leaves the state machine new, and the next start() emits init again` (`DisplayStateMachine.spec.ts`) und `an init listener that throws rejects start(), and the next start() emits init to every listener` (`Display.spec.ts`); `a restart listener that throws rejects start(), and the next start() emits restart again` sichert die TSDoc-Aussage und war vorher grün · Dispose-Browser-Test `a start() that dispose() lands in while it waits for the renderer rejects and leaves the frame loop empty` mit auskommentiertem Wächter nach `await #waitForRenderer` rot · Abweichung: Kommentar `display-dispose.test.js:59-72` neu umbrochen · klein offen: »fires `OnDisplayInit` (once)« in Lifecycle Punkt 2 (`Display.ts:382`) steht neben dem neuen Satz zum werfenden Listener; CHANGELOG-Fixed-Eintrag nennt nur `OnDisplayInit`, nicht `OnDisplayRestart`; Testtitel mit 112 Zeichen (Plan-Vorgabe)
- Nebenbefunde: keine
- Folgen: keine
- Schnittstellen: `DisplayStateMachine#initOrRestart` löscht `#initMustBeCalled` erst nach dem `emit(Init)`: ein werfender Init-Listener lässt den Zustand `NEW`, das nächste `start()` emittiert `Init` erneut an alle Listener
