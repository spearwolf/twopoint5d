# Remediation-Plan — @spearwolf/twopoint5d

Quelle: ./audit.html vom 2026-09-19 · Branch: main · erstellt: 2026-09-19
Baseline: `pnpm run ci` ✓ (clean, lint, build, typecheck, checkPkgTypes, checkNameableTypes, lintPkg, test:ci, test:browser)
Arbeitsverzeichnis: /tmp/claude-1000/-home-spw-spaceland-twopoint5d/e44e579b-d9de-4c01-acae-af1edfbe58ff/scratchpad (Diffs und Verify-Logs, außerhalb der Versionierung)
Paketdetails: docs/remediation/paket-<N>.md — je Paket eine Datei, angelegt von dessen Zug 0
Scope: 33 von 130 Findings (4 high, 13 medium, 16 low) — alle Findings mit Präfix BUG · ausgenommen: alle übrigen Kategorien, acknowledged
Scope-Regel: alle Bugs und Korrektheitsdefekte (BUG-Serie), jede Severity — gilt auch für Befunde, die erst im Lauf auffallen; Nicht-Bugs (API, DOC, PERF, TEST, CONS …) gehen ins Audit
Stand (2026-09-19): Lauf abgeschlossen — 19 Pakete committet (f1c0e58..160b26ad), keines blockiert, Befund-Queue leer, `pnpm run ci` grün ohne Nx-Cache · Report: docs/remediation/20260919-remediation-report.md

Diese Datei führt einen Lauf des Skills `js-ts-audit-remediation` und hält
seinen Stand. Wer hier weiterarbeitet: diesen Skill laden, die eingetragenen
Hashes gegen `git log --oneline` halten, beim obersten Paket ohne `[x]`
einsteigen. Der Lauf ist erst fertig, wenn auch »Offene Befunde« leer ist.
Statusmarken: `[ ]` offen · `[~]` Detailplan steht, Umsetzung läuft · `[x]`
erledigt · `[r]` committet, Review wird nachgezogen · `[!]` blockiert.

## Entscheidungen
- BUG-003: PanControl2D liest `event.code`; neue Option `keys: [string, string, string, string]` (Default `KeyW`/`KeyS`/`KeyA`/`KeyD`). `keyCodes` bleibt als deprecated Fallback erhalten und wird auf `event.keyCode` weiter ausgewertet, wenn der Aufrufer es explizit setzt — kein Breaking Change (2026-09-19)
- BUG-031: Projektionen bekommen im Konstruktor einen brauchbaren Default-Spec; die Signatur bleibt optional (2026-09-19)
- BUG-092: `unpick()` wird repariert (Reflect.ownKeys), bleibt öffentlich (2026-09-19)
- BUG-080: Code folgt Doku — solange der Container 0×0 ist, entsteht keine Kamera; der erste `resize()` baut sie (2026-09-19)
- BUG-089: `time` aus den Konstruktoroptionen lesen (nicht aus dem Interface streichen) — Empfehlung ohne Breaking Change (2026-09-19)
- Übrige Findings folgen ihrer Empfehlung im Audit; wo die Empfehlung zwei Wege nennt, gilt der erste, sofern er keinen Breaking Change bedeutet (2026-09-19)
- `scripts/publishNpmPkg.mjs` wird geändert, aber nie ausgeführt; Verifikation dort über Unit-Test/Trockenlauf der geänderten Funktion ohne Publish (2026-09-19)
- Paket 9: blockiertes Gate war Umgebung (GPU-Treiber); nach Wiederherstellung Stash anwenden, `pnpm run ci` frisch fahren und ohne neuen Runner committen (2026-09-19)
- Arbeitsverzeichnis nach Neustart der Maschine neu gesetzt; das alte unter /tmp ist weg. Die Token-Tabelle bis einschließlich Paket 9 ist gesichert in `/tmp/claude-1000/-home-spw-spaceland-twopoint5d/e44e579b-d9de-4c01-acae-af1edfbe58ff/scratchpad/tokenverbrauch-bis-paket-9.md` und wird im Abschluss mit der neuen Tabelle zusammengeführt (2026-09-19)

## Konventionen
Gelten für jede Zeile, die in diesem Lauf entsteht — Code, Kommentare,
Dokumentation, CHANGELOG, Migrations-Hinweise, Commit-Messages:
- Inline-Kommentare sind erwünscht, wo sie erklären, *warum* etwas so ist.
- Keine Finding-IDs, auch nicht in der Commit-Message. Sie gehören diesem einen
  Audit, sind danach tot, und die Commit-Message überdauert den Lauf. Sie leben
  in diesem Plan und sonst nirgends; die Verbindung zwischen Finding und Commit
  trägt das Feld `Hash:` unter dem Paket — in genau der Richtung, in der jemand
  sie später sucht. Eine Commit-Message sagt in eigenen Worten, was sie ändert.
- Kein Rückblick auf den Vorzustand: kein »früher«, kein »statt bisher«, kein
  »im Zuge des Audits umgestellt«. Der Test: Ergibt der Satz für jemanden Sinn,
  der den Vorzustand nie gesehen hat? Dann bleibt er. Braucht er ihn, gehört er
  in die Commit-Message — die Historie ist bereits konserviert.
- Projektspezifisch: AGENTS.md gilt (Code, Kommentare, Doku auf Englisch; Conventional Commits; `.js`-Suffix in relativen Imports; `import type`; Ownership nach `packages/twopoint5d/docs/resource-lifecycle.md`). Änderungen an Rendering- oder GPU-Buffer-Code brauchen Vitest-Spec und Browsertest. Neue öffentliche Symbole in das `public-api.ts` ihres Moduls. Nutzerrelevante Änderungen bekommen einen Eintrag unter `Unreleased` im CHANGELOG der Bibliothek (Skill `updating-changelog`).
- Verify-Gate je Paket: `pnpm run ci`.
- Niemals `pnpm publishNpmPkg` oder `scripts/publishNpmPkg.mjs` ausführen.

## Vorbestehende Fehler
- keine — `pnpm run ci` war zu Lauf-Beginn grün

## Offene Befunde
Nebenbefunde aus den Paketen: was auch ohne diesen Lauf falsch war. Jeder
Eintrag wird beschlossen, bevor der Lauf endet — Paket oder Rückgabe ins Audit.
Ein leerer Abschnitt ist Abschlussbedingung, kein Zufall. Das Urteil am Ende
der Zeile misst den Eintrag an der Scope-Regel oben: `→ Scope`, `→ Audit`,
`→ Rückfrage`.
- [x] `packages/twopoint5d/src/stage/OrthographicProjection.ts:48`, `ParallaxProjection.ts:49` — ein direkter `updateViewRect(0, h)` ohne `Stage2D` liefert `NaN`/`Infinity` in Pixel-Ratio und (Parallax) Aspect; `Stage2D` fängt 0×0 ab, die Projektion selbst nicht · aus Paket 1 · low · → Scope → Paket 12
- [x] `packages/twopoint5d/src/stage/fitIntoRectangle.ts:196` — `{pixelZoom: 0}` teilt durch 0 und ergibt eine unendlich große View · aus Paket 1 · low · → Scope → Paket 12
- [x] `packages/twopoint5d/src/stage/Stage2D.ts:179` (Stand `69e27081`) — `#updateProjection` ruft `projection.updateCamera(this.camera)` auch für eine vom Nutzer gesetzte Kamera und überschreibt deren `fov`/`aspect` bzw. Frustum (bei Ortho-Projektion + `PerspectiveCamera` Felder, die sie nicht nutzt) · aus Paket 1 · → Rückfrage: ob das gewollt ist (Projektion führt jede Kamera nach) oder ein Defekt, ist offen, und es überschneidet das Audit-Finding zu `updateCamera()` vs. `createCamera()` außerhalb der BUG-Serie → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/stage/StageRenderer.ts:141` — TSDoc von `stages` verweist auf `getOrderedStages()`, gemeint ist der Getter `orderedStages` · aus Paket 1 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/stage/StageRenderer.ts:172` — Hook `onRenderOrderChanged()` trägt nur `// ntdh`, undokumentiert · aus Paket 1 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/stage/ParallaxProjection.ts:102` (Stand `69e27081`) — `getZoom()` trägt `// TODO add jsdoc` · aus Paket 1 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/Map2DTileCoordsUtil.ts:45-53` — TSDoc von `TilesWithinCoords` vertauscht die Bedeutungen: `rows` heißt »the number of tiles in a row«, `columns` »the number of tiles in a column«, gemeint ist jeweils die Zahl der Zeilen bzw. Spalten · aus Paket 2 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/CameraBasedVisibilityHelpers.ts:118-123` — `update()` schreibt über `document.querySelector('.map2dCoords')` ins globale DOM (nur `// TODO remove this!`); ohne `document` (Worker, Node) wirft es einen `ReferenceError` · aus Paket 2 · low · → Scope → Paket 13
- [x] `packages/twopoint5d/src/map2d/CameraBasedVisibility.ts:115` — TSDoc nennt für `lookAtCenter` den Default `true`, das Feld (Zeile 119) steht auf `false` · aus Paket 2 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/CameraBasedVisibility.ts:314-315` — `computeVisibleTiles()` ruft `updateMatrixWorld()` und `updateProjectionMatrix()` auf der Kamera des Aufrufers und überschreibt damit eine von Hand gesetzte `projectionMatrix` · aus Paket 2 · nicht nachgestellt · → Rückfrage: ob der Visibilitor die Kamera nachführen darf (Bequemlichkeit) oder nur lesen soll, ist nirgends festgelegt → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/RepeatingTilesProvider.ts:131` — der `switch` in `getTileIdsWithin()` hat keinen `default`; ein ungültiger `limitToAxis` aus JavaScript lässt den Puffer ungefüllt, während `getTileIdAt()` ihn als `'none'` behandelt (Zeile 84) · aus Paket 2 · low · → Scope → Paket 13
- [x] `packages/twopoint5d/src/stage/StageRenderer.ts:690` — in `orderedStages` ist die Bedingung `renderOrder[0] === ''` toter Code, `renderOrderArray` filtert leere Einträge schon heraus · aus Paket 8 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/texture/TileSet.ts:147-148` — `padding << 1` schneidet einen gebrochenen `padding` ab (`1.5 << 1` ist 2, nicht 3): die Schrittweite von Kachel zu Kachel ist dann 1 px zu klein, jede weitere Spalte und Zeile verrutscht um 1 px gegen das Bild · aus Paket 3 (Zug 0, vorbestehend seit e352b56) · low · → Scope → Paket 14
- [x] `packages/twopoint5d/src/texture/TileSet.ts:67` — `readonly options: TileSetOptions` ist bei `new TileSet(coords)` ohne Optionen `undefined`; der Typ verspricht ein Objekt, die Getter lesen deshalb `this.options?.` · aus Paket 3 · → Audit (TYPE) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/texture/TileSetLoader.ts:33` — `load()` nimmt `textureClasses` als `Array | null | undefined`, `TextureImageLoader.load()` nur als `Array`; uneinheitliche Signaturen der Geschwister-Loader · aus Paket 3 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/vertex-objects/VertexObjectBuffer.ts:85` — der Konstruktor prüft eine numerische `capacity` nicht; `new VertexObjectBuffer(descriptor, 1.5)` oder `NaN` endet in `RangeError: Invalid array length` aus `createTypedArray` (Zeile 187) statt in einer benannten Meldung; der Pool prüft vorher, der öffentliche Buffer-Konstruktor nicht · aus Paket 4 · low · → Scope → Paket 15
- [x] `packages/twopoint5d/src/vertex-objects/VertexObjectDescriptor.ts:55` — der Descriptor hält `description` per Referenz, `vertexCount`, `meshCount` und `indices` lesen sie live (ab Zeile 113); wer die Description nach dem Bau ändert, umgeht die Prüfungen des Konstruktors · aus Paket 4 · low · → Rückfrage: ob eine Description nach dem Bau als eingefroren gilt (kopieren/einfrieren) oder das Ändern ein erlaubter Weg ist, ist nirgends festgelegt → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/vertex-objects/VertexObjectDescriptor.ts:125` — TSDoc von `getInstanceCount()` trägt `TODO remove?!` · aus Paket 4 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/vertex-objects/createVertexObjectPrototype.ts:157` — generierte Accessoren überschatten gleichnamige Properties des `basePrototype` stumm; die Eindeutigkeitsprüfung des Descriptors deckt `basePrototype` nicht ab · aus Paket 4 · → Rückfrage: ob das Überschatten ein Defekt ist (wie eine Kollision unter `methods`) oder ein gewollter Override-Weg über die Prototypkette, ist nicht festgelegt → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/texture/TextureResource.ts:599`, `:752` (Stand `4e45acc`) — die Animations-Effekte schreiben nur bei `tileSet && this.frameBasedAnimationsData` bzw. `atlas && …`; `frameBasedAnimationsData = undefined` (etwa `TextureStore#parse()` mit einem Item ohne Animationsdaten, `TextureStore.ts:471`) lässt die alten `frameBasedAnimations` stehen · aus Paket 9 · low · → Scope → Paket 14
- [x] `packages/twopoint5d/src/texture/TextureResource.ts:578`, `:631` (Stand `4e45acc`) — ob `load()` den TileSet- bzw. Atlas-Effekt registriert, hängt am Wert bei `load()` (`if (this.tileSetOptions)`, `if (this.atlasUrl)`); wird der Wert vor `load()` geleert und später wieder gesetzt, baut die Resource nie ein TileSet bzw. einen Atlas · aus Paket 9 · low · → Scope → Paket 14
- [x] `packages/twopoint5d/src/texture/TextureResource.ts:538-547` — die Übergabe von `#ownTexture` steht hinter dem `batch()` des Bild-Effekts; wirft darin ein abgeleiteter Effekt — der Atlas-Effekt auf einem von außen gesetzten `atlasJson`, dessen Frames `TexturePackerJson.parse()` nicht lesen kann (`{frames: {a: {}}}`) —, re-raised `batch()` und die Übergabe fällt aus: die neue Textur steht auf dem Signal, gehört niemandem und wird nie freigegeben, der Vorgänger lebt bis `dispose()` · aus Paket 9 (Zug 0, vorbestehend seit e352b56; den TileSet-Weg schließt Paket 9) · low · → Scope → Paket 14
- [x] `packages/twopoint5d/src/display/types.ts:57` — TSDoc von `resizeToAttributeEl` sagt, `"self"` messe den Canvas; `Display.resize()` (`Display.ts:677`) nimmt `this.resizeToElement ?? canvasElement`, und die Klassen-TSDoc (`Display.ts:111`) sagt das auch · aus Paket 5 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/controls/PanControl2D.ts:286` — der Setter `panView` setzt `#isFirstPanViewUpdate = prevPanView !== this.#panView`; wer vor dem ersten `update()` dasselbe State-Objekt noch einmal zuweist, löscht das Flag, und das garantierte erste `update`-Event fällt aus, solange sich nichts bewegt · aus Paket 5 · low · → Scope → Paket 16
- [x] `packages/twopoint5d/src/display/FixedFrameLoop.ts:84-85` — `DefaultFps` und `DefaultMaxStepsPerFrame` sind beschreibbare Statics, die der Konstruktor ungeprüft als Ausgangswert übernimmt; `FixedFrameLoop.DefaultFps = 0` ergibt weiter `fixedDelta = Infinity` · aus Paket 5 · low · → Scope → Paket 16
- [x] `packages/twopoint5d/src/display/Stylesheets.ts` (TSDoc von `addRule()`) — `@param element` fehlt, »uniq-number« statt »unique number« · aus Paket 10 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/controls/PanControl2D.ts` (TSDoc von `dispose()`) — die Aufzählung `keyboardDisabled`, `pointerDisabled`, `panView` bricht mitten im Satz um · aus Paket 10 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/controls/PanControl2D.ts:604` (Stand `ed1b11e4`) — TSDoc von `dispose()`: der Satz »by a key that was still held down when `dispose()` ran gives its field back« hat kein Verb im Hauptsatz · aus Paket 16 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/display/FixedFrameLoop.ts:253-254` (Stand `ed1b11e4`) — TSDoc von `dispose()` bricht mitten im Satz um, mit überlanger Zeile nach »writes them: a write the setter accepts lands, …« · aus Paket 16 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/docs/resource-lifecycle.md:5-6`, `:55` — verweist auf »section 7 the checklist, section 8 the tests«; das Dokument hat nur §1–§7, Checkliste ist §6, Tests §7 · aus Paket 10 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `scripts/makePackageJson.mjs:84-86` (Stand `0e7db162`) — `removePathPrefixAt` entfernt mit `String#replace('dist/', '')` das erste `dist/` irgendwo im Pfad, nicht nur ein führendes: `./lib/dist/x.js` würde zu `./lib/x.js`; heute beginnen alle Pfade in `main`, `module`, `types` und `exports` mit `dist/` bzw. `./dist/` · aus Paket 7 (Zug 0, vorbestehend seit e352b56) · low · → Scope → Paket 17
- [x] `scripts/makePackageJson.mjs:65-82` (Stand `0e7db162`) — `removeDistPathPrefix` wirft `TypeError`, wenn dem Manifest `exports` fehlt (`Object.keys(undefined)`, Zeile 72), und hält `null` für ein Objekt (`typeof null === 'object'`, Zeile 75); heute trägt die Bibliothek `exports`, folgenlos · aus Paket 7 (vorbestehend seit e352b56, dort `:101`) · low · → Scope → Paket 17
- [x] `scripts/publishNpmPkg.mjs:11` — ohne Argument ist `process.argv[2]` `undefined`, `path.resolve` wirft einen unklaren `TypeError` statt einer Usage-Meldung · aus Paket 7 · → Audit (DX) → in ./audit.html übernommen (2026-09-19)
- [x] `scripts/publishNpmPkg.mjs:29` — `pkgJson.name` geht unmaskiert in den Shell-String von `exec()`; der Name stammt aus dem eigenen Manifest · aus Paket 7 · → Audit (SEC) → in ./audit.html übernommen (2026-09-19)
- [x] `apps/lookbook/src/pages/demos/crosses.astro:12`, `:16` — zwei getrennte `body`-Regeln im selben `<style>`-Block · aus Paket 7 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `apps/lookbook/src/pages/demos/crosses.astro:73` — `console.dir(demo)` bleibt als Debug-Ausgabe stehen; `no-console` erfasst `.astro` nicht · aus Paket 7 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/stage/ParallaxProjection.ts:72`, `:76` (Stand `69e27081`) — `distanceToProjectionPlane` aus den Specs geht ungeprüft in `#fovy = 2 · atan(#halfHeight / distance)`: `0` ergibt einen `fov` von 180°, ein negativer Wert einen negativen `fov`; `getZoom()` rechnet mit demselben Wert · aus Paket 12 (Zug 0, vorbestehend seit e352b56) · low · → Scope → Paket 19

- [x] `packages/twopoint5d/src/map2d/RepeatingTilesProvider.ts:189` (Stand `fdba2c34`) — `getTileIdsWithin()` liest `this.tileIds[0]![0]!` über den Getter, sonst überall `#tileIds` · aus Paket 13 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/CameraBasedVisibilityHelpers.ts:256` — `makePointOnPlane(point?: Vector2)` bekommt bei allen Aufrufen einen Vector2; optionaler Parameter und `point?.x ?? 0` sind toter Zweig · aus Paket 13 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/RepeatingTilesProvider.spec.ts:378-385` — Block im Test `4x1 pattern in-outside` (`vertical`) falsch eingerückt, `prettier-ignore` verdeckt es · aus Paket 13 · → Audit (CONS) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/map2d/RepeatingTilesProvider.ts:120` — TSDoc von `getTileIdsWithin()` nennt weder Rückgabewert noch `target` (jede Zelle wird überschrieben, Länge wird nicht geprüft) · aus Paket 13 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/stage/Stage2D.ts:257` (Stand `121a8fa9`) — Fehlermeldung von `asPassNode()` »no scene or camera yet — call resize() first« ist ungenau: ein `resize()` reicht nicht, wenn die Specs keine View ergeben oder die Projektion fehlt; `updateFrame()` (`:214`) nennt die Bedingung vollständig · aus Paket 18 · → Audit (DOC) → in ./audit.html übernommen (2026-09-19)
- [x] `packages/twopoint5d/src/texture/TextureResource.ts:525` (Stand `121a8fa9`) — `imageUrl = undefined` lässt Textur, `imageCoords` und alles daraus Gebaute (TileSet, Atlas, Animationen) stehen, der Bild-Effekt kehrt nur zurück; erreichbar über `TextureStore#parse()` mit einem `tileSet`-Item ohne `imageUrl` (`TextureStore.ts:468`) · aus Paket 14 (Zug 0, vorbestehend seit e352b56, dort `:507`) · low · → Rückfrage: ob ein geleertes Bild die Textur zurücknimmt und freigibt (ein Aufrufer kann sie noch halten) oder die letzte Textur stehen bleibt, ist nirgends festgelegt → in ./audit.html übernommen (2026-09-19)

## Pakete

### [x] 1. stage: Output- und Pass-Nodes invalidieren, Render-Reihenfolge und Projektionen robust machen
- Findings: BUG-078 (high), BUG-079 (high), BUG-080 (medium), BUG-031 (medium), BUG-081 (low), BUG-082 (low)
- Ziel: Wechsel von Pipeline, Output-Builder und Stage-Kamera bauen die Nodes neu, und Projektionen bzw. Stage2D entstehen nie in einem Zustand, der nichts rendert.
- Bereich: `packages/twopoint5d/src/stage/`
- Hängt ab von: —
- Detail: `docs/remediation/paket-1.md`
- Mitgenommen: zwei vorbestehende Nebenbefunde gleicher Ursache — `Stage2D` bleibt ohne Kamera, wenn eine vor dem ersten `resize()` gesetzte Nutzerkamera bei gleicher Containergröße zurückgesetzt wird (`Stage2D.ts:97-106, 131-140`); ein in `renderOrder` doppelt genannter Name bzw. `'*'` rendert seine Stages zweimal pro Frame (`StageRenderer.ts:644-662`)
- Hash: f1c0e58
- Ergebnis: 1 Runde · BUG-078, BUG-079, BUG-080, BUG-031, BUG-081, BUG-082 sowie beide Mitgenommenen behoben · Regressionstests (alle vor dem Fix rot) u. a. `replacing the pipeline rebuilds the output node`, `assigning buildOutputNode after the first frame switches mode`, `swapping the projection of a Stage2D after the first render rebuilds with a new pass node`, `creates no camera before the first resize() with an area`, `never emits OnStageResize with NaN`, `fills the container when built without specs`, `restores autoClear when a stage throws`, `places a name or wildcard listed twice once`, `hands back to the projection camera when the assigned one is cleared` · Browsertests in `stage-pipeline.test.js` nicht rot gesehen (laufen gegen `dist`) · 3 kleine Reviewer-Befunde in der Paketdatei
- Nebenbefunde: → Queue (6)
- Folgen: `packages/twopoint5d/src/stage/StageRenderer.ts:738` — `#warnAboutSharedNames` prüft den Rohstring gegen `'*'`; ein gleichwertiges `renderOrder` wie `'*,*'` oder `' * '` warnt fälschlich über geteilte Namen · `packages/twopoint5d/src/stage/IStage.ts:9-10` — TSDoc zu `name` (»Should be unique …«) verschweigt, dass geteilte Namen jetzt gemeinsam in Einfügereihenfolge rendern und eine Umbenennung ab dem nächsten Frame greift · → beide in Paket 8 (Nachtrag, verteilt in Zug 0 von Paket 2)
- Schnittstellen: `StageRenderer#buildOutputNode` ist ein Accessor-Paar auf dem Prototyp (Unterklassen dürfen es nicht als Feld deklarieren) · `Stage2D#camera` ist `undefined` bis zum ersten `resize()` mit Breite und Höhe > 0; `asPassNode()` wirft davor · komponierter `StageRenderer` (mit `buildOutputNode`/`RootRenderPipeline`) zeichnet bei 0×0 nichts · `StageRenderer.add()` abonniert `OnStageAfterCameraChanged` auf eventisierten Stages, `remove()` meldet ab · `Stage2D` sendet `OnStageAfterCameraChanged` bei jedem Kamerawechsel, auch beim Projektionswechsel (zwei Events: alt → `undefined`, `undefined` → neu) · `OrthographicProjection`/`ParallaxProjection` ohne Specs: `viewSpecs` = `{fit: 'fill'}` · `fitIntoRectangle` liefert für `contain`/`cover` bei Rechteck ohne Fläche `[0, 0]`

### [x] 2. map2d: Tile-Ermittlung, Bezugssystem und Visibilitor-Wechsel korrigieren
- Findings: BUG-070 (high), BUG-071 (medium), BUG-072 (medium)
- Ziel: Map2D liefert für jede Kamera- und Visibilitor-Lage die richtigen Tiles an der richtigen Weltposition.
- Bereich: `packages/twopoint5d/src/map2d/`, dazu ein neuer Browsertest `packages/twopoint5d-testing/test/map2d-placement.test.js`
- Hängt ab von: —
- Detail: `docs/remediation/paket-2.md`
- Hash: f499ca2
- Ergebnis: 1 Runde · BUG-070, BUG-071, BUG-072 behoben · Regressionstests (alle vor dem Fix rot) u. a. `starts at the pattern column of a left edge inside the pattern`, `agrees with getTileIdAt()` (vertical: 1620 Throws), `a visibilitor that takes over from another one gets the tiles built again`, `a visibilitor switched away and back leaves only its own tiles in the renderers`, `places the renderer node in the local space of a moved map`, `finds the same tiles for a map and a camera moved together` (15 statt 437), Browsertest `a moved map draws as many tiles as the same map at the origin` (rot gesehen gegen `dist`) · CHANGELOG mit `### Fixed`, `### Changed` und Migration-Guide-Abschnitt · 2 kleine Reviewer-Befunde in der Paketdatei
- Nebenbefunde: → Queue (4)
- Folgen: keine
- Schnittstellen: `Map2DTileStreamer#visibilitor` ist ein Accessor-Paar auf dem Prototyp (Unterklassen dürfen es nicht als Feld deklarieren); ein Visibilitor, der einen anderen oder `undefined` ersetzt, lässt beim nächsten `update()` alle Tiles neu bauen (`clearTiles`), die erste Zuweisung und dieselbe Instanz nicht · `Map2D#visibilitor` liest und schreibt den Visibilitor seines Streamers (keine eigene Kopie); `Map2D#tileStreamer =` reicht den Visibilitor des alten Streamers weiter, hatte der keinen, behält der neue seinen · `Map2DTileStreamer#update(node)` setzt die Renderer-Knoten auf `(offset.x, 0, offset.y)` im lokalen Raum von `node` (Renderer-Knoten müssen Kinder von `node` sein) · `IMap2DVisibleTiles#translate` wird weiter gefüllt, ist aber nur informativ · `TileBox#box` liegt im lokalen Raum des Map-Knotens, `frustumBox` und `centerWorld` im Weltraum; Tile-Box-Helper gehören per `helpers.add(map2d)` in den Map-Knoten

### [x] 8. stage: Nachtrag — Warnung und TSDoc zu geteilten Stage-Namen an die Render-Reihenfolge angleichen
- Folge von: Paket 1
- Findings: — (Folgen aus Paket 1, kein Audit-Finding)
- Fundstellen: `packages/twopoint5d/src/stage/StageRenderer.ts:737-751` — `#warnAboutSharedNames` prüft den Rohstring `#renderOrder` gegen `'*'`, während die Sortierung mit `renderOrderArray` (split, trim) arbeitet; ein gleichwertiges `renderOrder` wie `'*,*'` oder `' * '` warnt deshalb über geteilte Namen, die gar nicht unterschieden werden müssen · `packages/twopoint5d/src/stage/IStage.ts:8-11` — TSDoc zu `name` (»Should be unique within a single `StageRenderer`, otherwise `renderOrder` cannot disambiguate«) verschweigt, dass Stages mit geteiltem Namen gemeinsam an dessen Position in Einfügereihenfolge rendern und eine Umbenennung ab dem nächsten Frame greift (so steht es in der TSDoc des `renderOrder`-Setters, `StageRenderer.ts:148-157`)
- Ziel: Die Warnung zu geteilten Namen und die TSDoc von `IStage#name` beschreiben genau das Verhalten, das `StageRenderer#renderOrder` nach Paket 1 hat.
- Bereich: `packages/twopoint5d/src/stage/`
- Hängt ab von: Paket 1 (committet)
- Einsortiert: direkt hinter Paket 2 — die Ursache aus Paket 1 wird geschlossen, bevor Paket 5 `StageRenderer.ts` wieder anfasst
- Detail: `docs/remediation/paket-8.md`
- Hash: 7130201
- Ergebnis: 1 Runde · beide Folgen aus Paket 1 behoben (Warnung nur für von `renderOrder` gelistete Namen; TSDoc `IStage#name`, Setter- und `add()`-TSDoc, `stage/README.md`, CHANGELOG angeglichen) · Regressionstests `does not warn about a shared name while renderOrder lists no name` und `does not warn about a shared name that renderOrder does not list` (vor dem Fix rot), `warns about a shared name that renderOrder lists between blanks` (Wächter, grün) · Reviewer ohne Befunde
- Nebenbefunde: → Queue (1)
- Folgen: keine

### [x] 3. texture: Katalog- und Loader-Eingaben absichern
- Findings: BUG-067 (high), BUG-068 (medium), BUG-069 (low), BUG-066 (low)
- Ziel: Fehlerhafte Katalogdaten und Throws in Loader-Callbacks enden als benannter Fehler bzw. Rejection statt als Endlosschleife, Absturz oder verlorenem Zustand.
- Bereich: `packages/twopoint5d/src/texture/`
- Hängt ab von: —
- Mitgenommen: zwei vorbestehende Nebenbefunde gleicher Ursache wie BUG-067 (ungeprüfte Werte vor der Layout-Schleife von `TileSet`) — ein `tileCount` von 0, negativ, `NaN` oder gebrochen ergibt still ein leeres bzw. schiefes TileSet; nicht endliche `baseCoords`-Maße lassen die Schleife nie enden (`TileSet.ts:143`, `154-180`)
- Detail: `docs/remediation/paket-3.md`
- Hash: d90a308
- Ergebnis: 2 Runden · BUG-067, BUG-068, BUG-069, BUG-066 sowie beide Mitgenommenen (`tileCount`, `baseCoords`) behoben · Regressionstests (vor dem Fix rot) u. a. `TileSet` › `degenerate options` (11 Fälle; drei Hänger-Fälle erst nach dem Fix), `a tile set that is refused is reported as a texture failure`, `a tileset resource skips an animation entry that is no object`, `an atlas resource skips an animation entry that is no object`, `clearing the overrideImageUrl gives the image back to the one the json names`, `a json without an image reports an override that is cleared again`, `a parse without the overrideImageUrl gives the atlas its json image back`, Loader-Specs in `PowerOf2ImageLoader.spec.ts`, `TextureImageLoader.spec.ts`, `TileSetLoader.spec.ts` · Runde 2: CHANGELOG-Rückblick umformuliert · kleine Reviewer-Befunde in der Paketdatei
- Nebenbefunde: → Queue (2)
- Folgen: `packages/twopoint5d/src/texture/TextureResource.ts:580-590` — der TileSet-Effekt setzt `tileSetSignal` nur bei Erfolg; weist `new TileSet()` geänderte `tileSetOptions` ab, bleibt das zuvor gebaute TileSet auf `resource.tileSet` stehen (samt der Animationen, die daran hängen), obwohl ein Fehler gemeldet wird — vor dem Paket hing die Seite dort, jetzt ist der Zustand erreichbar · → Paket 9 (Nachtrag, verteilt in Zug 0 von Paket 4)
- Schnittstellen: `new TileSet(baseCoords, options?)` wirft `RangeError` (`[TileSet] <name> must be <rule>, got <value>`) für nicht endliche `baseCoords.width`/`.height`, `tileWidth`/`tileHeight` nicht endlich oder ≤ 0, `margin`/`padding`/`spacing` nicht endlich oder < 0, `tileCount` weder `Infinity` noch ganze Zahl ≥ 1 · `PowerOf2ImageLoader`, `TextureImageLoader`, `TileSetLoader`: ein Throw im `load`-Event geht an `onErrorCallback`, `loadAsync()` rejected · `TextureResource`: gefetchte Atlas-JSON liegt in privatem `#fetchedAtlasJson`, `atlasJson.meta.image` trägt die aufgelöste URL; der Setter `atlasJson` verwirft die gefetchte JSON

### [x] 4. vertex-objects: Pool-Kopplung, Index-Stride und Eingabevalidierung
- Findings: BUG-073 (medium), BUG-074 (medium), BUG-075 (low), BUG-076 (low), BUG-077 (low)
- Ziel: Vertex-Object-Pools halten keine verwaisten Objekte, rechnen Indizes mit dem richtigen Stride und weisen fehlgeformte Descriptions, Buffer-Daten und Indizes ab.
- Bereich: `packages/twopoint5d/src/vertex-objects/`, dazu ein Fall in `packages/twopoint5d-testing/test/vertex-objects-gpu-upload.test.js`
- Hängt ab von: —
- Mitgenommen: fünf vorbestehende Nebenbefunde gleicher Ursache — `getter: false`/`setter: false` legt den Accessor unter dem Namen `"undefined"` an, eine Komponente mit dem Namen ihres Attributs bekommt bei `vertexCount` 1 und `size` > 1 keinen Accessor (`createVertexObjectPrototype.ts:103-124`); mehr `components` als `size` schreiben ins Nachbarattribut; der Pool-Konstruktor nimmt jede `capacity` (`VOBufferPool.ts:58-71`); `usedCount` nimmt `NaN` und Brüche (`VOBufferPool.ts:84-87`)
- Detail: `docs/remediation/paket-4.md`
- Hash: 4e45acc
- Ergebnis: 1 Runde · BUG-073, BUG-074, BUG-075, BUG-076, BUG-077 sowie alle fünf Mitgenommenen behoben (Reviewer-Urteil in der Paketdatei) · Regressionstests (vor dem Fix rot, 30 Fälle) u. a. `createIndicesArray` › `steps every object by the stride, even when its indices leave a vertex unused`, `VertexObjectGeometry` › `a description whose indices leave a vertex unused draws every object from its own vertices`, `VertexObjectPool` › `a count that goes down` › `clear() lets go of every vertex object it handed out`, `usedCount refuses NaN and a fraction`, `getVO()` › `answers undefined for an index that names no used slot`, `VertexObjectBuffer` › `buffers data that does not fit the layout`, `VertexObjectDescriptor` › `refuses a description the layout cannot hold` (12 Regeln), `createVertexObjectPrototype` › `an attribute declared without a getter gets none`, `a component named like its attribute gets its accessor` · Browsertest `an object whose indices leave a vertex unused is drawn from its own vertices` (Chromium/WebGL2, Firefox/WebGPU; mit alter Erwartung rot gegengeprüft) · klein: Regeln 3 und 4 laufen je Attribut verschränkt
- Nebenbefunde: → Queue (4)
- Folgen: keine — einziger Aufrufer von `createIndicesArray` (`initializeAttributes.ts:37`) mitgezogen; Bibliothek, Lookbook und Browsertests halten die neuen Regeln
- Schnittstellen: `createIndicesArray(indices, count, stride)` — dritter Parameter neu und pflichtig (intern) · neu intern: `checkBufferArray(owner, bufferName, array, dataType, layoutLength, 'exact' | 'at-most')`, `vertexObjectPropertyNames(attributes, vertexCount, methods)` · `VOBufferPool`: protected `onUsedCountShrunk(from, to)` (`@internal`), `usedCount`-Setter wirft `RangeError` für `NaN`/Brüche, Konstruktor wirft `Error('Capacity must be a non-negative integer')` · `VertexObjectPool#getVO()` gibt `undefined` für Index außerhalb `0 … usedCount - 1` oder nicht ganzzahlig · `new VertexObjectDescriptor()` wirft `RangeError`/`Error` für fehlgeformte Descriptions (sechs Regeln) · `VertexObjectBuffer`-Konstruktor (genaue Länge) und `fromBuffersData()` (höchstens Layout-Länge) werfen `TypeError`/`RangeError` für unpassende Arrays

### [x] 9. texture: Nachtrag — eine abgewiesene TileSet-Option lässt kein TileSet aus anderen Optionen stehen
- Folge von: Paket 3
- Findings: — (Folge aus Paket 3, kein Audit-Finding)
- Fundstellen: `packages/twopoint5d/src/texture/TextureResource.ts:584-594` — der TileSet-Effekt schreibt `tileSetSignal` und `atlasSignal` nur, wenn `new TileSet(this.imageCoords, this.tileSetOptions)` gelingt; wirft der Konstruktor (seit Paket 3 ein `RangeError` für Optionen, die das Layout nicht tragen), bleiben TileSet und Atlas aus den vorigen Optionen auf der Resource stehen, und der Animations-Effekt (`TextureResource.ts:596-628`) hält die daran gebauten `frameBasedAnimations` — obwohl der Fehler gemeldet wird. Vor Paket 3 hing die Layout-Schleife an solchen Optionen, der Zustand war unerreichbar · dazu (Zug 0) derselbe Throw an zwei weiteren Stellen: im Batch des Bild-Effekts fällt die Übergabe von `#ownTexture` aus (`TextureResource.ts:538-547`, die Textur steht unbesessen auf dem Signal), über den Setter bzw. `TextureStore#parse()` (`TextureStore.ts:466-471`) geht er an den Schreiber statt als `error`-Event
- Mitgenommen: ein vorbestehender Nebenbefund gleicher Ursache (der Effekt schreibt nur ein gelungenes Ergebnis und nimmt keines zurück) — `tileSetOptions = undefined` lässt TileSet, Atlas und Animationen der vorigen Optionen stehen (`TextureResource.ts:586`)
- Einordnung: Symptom — hätte Paket 3 seine Ursache bis zum einzigen Aufrufer von `new TileSet()` in der Resource zu Ende geführt (Ziel: fehlerhafte Katalogdaten enden als benannter Fehler statt in verlorenem Zustand), gäbe es den Eintrag nicht
- Ziel: Eine `TextureResource`, deren `tileSetOptions` von `new TileSet()` abgewiesen werden, meldet den Fehler und bietet kein TileSet, keinen Atlas und keine Animationen an, die aus anderen als ihren aktuellen Optionen gebaut sind.
- Bereich: `packages/twopoint5d/src/texture/`
- Hängt ab von: Paket 3 (committet)
- Einsortiert: direkt hinter Paket 4 — kein offenes Paket fasst `texture/` an, die Folge wird geschlossen, solange Paket 3 der jüngste Stand des Moduls ist
- Detail: `docs/remediation/paket-9.md`
- Hash: b7dd540
- Ergebnis: Gate nach Wiederaufnahme grün — `pnpm run ci` Exit 0 mit frischem Browserlauf ohne Nx-Cache (2026-09-19), committet aus dem Stash `paket-9-abgebrochen` ohne neue Implementierung und ohne neuen Review (Belege `paket-9.impl-1.json`, `paket-9.review-1.json` im Arbeitsverzeichnis des Laufs)

### [x] 5. display und controls: Layout-Mathe, Resize-Ziel, Frame-Loop und Eingabe
- Findings: BUG-083 (medium), BUG-084 (medium), BUG-003 (medium), BUG-085 (medium), BUG-086 (low), BUG-088 (low), BUG-087 (low), BUG-004 (low)
- Ziel: Display misst und resized korrekt, FixedFrameLoop weist ungültige Raten ab, und PanControl2D reagiert layout-unabhängig und ohne hängende Pointer.
- Bereich: `packages/twopoint5d/src/display/`, `packages/twopoint5d/src/controls/` (BUG-004 berührt eine Zeile in `stage/StageRenderer.ts`, jetzt `:601`), dazu `packages/twopoint5d-testing/test/display-resize.test.js`, `pan-control-*.test.js` und zwei neue Browsertests
- Hängt ab von: —
- Mitgenommen: ein vorbestehender Nebenbefund gleicher Ursache wie BUG-085 (ein Eintrag in `#pointersDown` endet nicht sauber) — `#onPointerUp` löscht den Eintrag samt dem seit dem letzten `update()` gesammelten Pan, die Bewegung bis zum Loslassen erreicht `panView` nie (`PanControl2D.ts:352-366`)
- Detail: `docs/remediation/paket-5.md`
- Hash: 68c3bff
- Ergebnis: 1 Runde · alle 8 Findings und der Mitgenommene behoben (Review 1: 0 kritisch, 0 wichtig, 8 klein) · Regressionstests vor dem Fix rot: Vitest `takes the horizontal inner margin from left and right`, `the constructor keeps DefaultFps for an fps of %s`, `maxStepsPerFrame refuses %s`, `the constructor keeps DefaultMaxStepsPerFrame for a maxStepsPerFrame of 0`; Browser `a canvas with a border on top only keeps the full width of its host`, `an invalid resize-to selector warns once and measures the resizeToElement`, `a resize-to selector finds its element inside the shadow root the display lives in`, `clamps oversized dimensions to Display.MaxResolution`, `moves north by the key at the W position whatever the layout calls it`, `a pointer the browser cancels gives the next drag no head start`, `a drag released between two updates delivers its last movement`, `a pan button let go during a drag ends the pan where it was let go`, `two controls with different cursor styles show their own` · `pnpm run ci` Exit 0 ohne Nx-Cache
- Nebenbefunde: → Queue (3)
- Folgen: `packages/twopoint5d/src/controls/PanControl2D.ts:504` — beim Maus-Akkord (Pan-Taste geht los, andere Taste bleibt unten) endet der Pan, der Cursor bleibt aber versteckt, bis alle Tasten oben sind (`buttons === 0`); ein `#restoreCursorUnlessMouseDown()` nach dem `#endPointer` fehlt · low · `packages/twopoint5d/src/controls/PanControl2D.ts:272` — jeder je geschriebene `cursorPanStyle` hinterlässt eine eigene Regel im Stylesheet der Root, die nie entfernt wird; bei laufend wechselnden Werten (`url(...)` mit Query) wächst das Stylesheet ohne Grenze · low · → beide in Paket 10 (Nachtrag, verteilt in Zug 0 von Paket 6)
- Schnittstellen: `PanControl2DOptions.keys` / `PanControl2D#keys` neu (`[string, string, string, string]`, `KeyboardEvent.code`, Default `KeyW`/`KeyS`/`KeyA`/`KeyD`) · `keyCodes` deprecated, entscheidet nur, solange es vom Default abweicht und `keys` seinen Default hält · `FixedFrameLoop#maxStepsPerFrame` ist ein Accessor-Paar auf dem Prototyp (Unterklassen dürfen es nicht als Feld deklarieren), Setter ignoriert nicht endliche Werte und Werte < 1; `fps` im Konstruktor wie der Setter · `getContentAreaSize(element: Element, style?)` · `InputControlBase#addEventListener<E extends Event>(host, eventName, callback: ((event: E) => void) | EventListenerObject, passive?)`, ebenso `removeEventListener` · `PanControl2D` hört zusätzlich auf `pointercancel` · Cursor-Klasse von `PanControl2D` je Cursor-Wert (`PanControl2D-<kodierter Wert>-<postfix>`) · `Display` sucht einen `resize-to`-Selektor im Root-Knoten von `resizeToAttributeEl`, ein ungültiger Selektor wirft nicht, sondern warnt einmal

### [x] 6. utils und sprites: Dependencies, Zweierpotenzen, unpick, AnimatedSprites-Zeit
- Findings: BUG-090 (medium), BUG-091 (low), BUG-092 (low), BUG-089 (low)
- Ziel: Die öffentlichen Helfer liefern für ihren ganzen Eingaberaum korrekte Ergebnisse, und AnimatedSpritesMaterial übernimmt `time` aus den Optionen.
- Bereich: `packages/twopoint5d/src/utils/`, `packages/twopoint5d/src/sprites/AnimatedSprites/`
- Hängt ab von: —
- Detail: `docs/remediation/paket-6.md`
- Hash: a25e927
- Ergebnis: 1 Runde · BUG-090, BUG-091, BUG-092, BUG-089 behoben (Review 1: 0 kritisch, 0 wichtig, 1 klein) · Regressionstests vor dem Fix rot: `a key left out of changed() is reported as a change once, not on every call`, `update() writes a declared key it is not given as absent`, `a cloneable key left out goes absent and takes a clone again when it comes back`, `no other number beyond 32 bits`, `no fraction, no negative number and nothing that is not finite`, `with symbols`, `with multiple keys`, `keeps a symbol key it is not asked to remove`, `takes the animation time from its options`; `findNextPowerOf2(2 ** 31)` hing vor dem Fix (node, Exit 137) · `pnpm run ci` Exit 0
- Nebenbefunde: keine
- Folgen: `packages/twopoint5d/src/utils/unpick.ts` — `result[key] = o[key]` ist eine Zuweisung; ein eigener aufzählbarer Key `"__proto__"` (etwa aus `JSON.parse`) setzt den Prototyp des Ergebnisses, statt als Datenproperty mitzukommen (Abhilfe: `Object.defineProperty` bzw. `Object.fromEntries` über die gefilterten Keys) · low · → Paket 11 (Nachtrag, verteilt in Zug 0 von Paket 10)
- Schnittstellen: `Dependencies#update()` schreibt jeden deklarierten Key, ein ausgelassener wird `undefined` · `findNextPowerOf2(NaN)` ist `NaN`, über `2 ** 1023` `Infinity` · `isPowerOf2` nur für ganzzahlige Zweierpotenzen ≥ 1 · `unpick` behält aufzählbare Symbol-Keys · `AnimatedSpritesMaterialParameters.time` wirkt im Konstruktor

### [x] 10. controls: Nachtrag — den Cursor mit dem Pan zurückgeben und Cursor-Regeln nicht auflaufen lassen
- Folge von: Paket 5
- Findings: — (Folgen aus Paket 5, kein Audit-Finding)
- Fundstellen: `packages/twopoint5d/src/controls/PanControl2D.ts:504-506` — der Akkord-Zweig in `#onPointerMove` beendet den Pan mit `#endPointer(event.pointerId, true)`, sobald die Pan-Taste losgelassen ist und eine andere Taste unten bleibt, gibt den Cursor aber nicht zurück; das tut erst der Zweig `buttons === 0` (`:507-509`). Bis alle Tasten oben sind, bleibt der Cursor versteckt, obwohl nichts mehr schwenkt und die Pan-Taste den Pan nicht wieder aufnimmt (ein Akkord löst kein `pointerdown` aus) — `#onPointerUp` und `#onPointerCancel` rufen dafür `#restoreCursorUnlessMouseDown()` · `packages/twopoint5d/src/controls/PanControl2D.ts:270-273` mit `packages/twopoint5d/src/display/Stylesheets.ts:49-76` — `#installCursorPanStyleRules` legt je Cursor-Wert eine Regel `PanControl2D-<kodierter Wert>` in der Root an; weder ein Wertwechsel noch `dispose()` gibt eine frei, und `Stylesheets` kennt kein Entfernen. Laufend wechselnde Werte (`url(...)` mit Query) lassen das Stylesheet der Root ohne Grenze wachsen
- Einordnung: der Cursor im Akkord ist Symptom — Paket 5 hat das Ende eines Pointers sauber gemacht (BUG-085 samt Mitgenommenem) und die Cursor-Rückgabe an einem der drei Wege nicht mitgenommen; die Regeln sind echte Folge — die Regel je Cursor-Wert (Empfehlung zu BUG-087) ist neu, vor Paket 5 schrieb jede Root eine einzige Regel `PanControl2D` um (`git show b7dd540:packages/twopoint5d/src/controls/PanControl2D.ts`, Zeile 204). Zusammengelegt: dieselbe Datei, dieselbe Cursor-Behandlung, beide low
- Ziel: Ein Pan, der im Maus-Akkord endet, gibt den Cursor sofort zurück, und das Stylesheet einer Root trägt nur Cursor-Regeln, die ein lebendes `PanControl2D` gerade nutzt — ohne die Klasse je Cursor-Wert aufzugeben, die Paket 5 als Schnittstelle eingeführt hat.
- Zug 0: dazu als Symptom derselben Ursache zwei Sätze im CHANGELOG (`Unreleased`, `CHANGELOG.md:183` und `:187`), die seit Paket 5 eine geteilte Cursor-Regel bzw. ein Wachstum durch `installRule` beschreiben, das es so nicht mehr gibt · Weg: `Stylesheets` zählt die Nutzer einer Regel (`retainRule()` ↔ `releaseRule()`, additiv, `installRule()` pinnt), `PanControl2D` gibt seine Regel beim Wertwechsel und in `dispose()` zurück — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/controls/PanControl2D.ts`, `packages/twopoint5d/src/display/Stylesheets.ts`, `packages/twopoint5d/CHANGELOG.md`, dazu `packages/twopoint5d-testing/test/pan-control-cursor.test.js`, `pan-control-dispose.test.js` und `stylesheets.test.js`
- Hängt ab von: Paket 5 (committet)
- Einsortiert: direkt hinter Paket 6 — kein offenes Paket fasst `controls/` oder `display/` an; die Folge wird geschlossen, solange Paket 5 der jüngste Stand der Cursor-Behandlung ist. Paket 7 (Skripte, Lookbook) ist davon unabhängig
- Detail: `docs/remediation/paket-10.md`
- Hash: 041c420
- Ergebnis: 1 Runde · Akkord-Cursor und Cursor-Regeln behoben, CHANGELOG-Sätze angeglichen (Review 1: 0 kritisch, 0 wichtig, 3 klein) · Regressionstests vor dem Fix rot: Browser `a pan that ends in a mouse chord gives the cursor back at once`, `a control that writes one cursor style after another keeps only the rule of the current one`, `a rule two controls show stays until the last of them is disposed`, `gives its cursor rule back, and only once`, dazu sechs neue Fälle in `stylesheets.test.js` (rot als fehlende API) · Wächter `a control that moves off a style leaves the rule to the control still showing it` vor dem Fix grün · `pnpm run ci` Exit 0 ohne Nx-Cache
- Nebenbefunde: → Queue (3)
- Folgen: keine
- Schnittstellen: `Stylesheets.retainRule(name, css, root = document.head): string` neu — legt die Regel wie `installRule()` an (ein Name je Root eine Regel, anderes `css` schreibt um) und zählt einen Nutzer · `Stylesheets.releaseRule(name, root = document.head): void` neu — gibt einen Retain zurück; beim letzten verlässt die Regel das Sheet, außer `installRule()` hat sie ebenfalls gelegt (pinnt); ohne offenen Retain oder ohne Sheet ein No-op · `PanControl2D` retained seine Cursor-Regel und gibt sie beim Wertwechsel und in `dispose()` zurück, nicht in `unsubscribe()`

### [x] 11. utils: Nachtrag — `unpick` übernimmt einen eigenen Key `"__proto__"` als Datenproperty
- Folge von: Paket 6
- Findings: — (Folge aus Paket 6, kein Audit-Finding)
- Fundstellen: `packages/twopoint5d/src/utils/unpick.ts:7-11` (Stand `a25e927`) — die Schleife über `Reflect.ownKeys(o)` schreibt `result[key] = o[key]`; ein eigener aufzählbarer Key `"__proto__"` (etwa aus `JSON.parse`) läuft dabei in den `__proto__`-Setter von `Object.prototype`: ist sein Wert ein Objekt oder `null`, wird er zum Prototyp des Ergebnisses (dessen Properties erscheinen dann geerbt), sonst fällt er still weg — als eigene Datenproperty kommt er nie mit. Nachgestellt in Node mit `JSON.parse('{"__proto__": {"polluted": 1}, "a": 2}')`: kein eigenes `"__proto__"`, `result.polluted` ist 1. Abhilfe laut Paket 6: `Object.defineProperty` bzw. `Object.fromEntries` über die gefilterten Keys
- Einordnung: echte Folge — vor dem Lauf baute `unpick` das Ergebnis mit `Object.fromEntries(Object.entries(o).filter(…))` (`git show e352b56:packages/twopoint5d/src/utils/unpick.ts`), das jeden Key als eigene Datenproperty definiert; die Zuweisung kam mit Paket 6 (Symbol-Keys behalten). Eigene Ursache — Zuweisung statt Definition —, nicht das halb behobene Symbol-Problem
- Ziel: `unpick()` übernimmt jeden eigenen aufzählbaren Key, den es nicht entfernen soll, als eigene Datenproperty des Ergebnisses — `"__proto__"` eingeschlossen — und setzt nie den Prototyp des Ergebnisses.
- Zug 0: Folge unverändert (HEAD `041c420`), in Node nachgestellt · Weg: `Object.defineProperty` in der bestehenden Schleife · kein CHANGELOG-Eintrag — Version 0.21.2 übernahm `"__proto__"` schon als Datenproperty (`Object.fromEntries`), der Defekt entstand und endet innerhalb von `Unreleased`, die Zeile zu `unpick()` (`CHANGELOG.md:228`) bleibt wahr — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/utils/unpick.ts`, `unpick.spec.ts`
- Hängt ab von: Paket 6 (committet)
- Einsortiert: direkt hinter Paket 10 — kein offenes Paket fasst `utils/` an, die Folge wird geschlossen, solange Paket 6 der jüngste Stand von `unpick` ist; Paket 7 (Skripte, Lookbook) ist unabhängig und bleibt das letzte
- Detail: `docs/remediation/paket-11.md`
- Hash: ef2e69e
- Ergebnis: 1 Runde · Folge behoben (`unpick.ts` definiert jeden übernommenen Key per `Object.defineProperty`) · Regressionstests `keeps an own "__proto__" key as a data property and leaves the prototype alone` und `keeps an own "__proto__" key whose value is %s` (null, 5, 'x') vor dem Fix rot (4 failed / 7 passed), Wächter `drops an own "__proto__" key it is asked to remove` · Reviewer: keine Befunde
- Nebenbefunde: keine
- Folgen: keine

### [x] 7. Build-Skripte und Lookbook
- Findings: BUG-093 (low), BUG-094 (low), BUG-095 (low)
- Ziel: Die Publish-Pipeline erkennt Erstveröffentlichungen und löst workspace:/catalog:-Versionen richtig auf, und die Crosses-Demo rechnet Grad korrekt um.
- Bereich: `scripts/` (neu: Helfer-Module samt `node --test`-Specs unter `scripts/makePackageJson/` und `scripts/publishNpmPkg/`), `apps/lookbook/src/demos/crosses/Crosses.ts`, `apps/lookbook/src/pages/demos/crosses.astro`, dazu Root-`package.json` (`test:scripts` im Gate), `nx.json` (Named Input `makePackageJson`), `AGENTS.md`, `docs/architecture.md`
- Hängt ab von: —
- Mitgenommen: ein vorbestehender Nebenbefund gleicher Ursache wie BUG-094 (`resolvePackageVersion` bekommt vom Specifier nur ein Boolean) — der Operator bzw. Bereich eines `workspace:`-Specifiers geht verloren, `workspace:~` und `workspace:^2.0.0` würden als `^<version>` veröffentlicht (`scripts/makePackageJson.mjs:52-54`, `:77`)
- Zug 0: alle drei Findings unverändert seit `e352b56`, BUG-093 mit npm 11.19.1 und BUG-095 in Node nachgestellt; BUG-094 schlimmer als beschrieben (`catalog:<name>` bekommt still den Default-Bereich, `catalogs.default` allein wirft) · Weg: Logik beider Skripte in Helfer-Module mit `node --test`-Specs, neues Gate-Script `test:scripts`; Abbruch mit Exit 1, wenn ein `catalog:`/`workspace:`-Specifier im fertigen Manifest bleibt; Crosses dreht per `MathUtils.degToRad`, die Drehung je Reihe wird `90 / ROWS`; kein CHANGELOG-Eintrag (nichts erreicht Nutzer der Bibliothek) — Gründe in der Paketdatei
- Detail: `docs/remediation/paket-7.md`
- Hash: 0e7db162
- Ergebnis: 1 Runde · BUG-093, BUG-094, BUG-095 und der Mitgenommene (Operator/Bereich von `workspace:`) behoben (Review 1: 0 kritisch, 0 wichtig, 5 klein, in der Paketdatei) · Regressionstests (node:test, vor dem Fix rot) `a single published version comes back as a list`, `a version that only starts the single published one is not published`, `recognizes the not-found error of current npm`, `a workspace:* dependency takes the version of the package it names`, `a workspace:~ dependency keeps its operator`, `a workspace:^ dependency keeps its operator`, `a workspace: dependency with a range ships that range`, `a default catalog declared under catalogs.default resolves`, `catalog:<name> resolves from the named catalog`, `a catalog: dependency its catalog does not list stays as it is`, dazu `findUnpublishableSpecifiers.test.mjs` (rot als fehlendes Modul) · Crosses-Demo per Scratch-Prüfung außerhalb des Repos rot/grün belegt (kein Test-Runner im Lookbook) · `dist/package.json` der Bibliothek byte-gleich · `pnpm run test:scripts && pnpm run ci` Exit 0
- Nebenbefunde: → Queue (5)
- Folgen: keine
- Schnittstellen: neues Root-Script `pnpm test:scripts` (`node --test "scripts/**/*.test.mjs"`), in `ci` vor `test:ci` · `makePackageJson.mjs` bricht mit Exit 1 ab, wenn im fertigen Manifest ein `catalog:`- oder `workspace:`-Specifier bleibt; löst jetzt auch `optionalDependencies` auf · Helfer `scripts/makePackageJson/resolveDependencies.mjs` (`resolveDependencies(section, context)`, `resolvePackageVersion(pkgName, specifier, {workspaceRoot, pnpmWorkspaceConfig, sharedDependencies, referencedFrom})`), `scripts/makePackageJson/findUnpublishableSpecifiers.mjs`, `scripts/publishNpmPkg/publishedVersions.mjs` (`parsePublishedVersions`, `isNotPublishedError`) · `nx.json` Named Input `makePackageJson` umfasst die Helfer ohne Specs · `Cross#rotate(degrees)` im Lookbook

### [x] 12. stage: Projektionen und fitIntoRectangle weisen eine Fläche ohne Ausdehnung und pixelZoom 0 ab
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 12`
- Findings: —
- Ziel: Projektionen und `fitIntoRectangle` liefern für eine View ohne Höhe oder Breite und für `pixelZoom: 0` keine `NaN`- oder `Infinity`-Werte.
- Mitgenommen: zwei vorbestehende Nebenbefunde gleicher Ursache (ungeprüfte Zahl in der View-Rechnung) — `width`/`height` von `contain`/`cover` sowie `minPixelZoom`/`maxPixelZoom` gehen ungeprüft in dieselbe Rechnung (`maxPixelZoom: 0` → unendliche View, `width: -640` → gespiegelte, `NaN` → `NaN`; `fitIntoRectangle.ts:217-218`, `:244-248`); `Stage2D` baut eine Kamera aus einer Projektion, deren Specs keine View mit Fläche ergeben (`{}`, `{fit: 'contain'}` ohne Seite; `Stage2D.ts:163-178`)
- Zug 0: beide Nebenbefunde unverändert seit `e352b56`, in Node gegen `dist` nachgestellt (`updateViewRect(0, 600)` → `[0, 600, NaN, 1]`, `{pixelZoom: 0}` → `[Infinity, Infinity]`) · Weg: interner Helfer `utils/isPositiveFinite.ts`; ein `pixelZoom`, der keine endliche Zahl über 0 ist, zoomt mit 1 (wie `Display#pixelZoom` bei 0), eine solche Seite gilt als nicht vorgegeben (wie `0`), ein solcher Clamp greift nicht; die Projektion behält View, Pixel-Ratio und Kamerawerte, solange Container oder Specs keine View mit Fläche ergeben, und meldet vor der ersten `[0, 0, 0, 0]`; `Stage2D` baut aus einer Projektion ohne View keine Kamera · kein Browsertest (reine Arithmetik), kein Migration Guide (keine Signatur ändert sich) — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/stage/` (`fitIntoRectangle.ts`, `OrthographicProjection.ts`, `ParallaxProjection.ts`, `Stage2D.ts`, deren Specs, `README.md`), neu `packages/twopoint5d/src/utils/isPositiveFinite.ts` (intern), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Detail: `docs/remediation/paket-12.md`
- Hash: 69e27081
- Ergebnis: 2 Runden · beide Nebenbefunde aus der Queue und die zwei mitgenommenen behoben (Review 1: alle vier erfüllt, 1 wichtig; Review 2: behoben, 0 kritisch, 0 wichtig, 1 klein) · Runde 1 zog eine Folge der eigenen Änderung mit: im Composed-Pfad warf `Stage2D#asPassNode()` für eine Stage ohne Kamera bei jedem Frame; `#renderPipelineComposed` zeichnet jetzt nichts, solange eine `Stage2D` der Komposition keine Kamera hat · Regressionstests vor dem Fix rot (30 rot): in `fitIntoRectangle.spec.ts` `a pixelZoom of %s gives the container as the view`, `a contain width of %s …`, `a cover height of %s …`, `a contain spec takes the side …`, `a maxPixelZoom of %s does not apply` (rot 0, -2, -Infinity), `a minPixelZoom of %s does not apply` (rot Infinity); je in beiden Projektions-Specs `keeps its view while the container has no area`, `has no view before a container with area`, `has no view from the specs %j`, `takes a pixelZoom of 0 as the container`; in `Stage2D.spec.ts` `creates no camera from specs that give no view`, `fills the container for a pixelZoom of 0`; in `StageRenderer.spec.ts` `a composing renderer draws nothing while a Stage2D of the composition has no camera` (Runde 1, vor dem Fix rot) · `pnpm run ci` Exit 0 · Commit-Message um den Composed-Guard ergänzt
- Nebenbefunde: keine
- Folgen: `packages/twopoint5d/src/stage/StageRenderer.ts:363-364` (Stand `69e27081`) — TSDoc von `buildOutputNode` (öffentlich, landet in der `d.ts`) nennt als einzigen Fall, in dem der Composed-Modus nichts zeichnet, einen Renderer ohne Breite oder Höhe; README und CHANGELOG nennen jetzt auch »eine `Stage2D` der Komposition hat keine Kamera« · klein · `packages/twopoint5d/src/stage/Stage2D.ts:82-90`, `:163-171` (Stand `69e27081`) — wechselt die Projektion auf eine, deren Specs keine View ergeben, während der Container eine Fläche hat, geht die Projektionskamera, `stage.width`/`height` behalten aber die Werte der vorigen Projektion (für `projection = undefined` vorbestehend) · klein · → beide in Paket 18 (Nachtrag, verteilt in Zug 0 von Paket 13)
- Schnittstellen: interner Helfer `packages/twopoint5d/src/utils/isPositiveFinite.ts` (`isPositiveFinite(value: unknown): value is number`, nicht öffentlich) · `OrthographicProjection#updateViewRect()`/`ParallaxProjection#updateViewRect()` lassen die Projektion unverändert bei Breite/Höhe, die keine endliche Zahl über 0 ist; Specs ohne View mit Fläche behalten die letzte View; vor der ersten View meldet `getViewRect()` `[0, 0, 0, 0]` · `Stage2D` baut keine Kamera aus einer Projektion ohne View (weder `OnStageResize` noch `OnStageAfterCameraChanged`) · `StageRenderer` im Composed-Modus zeichnet nichts, solange eine `Stage2D` (erkannt am Flag `isStage2D`) keine Kamera hat

### [x] 13. map2d: Visibility-Helfer ohne globales DOM, RepeatingTilesProvider mit Default-Achse
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 13`
- Findings: —
- Ziel: Die map2d-Helfer laufen ohne `document`, und `getTileIdsWithin()` behandelt einen ungültigen `limitToAxis` genauso wie `getTileIdAt()`.
- Zug 0: beide Nebenbefunde unverändert (HEAD `69e27081`), `limitToAxis` in Node nachgestellt · der DOM-Write deckt sich mit dem Audit-Finding ARCH-004 (medium, außerhalb der BUG-Serie): bleibt im Paket, weil die Drain-Runde ihn nach der Scope-Regel eingeplant hat und ein `ReferenceError` im DOM-losen Host ein Korrektheitsdefekt ist; ARCH-004 wird nicht als Finding geführt, der Abschluss kann es mit dem Hash dieses Pakets als behoben buchen · Weg: Block und `document`-Stub der Spec löschen; `default:` zu `case 'none'` in `getTileIdsWithin()`, TSDoc an `limitToAxis`; CHANGELOG je ein Eintrag unter `Removed` und `Fixed`; kein Browsertest, kein Migration Guide — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/map2d/` (`CameraBasedVisibilityHelpers.ts`, `RepeatingTilesProvider.ts`, deren Specs), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Detail: `docs/remediation/paket-13.md`
- Hash: fdba2c34
- Ergebnis: 1 Runde · beide Nebenbefunde behoben (DOM-Write entfernt, `default:` in `getTileIdsWithin()`), damit auch ARCH-004 behoben · Regressionstests `builds its set in a host without a document`, `a limitToAxis outside the type` (4 Fälle + Target-Test), vor dem Fix 17 rot · Reviewer: erfüllt, 0 kritisch/wichtig, 2 klein
- Nebenbefunde: → Queue (4, alle → Audit)
- Folgen: —

### [x] 18. stage: Nachtrag — eine Stage2D ohne View trägt keine Maße einer vorigen Projektion, und die TSDoc von buildOutputNode nennt jeden Fall ohne Zeichnung
- Folge von: Paket 12
- Findings: — (Folgen aus Paket 12, kein Audit-Finding)
- Fundstellen: `packages/twopoint5d/src/stage/Stage2D.ts:82-91` mit `:168-170` (Stand `69e27081`) — wechselt `projection` auf eine, deren Specs keine View mit Fläche ergeben, während der Container eine Fläche hat, nimmt der Setter die Projektionskamera weg, `#updateProjection` kehrt aber vor dem Schreiben von `#width`/`#height` zurück: `stage.width`/`height` melden weiter die View der vorigen Projektion. Vor Paket 12 schrieb `#updateProjection` die (unbrauchbaren) Werte der neuen Projektion und baute daraus eine Kamera (`git show f499ca2:packages/twopoint5d/src/stage/Stage2D.ts`, Zeilen 156–186) · `packages/twopoint5d/src/stage/StageRenderer.ts:363-364` — die TSDoc von `buildOutputNode` (öffentlich, landet in der `d.ts`) nennt als einzigen Fall, in dem der Composed-Modus nichts zeichnet, einen Renderer ohne Breite oder Höhe; seit Paket 12 zeichnet er auch nichts, solange eine `Stage2D` der Komposition keine Kamera hat — `stage/README.md:291`, `:470` und `CHANGELOG.md:127` sagen es, die TSDoc nicht
- Mitgenommen: ein vorbestehender Nebenbefund gleicher Ursache (die Maße werden nur geschrieben, wenn aus der Projektion eine View entsteht; eine Stage, die ihre Projektionskamera verliert, behält sie) — `projection = undefined` nimmt die Kamera weg, `updateProjection()` läuft ohne Projektion nicht (`Stage2D.ts:152-156`), `width`/`height` bleiben stehen; so schon in `e352b56` (`Stage2D.ts:142-146`)
- Einordnung: beide Symptome — Paket 12 hat den Zustand »Projektion ohne View, keine Kamera« eingeführt (Mitgenommenes: keine Kamera aus einer Projektion ohne View; Runde 1: Composed-Guard) und weder die Maße der Stage noch die TSDoc von `buildOutputNode` mitgezogen; hätte es diesen Zustand zu Ende gebracht, gäbe es keinen der beiden Einträge. Zusammengelegt: eine Ursache, beide klein
- Ziel: Eine `Stage2D`, deren Projektion keine View ergibt oder die keine Projektion hat, meldet über `width`/`height` keine Maße einer vorigen Projektion, und die TSDoc von `StageRenderer#buildOutputNode` nennt jeden Fall, in dem der Composed-Modus nichts zeichnet.
- Bereich: `packages/twopoint5d/src/stage/` (`Stage2D.ts`, `StageRenderer.ts`, deren Specs, `README.md`), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: Paket 12 (committet)
- Einsortiert: direkt hinter Paket 13 — kein offenes Paket (14 texture, 15 vertex-objects, 16 display/controls, 17 scripts) fasst `stage/` an; die Folge wird geschlossen, solange Paket 12 der jüngste Stand von `Stage2D` ist. Vor Paket 13 nicht: dessen Detailplan steht, B setzt es als nächstes um
- Zug 0: alle drei Einträge unverändert (HEAD `fdba2c34`); dazu derselbe Defekt bei einer Zuweisung von `projection`, während der Container keine Fläche hat (`Stage2D.ts:161`) — dieselbe Ursache, im Paket · Weg: der Setter `projection` setzt `width`/`height` vor dem Kamera-Event auf 0, eine vom Nutzer gesetzte Kamera bleibt; die erste View der neuen Projektion geht über `OnStageResize` hinaus, auch bei gleicher Größe wie die vorige, der Sturz auf 0 nicht · TSDoc von `buildOutputNode` nennt die `Stage2D` ohne Kamera, ein Satz im README-Pitfall, CHANGELOG unter `### Fixed`; kein Browsertest, kein Migration Guide — Gründe in der Paketdatei
- Detail: `docs/remediation/paket-18.md`
- Hash: 121a8fa9
- Ergebnis: 1 Runde · beide Folgen aus Paket 12 und der mitgenommene Fall `projection = undefined` behoben, dazu Fall 3 (Zuweisung bei Container ohne Fläche) · Regressionstests in `Stage2D.spec.ts`, Block `size after a change of projection`: `drops the size of the previous projection for one whose specs give no view`, `drops the size together with the projection`, `drops the size of the projection while it keeps an assigned camera`, `a listener to the camera change reads no size of the projection that went`, `announces the first view of a new projection, also at the size of the previous one`, `takes the view of a projection assigned while the container has no area on the next resize() with one` (alle sechs vor dem Fix rot), Wächter `sends no OnStageResize for the drop to no size` · Reviewer: alles erfüllt, 0 kritisch/wichtig, 2 klein · `pnpm run ci` Exit 0
- Nebenbefunde: → Queue (1, → Audit)
- Folgen: —
- Schnittstellen: `Stage2D#projection` (Setter) setzt `width`/`height` auf 0, bevor `OnStageAfterCameraChanged` für die gehende Kamera läuft, auch bei `undefined` und bei einer vom Nutzer gesetzten Kamera (die bleibt); die erste View der neuen Projektion geht immer über `OnStageResize` hinaus, auch bei gleicher Größe; der Sturz auf 0 sendet kein `OnStageResize`

### [x] 14. texture: gebrochenes Padding, geleerte Animationsdaten, späte Effekt-Registrierung, Textur-Übergabe im Bild-Batch
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 14`
- Findings: —
- Ziel: `TileSet` rechnet mit gebrochenem Padding exakt, und eine `TextureResource` bietet kein TileSet, keinen Atlas und keine Animationen an, die ihre aktuellen Eingaben nicht ergeben, registriert in `load()` die Effekte ihrer Shape unabhängig von den Werten beim Aufruf und besitzt jede Textur, die sie auf ihr Signal legt.
- Mitgenommen: ein vorbestehender Nebenbefund gleicher Ursache wie die Animationsdaten (ein abgeleiteter Wert der Resource bleibt stehen, wenn seine aktuelle Eingabe keinen mehr ergibt; Paket 9 hat den TileSet-Effekt behoben, der Atlas-Effekt ist der letzte dieser Art) — geleertes oder unlesbares `atlasJson` lässt Atlas und Animationen der vorigen JSON stehen, eine unlesbare JSON wirft beim Schreiber statt als `error` zu gehen (`TextureResource.ts:764-782`; so schon `e352b56:706-714`)
- Zug 0: alle vier Queue-Einträge unverändert (HEAD `121a8fa9`), in Node gegen `dist` nachgestellt · die Übergabe deckt sich mit dem Audit-Finding MEM-001 (medium, außerhalb der BUG-Serie): bleibt im Paket wie ARCH-004 in Paket 13, Weg ist dessen Empfehlung wörtlich, der Abschluss kann MEM-001 mit dem Hash dieses Pakets als behoben buchen · der Guard in `load()` geht auf das Vorhandensein der Shape-Signale, nicht auf `this.type` — API-047 (Signale im Konstruktor) bleibt offen und wird **nicht** als behoben gebucht · Ziel geschärft: »bietet immer genau den Zustand, den ihre aktuellen Eingaben ergeben« hätte ein geleertes `imageUrl` eingeschlossen, das als `→ Rückfrage` in die Queue ging · Weg: `padding * 2`; Animations- und Atlas-Effekt schreiben `undefined`, wenn ihre Eingabe fehlt, eine unlesbare JSON geht als `error` mit `source: 'texture'`; Ownership vor dem `batch()`, Vorgänger im `finally`; CHANGELOG unter `### Fixed`; kein Browsertest, kein Migration Guide — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/texture/` (`TileSet.ts`, `TextureResource.ts`, deren Specs, ein Fall in `TextureStore.spec.ts`), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Restplan: unverändert — die Pakete 15 (vertex-objects), 16 (display/controls) und 17 (scripts) fassen `texture/` nicht an; keine offene Folge zu verteilen
- Detail: `docs/remediation/paket-14.md`
- Hash: ced8c118
- Ergebnis: 1 Runde · alle vier Nebenbefunde (Padding, Animationsdaten, Registrierung nach Shape, Textur-Übergabe) und die mitgenommene Atlas-Rücknahme behoben; MEM-001 damit behoben (für den Abschluss), API-047 nicht · Regressionstests (14 vor dem Fix rot) u. a. `TileSet` › `a fractional padding steps each tile by twice the padding`, `a fractional padding lays out as many tiles as fit`, `TextureResource` › `texture ownership` › `a subscriber of the texture that throws leaves the texture to the resource`, `a resource disposed by a subscriber of its new texture releases both textures`, `load()` › `a tile set resource whose options were cleared before load() builds its tile set once they are set again`, `an atlas json that is cleared or cannot be read` › `an atlasJson that TexturePackerJson cannot read is reported and takes the atlas back`, `TextureStore` › `a parse() without animation data takes the animations of a known resource back`; Wächter `animations cleared with their data come back with new data`, `a readable atlasJson after an unreadable one brings the atlas back` · Reviewer: alles erfüllt, 0 kritisch/wichtig, 3 klein · `pnpm run ci` Exit 0
- Nebenbefunde: keine neuen (ein `atlasJson` ohne `meta` wirft weiter beim Setter — Eingabevalidierung, im Audit als SEC-001 geführt)
- Folgen: —
- Schnittstellen: `TextureResource#load()` registriert die Effekte nach Shape (vorhandene Signale aus `fromTileSet()`/`fromAtlas()`), nicht nach dem Wert von `tileSetOptions`/`atlasUrl` beim Aufruf · Setter `atlasJson` wirft bei unlesbaren Frames nicht mehr, sondern `error` mit `{source: 'texture', id, error}`, sobald das genannte Bild da ist; Atlas und `frameBasedAnimations` gehen dann auf `undefined` (retained Events geräumt), ebenso bei geleertem `atlasJson` · `frameBasedAnimations` wird `undefined`, sobald `frameBasedAnimationsData` oder die Basis (TileSet/Atlas) fehlt · `#ownTexture` wird vor dem `batch()` gesetzt, der Vorgänger im `finally` freigegeben

### [x] 15. vertex-objects: VertexObjectBuffer prüft eine numerische Capacity
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 15`
- Findings: —
- Ziel: Der öffentliche `VertexObjectBuffer`-Konstruktor weist eine `capacity` ab, die keine nicht-negative ganze Zahl ist, und nennt sie in der Fehlermeldung.
- Zug 0: Nebenbefund unverändert (HEAD `ced8c118`, Datei seit `4e45acc` unberührt), vorbestehend seit `e352b56`, in Node gegen `dist` nachgestellt — schwerer als notiert: `1.5`, `NaN` und `buffersData.capacity` `undefined` werfen nicht, sie bauen still einen Buffer, dessen `capacity` keine Objektzahl ist; nur negative und unendliche Werte enden im unbenannten `RangeError` der Engine · `buffersData.capacity` als derselbe Eingang im Paket · Weg: Regel wörtlich wie `VOBufferPool` (`capacity < 0 || !Number.isInteger(capacity)`), `RangeError` `VertexObjectBuffer: capacity|buffersData.capacity must be a non-negative integer, got <Wert>`, Meldung des Pools bleibt; CHANGELOG: die bestehenden Sätze unter `### Changed` und im Migration Guide um den Buffer erweitern, kein neuer Eintrag; kein Browsertest — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/vertex-objects/` (`VertexObjectBuffer.ts`, `VertexObjectBuffer.spec.ts`), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Restplan: unverändert — die Pakete 16 (display/controls) und 17 (scripts) fassen `vertex-objects/` nicht an; keine offene Folge zu verteilen, kein Queue-Eintrag mit derselben Ursache (die beiden `→ Rückfrage`-Einträge zu `VertexObjectDescriptor` und `createVertexObjectPrototype` haben eigene Ursachen)
- Detail: `docs/remediation/paket-15.md`
- Hash: 59d47e54
- Ergebnis: 1 Runde · Nebenbefund `VertexObjectBuffer.ts:85` behoben · Regressionstests `VertexObjectBuffer` › `a capacity that is no integer of 0 or more` › je `from a descriptor`/`from a source buffer`: `refuses a capacity of %s and names it`, `refuses a buffersData.capacity of %s and names it` (alle 18 Fälle vor dem Fix rot); Wächter `takes a capacity of 0` · Reviewer: erfüllt, 0 kritisch/wichtig, 3 klein · `pnpm run ci` Exit 0
- Nebenbefunde: keine
- Folgen: —
- Schnittstellen: `new VertexObjectBuffer(source, capacity | buffersData)` wirft `RangeError` `VertexObjectBuffer: capacity|buffersData.capacity must be a non-negative integer, got <Wert>` für eine Capacity, die keine ganze Zahl ≥ 0 ist (nach der Prüfung auf freigegebene Quelle)

### [x] 16. display und controls: erstes panView-Update und FixedFrameLoop-Defaults
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 16`
- Findings: —
- Ziel: PanControl2D liefert sein erstes `update`-Event auch nach erneuter Zuweisung desselben State, und `FixedFrameLoop` weist ungültige Default-Statics genauso ab wie ungültige Optionen.
- Zug 0: beide Nebenbefunde unverändert (HEAD `59d47e54`), vorbestehend seit `e352b56`, in Node gegen `dist` nachgestellt · `panView` trifft auch einen neuen State, der vor seinem ersten `update()` zweimal zugewiesen wird (dieselbe Zeile, im Paket) · die Statics wirken schwerer als notiert (negativer Default lässt die Zeit rückwärts laufen, `alpha` wird `NaN` oder > 1, eine abgewiesene Option fällt auf den kaputten Default) · Weg: der Konstruktor schickt `DefaultFps`/`DefaultMaxStepsPerFrame` durch dieselben Setter wie die Optionen, über festen Ausgangswerten 60 und 5; die Statics bleiben einfache Felder · der Setter `panView` setzt das Flag nur für einen neuen State und löscht es nie, derselbe State ändert nichts (auch nach dem ersten `update()` kein zusätzliches Event) · TSDoc für `panView` und `update()`, zwei bestehende CHANGELOG-Sätze erweitert; Browsertest für `PanControl2D`, Vitest für `FixedFrameLoop`; kein Migration Guide — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/display/` (`FixedFrameLoop.ts`, `FixedFrameLoop.spec.ts`), `packages/twopoint5d/src/controls/PanControl2D.ts`, `packages/twopoint5d-testing/test/pan-control-input.test.js`, `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Restplan: unverändert — Paket 17 (scripts) fasst weder `display/` noch `controls/` an; keine offene Folge zu verteilen, kein Queue-Eintrag mit derselben Ursache (die DOC-Einträge zu `PanControl2D#dispose()`, `Stylesheets.addRule()` und `display/types.ts:57` bleiben `→ Audit`)
- Detail: `docs/remediation/paket-16.md`
- Hash: ed1b11e4
- Ergebnis: 1 Runde · beide Nebenbefunde behoben · Regressionstests `FixedFrameLoop` › `default statics the loop cannot run with` › `ignores a DefaultFps of %s and runs at 60`, `falls back past a DefaultFps of %s to 60 for an fps option it refuses`, `ignores a DefaultMaxStepsPerFrame of %s and takes 5`, `falls back past a DefaultMaxStepsPerFrame of 0 to 5 …` (14 Fälle vor dem Fix rot), Wächter `starts from a DefaultFps and DefaultMaxStepsPerFrame it can run with`; Browsertests `reports the first update() after a state that is assigned again before it`, `reports the first update() after a new state that is assigned twice before it` (vor dem Fix rot), Wächter `reports nothing for the state it holds assigned again after its first update()` · Reviewer: erfüllt, 0 kritisch/wichtig/klein · `pnpm run ci` Exit 0
- Nebenbefunde: → Queue (2× DOC, `→ Audit`)
- Folgen: —

### [x] 17. Build-Skripte: makePackageJson schneidet nur ein führendes dist/ ab und verkraftet fehlende exports
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 1, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 17`
- Findings: —
- Ziel: `makePackageJson.mjs` entfernt nur ein führendes `dist/` bzw. `./dist/` und verarbeitet Manifeste ohne `exports`, mit `exports` als einzelnem Pfad oder mit `null`-Einträgen, ohne zu werfen.
- Zug 0: beide Nebenbefunde unverändert (HEAD `ed1b11e4`, `scripts/makePackageJson.mjs:65-88` seit `0e7db162` unberührt), vorbestehend seit `e352b56` (dort `:94-117`), in Node nachgestellt · dazu `exports` als einzelner String (gültige npm-Form, wirft `TypeError`; dieselbe Ursache — der Walk nimmt ein Objekt an —, im Paket, Ziel entsprechend geschärft) · keine Audit-Überschneidung (BUG-094 betraf `resolvePackageVersion`, erledigt in Paket 7) · Weg: Logik in das Helfer-Modul `scripts/makePackageJson/removeDistPathPrefix.mjs` mit `node --test`-Spec (erst unverändert auslagern, Spec rot sehen, dann fixen), führend heißt `dist/` oder `./dist/`, `null`-Ziele und fehlendes `exports` bleiben unangetastet; ein Satz in `docs/architecture.md` §4; kein CHANGELOG (nichts erreicht Nutzer der Bibliothek), kein `nx.json`-Eingriff (Named Input deckt das Modul schon); Verify prüft `dist/package.json` der Bibliothek per SHA-256 auf Byte-Gleichheit — Gründe in der Paketdatei
- Bereich: `scripts/makePackageJson.mjs`, neu `scripts/makePackageJson/removeDistPathPrefix.mjs` samt `removeDistPathPrefix.test.mjs`, `docs/architecture.md`
- Hängt ab von: —
- Restplan: unverändert — Paket 17 ist das letzte offene Paket; keine offene Folge zu verteilen, kein Queue-Eintrag mit derselben Ursache (die Einträge zu `publishNpmPkg.mjs:11` und `:29` haben eigene Ursachen und bleiben `→ Audit`)
- Detail: `docs/remediation/paket-17.md`
- Hash: e90de130
- Ergebnis: 1 Runde · beide Nebenbefunde behoben (nur führendes `dist/`/`./dist/` fällt; fehlendes `exports`, `null`-Ziel und `exports` als String werfen nicht mehr) · Regressionstests in `scripts/makePackageJson/removeDistPathPrefix.test.mjs`: `keeps a dist/ that does not lead the path`, `takes a manifest without exports`, `keeps a null target in exports`, `rewrites exports given as a single path` (vor dem Fix 4 rot, 3 Wächter grün) · `dist/package.json` der Bibliothek byte-gleich (SHA-256) · Reviewer: erfüllt, 0 kritisch/wichtig, 2 klein (in der Paketdatei) · `pnpm run ci` Exit 0
- Nebenbefunde: —
- Folgen: —
- Schnittstellen: neu `removeDistPathPrefix(manifest)` in `scripts/makePackageJson/removeDistPathPrefix.mjs` — ändert `manifest` an Ort und Stelle und gibt es zurück; die gleichnamigen modulinternen Funktionen in `scripts/makePackageJson.mjs` entfallen

### [x] 19. stage: ParallaxProjection weist eine ungültige distanceToProjectionPlane ab
- Nebenbefunde: aus der Befund-Queue (Drain-Runde 2, 2026-09-19), siehe »Offene Befunde« mit `→ Paket 19`
- Findings: —
- Ziel: `ParallaxProjection` und `OrthographicProjection` nehmen eine Kamera-Spec (`distanceToProjectionPlane`, `near`, `far`), aus der keine Kamera abbilden kann, als nicht angegeben: die Parallax-Distanz und den Parallax-`near` nur als endliche Zahl über 0, die orthografische Distanz und den orthografischen `near` als endliche Zahl, `far` als endliche Zahl über dem geltenden `near`.
- Mitgenommen: vorbestehende Nebenbefunde gleicher Ursache (`updateViewRect()` übernimmt eine Kamera-Spec mit `?? default`, ohne zu prüfen, ob die Kamera damit abbilden kann) — `near`/`far` der `ParallaxProjection` (`ParallaxProjection.ts:69-70`: `near` 0 → Projektionsmatrix `NaN`, `far` = `near` → Division durch 0, `far` < `near` oder `near` über dem Default-`far` → umgedrehte Tiefe, `far` `Infinity` → `NaN`) sowie `near`, `far` und die nicht endliche Distanz der `OrthographicProjection` (`OrthographicProjection.ts:68-71`); so schon in `e352b56`
- Zug 0: Queue-Eintrag unverändert (HEAD `e90de130`, Datei seit `69e27081` unberührt), vorbestehend seit `e352b56`, in Node gegen `dist` nachgestellt (Distanz 0 → `fov` 180, −300 → `fov` −90, `NaN` → `fov` und Position `NaN`) · keine Audit-Überschneidung in der BUG-Serie; CONS-025 (`updateCamera()` wendet nicht alle Specs an) und DOC-020 (Defaults benennen, `getZoom()` dokumentieren) bleiben offen und werden nicht gebucht · Ziel auf beide Projektionen und alle drei Kamera-Specs geschärft · Weg: ungültig heißt »nicht angegeben« wie bei den Spec-Zahlen von `fitIntoRectangle` (Paket 12), kein Throw, keine Warnung; Parallax verlangt Distanz und `near` über 0, orthografisch jede endliche Zahl; `far` nicht über `near` → beide auf 0.1/100000; interner Helfer `utils/isFiniteNumber.ts`, modulprivate Default-Konstanten je Datei; TSDoc an den Spec-Feldern und an `updateViewRect()`; CHANGELOG unter `### Fixed`; kein Browsertest, kein README, kein Migration Guide — Gründe in der Paketdatei
- Bereich: `packages/twopoint5d/src/stage/` (`ParallaxProjection.ts`, `OrthographicProjection.ts`, deren Specs), neu `packages/twopoint5d/src/utils/isFiniteNumber.ts` (intern), `packages/twopoint5d/CHANGELOG.md`
- Hängt ab von: —
- Restplan: unverändert — Paket 19 ist das letzte offene Paket; keine offene Folge unter erledigten Paketen, kein weiterer Queue-Eintrag mit derselben Ursache (`Stage2D.ts:179` `→ Rückfrage` und `ParallaxProjection.ts:102` `→ Audit` haben eigene Ursachen)
- Detail: `docs/remediation/paket-19.md`
- Hash: 160b26ad
- Ergebnis: 1 Runde · Ziel erfüllt (Reviewer: alle sechs Ziel-Punkte behoben, nichts kritisch/wichtig) · Regressionstests in `ParallaxProjection.spec.ts` und `OrthographicProjection.spec.ts` (`camera values from the specs > takes a distanceToProjectionPlane of %s as not given`, `… > takes a near of $near and a far of $far as 0.1 and 100000` u. a.), vor dem Fix 28 von 62 rot · klein: Kommentar `ParallaxProjection.ts:90-91` nennt für `far` »above 0« statt »above near«, TSDoc-Umbruch, Testlücken `{far: 0.05}` und orthografisch `{far: -Infinity}`
- Nebenbefunde: keine neuen (`getZoom()`-TSDoc und `updateCamera()` sind CONS-025/DOC-020, bereits im Audit)
- Folgen: keine
- Schnittstellen: keine öffentliche Änderung · intern neu `isFiniteNumber(value)` in `packages/twopoint5d/src/utils/isFiniteNumber.ts`
