# Remediation-Plan — @spearwolf/twopoint5d

Quelle: ./audit.html vom 2026-09-19 (nachgeführt 2026-09-20) · Branch: main · erstellt: 2026-09-21
Baseline: `pnpm run ci` ✓ (exit 0, 49 s lokal mit warmem Nx-Cache; umfasst clean, lint, build, typecheck, checkPkgTypes, checkNameableTypes, lintPkg, test:scripts, test:ci, test:browser — Browser-Suite unter `xvfb-run` in CI)
Arbeitsverzeichnis: /tmp/claude-1000/-home-spw-spaceland-twopoint5d/0ec96af6-3174-4c1b-82ff-efaa7423bfa6/scratchpad (Diffs und Verify-Logs, außerhalb der Versionierung)
Paketdetails: docs/remediation/paket-<N>.md — je Paket eine Datei, angelegt von dessen Zug 0
Scope: 18 Findings (3 medium, 12 low, 3 info) + CI-Cache-Auftrag + 2 offene Fragen des Reports · ausgenommen: alles übrige, acknowledged
Scope-Regel: alles, was CI, Build, Nx/pnpm-Cache, Test-Harness, Lint-/Typecheck-Gate, Dependencies oder die Publish-Pipeline betrifft, jede Severity — gilt auch für Befunde, die erst im Lauf auffallen; der Rest geht als neues Finding ins Audit
Kaltstarts: 5 Pakete (3 in Zug 0 geteilt in 3 und 3b) × mindestens 3 Agenten ≈ 15, je Nachrunde zwei mehr · 21 Aufträge (18 Findings + Cache + 2 Fragen) ≈ 4,2 je Paket
Stand (2026-09-21): Lauf abgeschlossen — 10 Pakete (1, 2, 3, 3b, 4–9) committet 42a88429..320dd067, keines blockiert, »Offene Befunde« leer; Verify `pnpm run ci` ohne Nx-Cache ✓

Diese Datei führt einen Lauf des Skills `js-ts-audit-remediation` und hält
seinen Stand. Wer hier weiterarbeitet: diesen Skill laden, die eingetragenen
Hashes gegen `git log --oneline` halten, beim obersten Paket ohne `[x]`
einsteigen. Der Lauf ist erst fertig, wenn auch »Offene Befunde« leer ist.
Statusmarken: `[ ]` offen · `[~]` Detailplan steht, Umsetzung läuft · `[x]`
erledigt · `[r]` committet, Review wird nachgezogen · `[!]` blockiert.

## Entscheidungen
- Leitmotiv des Laufs: Projekt-Harness und Build-Stabilität; die Cache-Reparatur hat Vorrang vor allem anderen (2026-09-21)
- Diagnose vor Planung (2026-09-21): CI bewahrt den Nx-Cache nicht zwischen Läufen auf — kein `actions/cache`, keine Nx Cloud; gecacht wird nur der pnpm-Store über `setup-node`. Jeder CI-Lauf zeigt `Cache: 0/2 hit` beim Build, Deploy baut danach ein zweites Mal (0/2). Lokal liegt der Nx-Cache unter `.nx/workspace-data` (Nx 23), nicht unter `.nx/cache`; welches Verzeichnis in CI zu sichern ist, klärt Zug 0 von Paket 1 gegen die Nx-23-Doku bzw. `nx report`. Die Input-Definitionen in `nx.json`/`project.json` prüft Paket 1 auf fehlende Inputs (veraltete Treffer) und zu breite Inputs (unnötige Misses)
- CI-Cache: Nx-Cache per `actions/cache` sichern, Key aus Lockfile + Commit-SHA, Restore-Keys auf den letzten Stand; Playwright-Browser auf ihre Version gekeyt ebenfalls cachen. Keine Nx Cloud (2026-09-21)
- Deploy steigt früh aus: erster Schritt vergleicht die Manifest-Version mit npm, ist sie schon veröffentlicht, endet der Job vor Install und Build. `workflow_run` nach CI und OIDC bleiben (2026-09-21)
- Offene Frage »OIDC Trusted Publisher« geklärt (2026-09-21): 0.21.2 trägt `_npmUser: GitHub Actions <npm-oidc-no-reply@github.com>` und SLSA-Provenance — der Trusted Publisher ist konfiguriert und hat erfolgreich veröffentlicht; Deploy-Läufe seither grün (Skip, Version existiert). `always-auth: true` fällt weg, die Token-Echo-Zeilen ebenso; das Ergebnis gehört in `docs/architecture.md`
- Offene Frage »Welches Backend läuft in CI?«: die Browser-Suite loggt einmal je Browser und Lauf, welches Backend (WebGPU/WebGL2) three tatsächlich bekommt; der lokal gemessene Stand und die Log-Stelle kommen in `docs/architecture.md`. Der CI-Wert selbst zeigt sich erst im ersten Lauf nach dem Push — der Lauf pusht nicht (2026-09-21)
- CFG-020: `test` läuft ohne Coverage; ein eigenes Script/Nx-Target (`test:coverage` o. ä.) mit den Schwellen fährt im Gate und in CI (2026-09-21)
- CFG-014: Opt-in-Marker — geprüft werden nur als selbsttragend markierte ```ts-Blöcke (z. B. ```ts check), typgeprüft gegen den Build; Auszüge bleiben unmarkiert. Konvention in AGENTS.md (2026-09-21)
- CFG-016: `declarationMap` im Publish-Build (`tsconfig.build.json`) abschalten, lokal bleibt es an (2026-09-21)
- CONS-050: validRange normalisiert `^1` zu `>=1.0.0 <2.0.0-0` — das gehört nicht ins veröffentlichte Manifest. Zurück geht der getrimmte Rohwert, der Vorsatz steht als Kommentar an der Stelle (2026-09-21)
- CONS-052: Schreibweise `>=10.22.0`, mit Verweis auf die Quelle `package.json#engines.pnpm` (2026-09-21)
- CFG-019: `scripts/makeBanner*` wird gelöscht (kein Aufrufer), samt nx-Input und Doku-Satz (2026-09-21)
- DEPS-005: Dependabot (npm + github-actions), `pnpm audit --audit-level=high` nicht-blockierend in CI; Overrides nur für das, was nach `pnpm update` rot bleibt (2026-09-21)
- Zusatz-Findings auf Wunsch aufgenommen: TYPE-014, CONS-030, CFG-014, CFG-016 (2026-09-21)

## Konventionen
Gelten für jede Zeile, die in diesem Lauf entsteht — Code, Kommentare,
Dokumentation, CHANGELOG, Migrations-Hinweise, Commit-Messages:
- Inline-Kommentare sind erwünscht, wo sie erklären, *warum* etwas so ist.
- Keine Finding-IDs, auch nicht in der Commit-Message. Sie gehören diesem einen
  Audit, sind danach tot, und die Commit-Message überdauert den Lauf. Sie leben
  in diesem Plan und sonst nirgends; die Verbindung zwischen Finding und Commit
  trägt das Feld `Hash:` unter dem Paket — in genau der Richtung, in der jemand
  sie später sucht. Eine Commit-Message sagt in eigenen Worten, was sie ändert.
- Kein Rückblick auf den Vorzustand: kein »früher«, kein »statt bisher«, kein
  »im Zuge des Audits umgestellt«. Der Test: Ergibt der Satz für jemanden Sinn,
  der den Vorzustand nie gesehen hat? Dann bleibt er. Braucht er ihn, gehört er
  in die Commit-Message — die Historie ist bereits konserviert.
- Commit-Messages im Stil von `git log`: Conventional Commits, englisch, ein ausformulierter Satz im Subject (`ci: …`, `build: …`, `test: …`, `chore(deps): …`).
- `pnpm run ci` ist das Pre-Commit-Gate (AGENTS.md); Browser-Tests lokal ohne `xvfb-run`. `pnpm publishNpmPkg` und `scripts/publishNpmPkg.mjs` werden nie ohne `--dry-run` ausgeführt.
- Workflow-Änderungen lassen sich lokal nicht fahren: YAML wird mindestens mit `actionlint` (falls verfügbar, sonst `pnpm dlx`/Docker-frei per YAML-Parse) geprüft, und die Paketdatei hält fest, was erst der nächste CI-Lauf nach dem Push belegen kann.
- Kein Push. Nx-Cache-Verhalten wird lokal belegt: zweiter Lauf = Treffer, Änderung an einem Input = Miss, Änderung an einer Nicht-Input-Datei = Treffer.

## Vorbestehende Fehler
- keine — Baseline grün

## Offene Befunde
Nebenbefunde aus den Paketen: was auch ohne diesen Lauf falsch war. Jeder
Eintrag wird beschlossen, bevor der Lauf endet — Paket oder Rückgabe ins Audit.
Ein leerer Abschnitt ist Abschlussbedingung, kein Zufall. Das Urteil am Ende
der Zeile misst den Eintrag an der Scope-Regel oben: `→ Scope`, `→ Audit`,
`→ Rückfrage`.

- [x] `.github/workflows/deploy.yml:24` und `:67` — `actions/checkout` ohne `ref` (beide Checkouts, Job `version` und `deploy`) holt bei `workflow_run` den aktuellen Kopf von `main` statt `github.event.workflow_run.head_sha`; ein Push zwischen CI-Ende und Deploy wird ungeprüft veröffentlicht. Der Fix muss beide Checkouts gemeinsam umstellen (Paket 1, low) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `packages/twopoint5d-testing/package.json:1-4` — `"private": true` fehlt im Kopf; das reine Testpaket ist nicht gegen ein versehentliches Publish geschützt (Paket 1, low) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `README.md:19` — das Badge »main workflow status« zeigt `deploy.yml`, nicht die CI (Paket 1, info) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `README.md:91` — der Kommentar zu `pnpm cbt` lässt `checkNameableTypes` und `test:scripts` aus (Paket 1, info) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `.gitignore:4` (`build`) und `.prettierignore:11` (`packages/*/build`) — nennen ein Verzeichnis, das kein Script der Bibliothek erzeugt; `.prettierignore:9` (`apps/*/build`) ungeprüft (Paket 1, info) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `packages/twopoint5d-testing/web-test-runner.config.js:30` (`'dom.webgpu.enabled': true`) — auf Firefox 155 (Playwright 1.63) hängen 13 Browser-Tests in `map2d-tile-upload`, `map2d-visibility-helpers`, `stage-pipeline`, `stage-renderer` bis zum Timeout, Log `GPUInternalError: Buffer with '' label has been destroyed`; Firefox 151/153 grün, Firefox 155 mit WebGPU aus grün. Paket 2 hält Playwright deshalb mit Tilde auf 1.62; der Sprung auf 1.63 braucht erst eine Suite, die dort grün ist (Paket 2, medium; berührt TEST-021 und die Backend-Frage in Paket 3) → Scope · beschlossen 2026-09-21: → Paket 6
- [x] `.gitignore:29` — `/.sass-cache` ist tot: Dart-Sass legt das Verzeichnis nicht an, und die Lookbook hat kein `sass` mehr (Paket 2, info) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `.github/workflows/ci.yml:27-28` — `pnpm/action-setup` nennt `version: 10.27.0` neben `packageManager: pnpm@10.27.0` in `package.json:12`; Dependabot hebt beide nicht gemeinsam, sie laufen beim nächsten pnpm-Bump auseinander (Paket 2, low) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `packages/twopoint5d-testing/test/texture-store-on.test.js:32` — der Doc-Kommentar von `waitUntil` sagt »Resolve when `cb` returns a truthy value or the deadline elapses«, bei Ablauf der Frist rejected die Funktion aber (Zeile 46) (Paket 3, info) → Scope · beschlossen 2026-09-21: → Paket 6
- [x] `packages/twopoint5d-testing/test/vertex-objects-heap.test.js:8-13` — der Heap der Testseite wächst über die sechs Proben linear, rund 13 KB je Runde, 11,72 % der ersten Probe auf Chromium 151, in sechs Läufen auf 0,01 Punkte gleich; Ursache ungeklärt (Leck in `vertex-objects`/`display` beim Dispose oder ein Cache in three). Paket 3 hält die Grenze deshalb auf 0,15 statt 0,1; die alte 4-MiB-Linie (≈ 35 %) hat den Zuwachs verdeckt (Paket 3, low geschätzt) → Rückfrage: die Messung sitzt im Test-Harness, die Ursache vermutlich in Bibliothekscode unter `src/` — die Scope-Regel deckt den Harness, nicht ein Leck in der Bibliothek · beschlossen 2026-09-21: → Audit (Rückfrage)
- [x] `packages/twopoint5d-testing/test/display-resize.test.js:469` — der Kommentar »this used to trigger a double emit on frame 1« erzählt den Vorzustand statt das Verhalten, das der Fall festhält (vorbestehend, 7de63c45e) (Paket 3b, info) → Scope · beschlossen 2026-09-21: → Paket 6
- [x] `packages/twopoint5d-testing/test/vertex-objects-heap.test.js:73-75` — Kommentar hängt an einer Maschine (»on this machine it gets no GL context«, Paket 3 maß Firefox 153 mit WebGPU) und blickt zurück (»stays exactly where it was before this file existed«) (vorbestehend, bf868e409) (Paket 3b, info) → Scope · beschlossen 2026-09-21: → Paket 6
- [x] `pnpm-lock.yaml:58` und die Snapshots von `@astrojs/compiler-binding*`, `astro-eslint-parser`, `eslint-plugin-astro` — ein nicht eingefrorenes, online aufgelöstes `pnpm install` schreibt die optionalen `@emnapi/core`/`@emnapi/runtime`-Peers neu (+45/−5) und warnt `missing peer @emnapi/core@"^1.7.1 || ^2.0.0-alpha.4"`; `--frozen-lockfile` und `--lockfile-only --offline` bleiben Fixpunkt. Paket 2 hatte die frische Auflösung als warnungsfrei vermerkt; ob neue Registry-Stände oder der Lockfile-Stand von Paket 2 die Ursache sind, ist nicht isoliert (Paket 3b, low) → Scope · beschlossen 2026-09-21: → Paket 5
- [x] `scripts/publishNpmPkg.mjs:70` — scheitert `npm publish`, wirft `execFileSync` ungefangen und das Skript endet mit rohem Stacktrace statt einer Meldung wie `npm show failed: …` (vorbestehend, gleiches Verhalten mit `execSync`) (Paket 4, info) → Scope · beschlossen 2026-09-21: → Paket 7
- [x] `scripts/publishNpmPkg.mjs:31` — `pkgJson.version.endsWith('-dev')` wirft einen `TypeError`, wenn das Manifest keine `version` trägt (vorbestehend) (Paket 4, info) → Scope · beschlossen 2026-09-21: → Paket 7
- [x] `scripts/makePackageJson.mjs:62` — `writeFileSync` legt `dist/` nicht an; ohne vorheriges `tsc` endet das Skript mit `ENOENT`-Stacktrace statt einer Meldung (vorbestehend) (Paket 4, info) → Scope · beschlossen 2026-09-21: → Paket 7
- [x] `packages/twopoint5d/src/display/Display.ts:887` — `dispose()` ruft `renderer.dispose()`, und three (`WebGPUBackend.dispose()`) zerstört das Device, während eingereichte Arbeit noch läuft; auf Firefox 155 mit WebGPU meldet das zerstörte Device `GPUInternalError: Buffer with '' label has been destroyed`, danach bekommt die ganze Seite keinen `requestAnimationFrame`-Callback mehr — eine App, die ein Display abbaut, verliert jede Animation der Seite. Ursache ist ein Firefox-Fehler, belegt nur headless mit Software-Rendering; die Browser-Suite umgeht ihn im Teardown (Paket 6) (vorbestehend, Paket 6 Zug 0, medium geschätzt — auf echter Hardware ungeprüft, dort ggf. high) → Audit · beschlossen 2026-09-21: → Audit
- [x] `scripts/makePackageJson/resolveDependencies.mjs:87-88` — ein `workspace:`-Specifier liest das `package.json` des Nachbarpakets per `JSON.parse` ohne `catch`, und `pkgJson.version.replace` wirft einen `TypeError`, wenn dort `version` fehlt; beides endet mit Stacktrace statt einer Meldung — dieselbe Ursache wie Paket 7, in dessen Zug 0 übersehen; praktisch kaum erreichbar, weil pnpm an kaputtem Workspace-JSON vorher scheitert (vorbestehend, Paket 7, info) → Scope · beschlossen 2026-09-21: → Paket 8
- [x] `scripts/publishNpmPkg.mjs:106-107` — `copyFile` überspringt eine fehlende Quelle stumm; ein Release ohne `README.md` oder `CHANGELOG.md` im Paketverzeichnis läuft ohne Hinweis durch (vorbestehend, Paket 7, info) → Scope · beschlossen 2026-09-21: → Paket 8
- [x] `docs/architecture.md:165-170` und `:199-200` — Absätze zu `makePackageJson` (Specifier-Satz) und `deploy.yml` brechen deutlich unter der Umbruchbreite der Nachbarabsätze um (vorbestehend bzw. Paket 5 klein, Paket 7, info) → Scope · beschlossen 2026-09-21: → Paket 8
- [x] `scripts/makePackageJson/resolveDependencies.mjs:11` — `specifier.startsWith(…)` wirft einen `TypeError` mit Stacktrace, wenn ein Dependency-Wert im Manifest kein String ist (`null`, Zahl), statt einer Meldung (vorbestehend, Paket 8, info) → Scope · beschlossen 2026-09-21: → Audit (dritte Drain-Runde, Rest geht ins Audit)
- [x] `scripts/ci/nxCacheServer/createCacheServer.test.mjs:90` — `a hash that is not alphanumeric …` prüft `path.join(dir, '..', '..', 'x')`, also `<os.tmpdir()>/x`, auf Nichtexistenz; eine fremde Datei `/tmp/x` macht `test:scripts` und damit das Gate rot (in Paket 8 so passiert). Der Traversal-Beleg braucht einen Pfad, den nur der Test besitzt, etwa einen eindeutigen Namen unter `root` (Folge von Paket 1, low) → Scope · beschlossen 2026-09-21: → Paket 9 (Folge, bleibt ausnahmslos im Lauf)

## Pakete

### [x] 1. CI-Pipeline: Nx-Cache über Läufe, keine Doppelarbeit, gepinnte Actions, schlanker Deploy
- Findings: Cache-Auftrag (Nutzer, Vorrang), CFG-019 (low), CFG-018 (low), SEC-003 (medium), Offene Frage »OIDC Trusted Publisher« (Deploy-Frühausstieg, `always-auth`), CONS-052 (info)
- Ziel: Ein CI-Lauf auf unveränderten Inputs holt Build, Typecheck und Tests aus dem Cache, eine Änderung invalidiert genau die betroffenen Targets, und keine Pipeline baut oder lädt etwas doppelt.
- Detail: docs/remediation/paket-1.md
- Bereich: `.github/workflows/`, `nx.json`, `packages/twopoint5d-testing/{package.json,project.json}`, `apps/lookbook/project.json`, `packages/twopoint5d/{package.json,package.override.json,.npmignore}`, `pnpm-lock.yaml`, `scripts/makeBanner*`, `scripts/ci/` (neu: Nx-Cache-Server für CI), `AGENTS.md`, `README.md`, `docs/architecture.md`
- Hängt ab von: —
- Hash: 42a88429
- Ergebnis: 2 Runden · Cache-Auftrag, CFG-019, CFG-018, SEC-003 (Pinning, `permissions`, `concurrency`; Token-Echo war schon weg), OIDC-Frage (`always-auth` weg, Deploy-Frühausstieg), CONS-052 behoben · Regressionstest `scripts/ci/nxCacheServer/createCacheServer.test.mjs` (9 Fälle, vor der Implementierung 0/9 rot) · lokaler Cache-Beleg a–e wie geplant (0/6, 6/6, 4/6, 0/6, 6/6) · Runde 1: Concurrency auf `main` je Commit statt je Branch (wartende Läufe wären verdrängt worden) · offen bis zum ersten Push: Server im Runner, Restore im zweiten Lauf, Playwright-Cache-Treffer mit `install-deps`, `deploy` skipped, SHAs lösen auf
- Nebenbefunde: → Queue
- Folgen: `pnpm-lock.yaml` — `pnpm install` löst die optionalen `@emnapi/core`/`@emnapi/runtime`-Peers an `eslint-plugin-astro`, `astro-eslint-parser`, `@astrojs/compiler-rs`/`-binding*` neu auf (stabil, `--frozen-lockfile` grün); Paket 2 muss den Lockfile-Churn daran messen, nicht an HEAD vor diesem Paket → in Paket 2 aufgegangen (Zug 0, 2026-09-21: Messbasis 42a88429 = HEAD, die frische Auflösung dort löst die Peers warnungsfrei)
- Schnittstellen: `scripts/ci/nxCacheServer.mjs --dir <pfad> --port <n>`, Token aus `NX_SELF_HOSTED_REMOTE_CACHE_ACCESS_TOKEN` · `ci.yml`: Step-IDs `playwright`, `playwright-cache`, `nx-cache`; Cache-Keys `nx-<os>-<lockfile-hash>-<sha>` und `playwright-<os>-<version>`; neue Schritte (z. B. `pnpm audit` aus Paket 2) nach `Install dependencies` und vor das Gate; `concurrency.group` = Workflow + (`github.sha` auf `main`, sonst `github.ref`), `cancel-in-progress: true` · `deploy.yml`: Jobs `version` (Output `publish`) und `deploy` (`needs: version`, `id-token: write` nur dort); die Skip-Regel (`-dev`, Version schon auf npm) steht doppelt, dort und in `scripts/publishNpmPkg.mjs:21-34` (Paket 4) · `nx.json` `vitestDefaults` nennt `three`, `@spearwolf/eventize`, `@spearwolf/signalize`, `sinon`; ein neues Coverage-Target (Paket 3) nutzt diesen Named Input · Konsumenten der Bibliothek (`twopoint5d-testing:test`, `lookbook:typecheck`, `lookbook:build`) nehmen `{"dependentTasksOutputFiles": "**/*", "transitive": true}` statt `^default` · Named Input `makeBanner` entfernt, `makePackageJson` enthält `{workspaceRoot}/package.json` · `packages/twopoint5d/package.json`: `clean` = `pnpm rimraf dist`, `checkPkgTypes`/`checkNameableTypes`/`lintPkg` bauen nicht mehr selbst · `twopoint5d-testing` hat kein eigenes `playwright`, kein `@playwright/test`, keinen `postinstall`; Browser via `pnpm exec playwright install chromium firefox`

### [x] 2. Dependencies: Audit-Gate, Update-Bot, Lookbook entrümpeln, .astro unter ESLint
- Findings: DEPS-005 (medium), DEPS-008 (low), CONS-030 (low)
- Ziel: Verwundbare und tote Dependencies werden sichtbar oder verschwinden, Updates kommen per Bot, und der Lint-Gate deckt auch die Lookbook-Komponenten ab.
- Detail: docs/remediation/paket-2.md
- Bereich: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`, `.github/dependabot.yml`, `.github/workflows/ci.yml`, `apps/lookbook/{package.json,public/js,src/components/icons,src/images/iconoir,src/layouts/Layout.astro}`, `eslint.config.mjs`, `.prettierignore`, `packages/twopoint5d/package.json` (`lintPkg`), `docs/architecture.md`
- Begründung für drei Findings: der Lockfile-Churn aus `pnpm update` soll allein reviewbar bleiben und vor dem Test-Harness-Paket liegen, damit dessen Schwellen auf dem finalen Dependency-Stand kalibriert werden
- Hängt ab von: 1 (Dependabot hält die dort gepinnten Action-SHAs aktuell; Audit-Schritt sitzt im umgebauten `ci.yml`)
- Hash: f0a16ba3
- Ergebnis: 2 Runden · DEPS-005, DEPS-008, CONS-030 behoben (Reviewer) · `pnpm audit`: 21 Advisories → 0, einziger Override `'nx>smol-toml': ^1.7.1` · Negativprobe `lintPkg` mit `left-pad` in `dist/package.json`: vorher Exit 0, nachher Exit 1 · Runde 1: Playwright 1.63 (Firefox 155) ließ 13 WebGPU-Browsertests hängen → Playwright mit Tilde auf `~1.62.1`, in Dependabot aus der Gruppe `toolchain` genommen, Befund in »Offene Befunde« · Frische Auflösung braucht `rm pnpm-lock.yaml node_modules/.pnpm/lock.yaml`, sonst bleibt `happy-dom` · offen bis zum ersten Push: Dependabot-PRs, Audit-Schritt im Runner, Nx-Cache-Fallback auf `nx-<os>-`
- Nebenbefunde: → Queue (Firefox 155/WebGPU, `/.sass-cache`, doppelte pnpm-Version)
- Folgen: —
- Schnittstellen: `ci.yml` Schritt `Audit dependencies` direkt nach `Install dependencies`, `continue-on-error: true` · `pnpm-workspace.yaml` hat einen Block `overrides:` (einziger Eintrag `'nx>smol-toml': ^1.7.1`, kommentiert mit Advisory und Wegfallbedingung — so auch jeder weitere) · `lintPkg` der Bibliothek scheitert, wenn `dist/package.json` `dependencies`/`optionalDependencies` trägt · `.github/dependabot.yml`: Gruppe `toolchain` (npm, Minor/Patch, ohne `three`, `@types/three`, `@spearwolf/eventize`, `@spearwolf/signalize`, `playwright`) und `actions` · Dependency-Stand für Paket 3: `vitest`/`@vitest/coverage-v8` 5.0.1, Playwright **1.62.1** (`~1.62.1`, Firefox 153) — nicht 1.63; Root und `@web/test-runner-playwright` lösen dieselbe Version · `sass`, `caniuse-lite`, `happy-dom`, `jsdom` nicht mehr im Baum · ESLint: `no-console` für `**/*.{js,ts,astro}`, `.ts`-Regelblock für `**/*.{ts,astro}`, kein Ignore mehr für `**/lookbook/public`

### [x] 3. Test-Harness: Coverage-Target, echte Schwellen, stabile Browsertests, Backend-Log
- Findings: CFG-020 (low), TEST-021 (medium), TEST-025 (low), Offene Frage »Welches Backend läuft in CI?«
- Ziel: Das Gate misst Coverage nur im Gesamtlauf und nah am Ist-Stand, die Browser-Suite ist reihenfolge- und timingunabhängig und loggt ihr Backend.
- Detail: docs/remediation/paket-3.md
- Bereich: `packages/twopoint5d/{package.json,project.json,vite.config.ts}`, `packages/twopoint5d-testing/test/`, `package.json` (Root), `AGENTS.md`, `docs/architecture.md`
- Geteilt in Zug 0 (2026-09-21): TYPE-014 und CFG-014 → Paket 3b. Der Typecheck-Teil ist mit der Messung gewachsen (`checkJs` über die Browsertests: 585 Fehler unter Root-Strenge, 83 gelockert, dazu der Extraktor) und teilt keine Ursache mit Coverage und Flakiness; die Nummer 3 bleibt diesem Teil, weil die Schleife den laufenden Zug unter ihr führt
- Hängt ab von: 1 (Nx-Targets und Cache-Inputs), 2 (finaler Dependency-Stand für die Schwellen)
- Hash: e4dc0a95
- Ergebnis: 2 Runden · CFG-020, TEST-021, TEST-025 und die Backend-Frage behoben (Reviewer) · Belege statt Regressionstest (Harness, kein Bibliothekscode): Einzeldatei-Lauf vorher Exit 1 an den globalen Schwellen, nachher Exit 0 ohne Coverage · Negativprobe `src/sprites/**` statements 90 → Exit 1 · Cache: 0/1, 1/1, Miss an `src/`, Treffer nach Checkout und an der README · Heap-Grenze 0,15 statt der geplanten 0,1 (gemessen 11,72 %, linear, Ursache offen → Queue) · Log-Zeilen per `console.debug`, weil `@web/test-runner` `console.info` nicht mitschneidet · lokal Chromium 151 → WebGL2, Firefox 153 → WebGPU · Runde 1: Heap-Kommentar nennt den Zuwachs ungeklärt, drei Doku-Sätze in `docs/architecture.md` · offen bis zum ersten Push: `[renderer-backend]`-Zeilen im CI-Log, Coverage-Artefakt aus dem Target `coverage`
- Nebenbefunde: → Queue (`waitUntil`-Kommentar, linearer Heap-Zuwachs)
- Folgen: —
- Schnittstellen: Nx-Target `twopoint5d:coverage` (`nx:run-script` auf das Script `coverage` = `pnpm vitest --run --coverage`, `cache: true`, Inputs `vitestDefaults` + `{projectRoot}/src/**/*.ts`, Output `{projectRoot}/coverage`) · Target `test` = `pnpm vitest --run`, ohne Coverage und ohne `outputs` · Root-Script `test:coverage` = `pnpm nx run-many -t coverage`; Gate `ci`: `… && pnpm run test:scripts && pnpm run test:coverage && pnpm run test:browser` (`test:ci` bleibt, aber nicht im Gate) · Schwellen in `packages/twopoint5d/vite.config.ts` global 83/78/82/83 plus je Modul außer `controls`/`display` · Browsertests: neu `packages/twopoint5d-testing/test/renderer-backend.test.js`, umgebaut `hello-twopoint5d-canvas`, `display-constructor`, `display-dispose` (beide ohne `wait()`), `texture-store-on`, `vertex-objects-heap` (`MAX_HEAP_GROWTH = 0.15`) · Log-Zeilen in Browsertests per `console.debug` (`info` erscheint nicht im Report)

### [x] 3b. Typecheck: Browsertests und markierte Doku-Snippets
- Findings: TYPE-014 (low), CFG-014 (low)
- Ziel: `pnpm typecheck` prüft auch die Browsertests und jeden als selbsttragend markierten ```ts-Block der Doku gegen die gebaute Bibliothek.
- Detail: docs/remediation/paket-3b.md
- Bereich: `packages/twopoint5d-testing/{package.json,project.json,tsconfig.json (neu),test/}`, `pnpm-lock.yaml` (`@types/mocha`), `scripts/checkDocSnippets.mjs` und `scripts/checkDocSnippets/` (neu, samt `*.test.mjs`), `packages/twopoint5d/src/stage/README.md` (zwei Fence-Zeilen), `AGENTS.md`, `docs/architecture.md`
- Bereich in Zug 0 präzisiert (2026-09-21): `nx.json` und Root-`package.json` bleiben unberührt — `pnpm typecheck` (`nx run-many -t typecheck`) und die Gate-Zeile greifen das neue Target `twopoint5d-testing:typecheck` von selbst auf, der Snippet-Check hängt an diesem Target
- Abgespalten aus Paket 3 in dessen Zug 0 (2026-09-21): Messwerte (`checkJs`-Probe 585 → 83 Fehler je nach Strenge, Typauflösung von `chai`/`mocha`, Zahl der ```ts-Blöcke) im Abschnitt »Übergabe an 3b« von `docs/remediation/paket-3.md` — der Zug 0 von 3b liest diesen Abschnitt
- Hängt ab von: 3 (schreibt fünf Testdateien um, legt `renderer-backend.test.js` an und ändert die Gate-Zeile `ci`), 1 (Nx-Targets und Inputs)
- Hash: e011cfe5
- Ergebnis: 1 Runde · TYPE-014 und CFG-014 behoben (Reviewer) · Belege: `tsc -p packages/twopoint5d-testing/tsconfig.json` vor den Test-Annotationen 81 Fehler/Exit 2, danach 0 · `scripts/checkDocSnippets/*.test.mjs` vor den Helfern 0/2 (Modul fehlt), danach 12/12 · Negativproben TS2551 in Probe-Test und Probe-Snippet, `js check` als Marker-Problem · Cache 0/2, 2/2, Miss an `sprite-features.md`, Treffer nach Checkout und an `web-test-runner.config.js` · 2 markierte Blöcke, 0 Fehler · Lockfile von Hand auf die `@types/mocha`-Einträge begrenzt (+8 Zeilen, `--frozen-lockfile` und `--lockfile-only --offline` sind Fixpunkt) · kein Test auf die echte API umgestellt (keiner las ein fehlendes Member) · Verlauf: erster Implementierer-Zug endete ohne Report, Resume derselben Session · klein: 5 Nachschärfungen in der Paketdatei · offen bis zum ersten Push: Target in CI
- Nebenbefunde: → Queue (zwei Rückblick-Kommentare in Tests, emnapi-Peers bei nicht eingefrorenem `pnpm install`)
- Folgen: —
- Schnittstellen: Nx-Target `twopoint5d-testing:typecheck` (`pnpm tsc -p tsconfig.json && node ../../scripts/checkDocSnippets.mjs`, `dependsOn: ["^build"]`, Inputs Tests, `package.json`, `sharedTsconfigs`, `{workspaceRoot}/**/*.md`, Module des Checks ohne `*.test.mjs`, Build-Ausgabe) — jede `.md`-Änderung im Repo invalidiert es · `packages/twopoint5d-testing/tsconfig.json` (erweitert die Root, `checkJs`, `noImplicitAny`/`strictNullChecks` aus, `types: ["mocha"]`), devDependency `@types/mocha` ^10.0.10 · `scripts/checkDocSnippets.mjs [file.md …]` (aus `packages/twopoint5d-testing` aufrufen; ohne Argumente alle getrackten `*.md`; Exit 0/1/2), Helfer `extractSnippets(markdown, file)` und `compileSnippets({snippets, anchorDir, tsconfigPath})` · Marker: Info-Zeile genau `ts check`, jede andere Form mit `check` ist ein Fehler — neue Beispiele in getrackten `.md` nie als Zeile mit drei Backticks und `ts check` schreiben, außer sie sollen geprüft werden · markiert: `packages/twopoint5d/src/stage/README.md:68`, `:368`

### [x] 4. Publish-Skripte und Paketinhalt
- Findings: DOC-035 (low), SEC-004 (low), TEST-027 (low), DOC-049 (info), CONS-050 (info), CFG-016 (low) · in Zug 0 aufgenommen: Nebenbefund JS-Source-Maps (`tsconfig.json:39` `sourceMap: true`, 134 `.js.map` im Paket zeigen auf `src/`, das nicht ausgeliefert wird — gleiche Ursache und Stelle wie CFG-016, gleiche Behandlung)
- Ziel: Die Publish- und Manifest-Skripte scheitern verständlich, rufen keine Shell mit interpolierten Werten auf, ihr Abbruchpfad ist getestet, und das Paket liefert keine Declaration-Maps ins Leere.
- Detail: docs/remediation/paket-4.md
- Bereich: `scripts/publishNpmPkg.mjs`, `scripts/publishNpmPkg/parseArguments{,.test}.mjs` (neu), `scripts/makePackageJson/resolveDependencies{,.test}.mjs`, `scripts/makePackageJson/makePackageJson.test.mjs` (neu), `packages/twopoint5d/tsconfig.build.json`, `docs/architecture.md`, `AGENTS.md`, `packages/twopoint5d/CHANGELOG.md`
- Bereich in Zug 0 präzisiert (2026-09-21): `scripts/makePackageJson.mjs` bleibt unverändert (TEST-027 testet es als Kindprozess), Root-`tsconfig.json` ebenso (»lokal bleibt es an«); die Kommandozeile von `publishNpmPkg.mjs` wird strikt (ein vertipptes `--dry-run` veröffentlichte bisher echt), `workspace:`-Ranges werden getrimmt (`workspace: ` ging als `" "` ins Manifest) — beides dieselbe Ursache wie DOC-035 bzw. CONS-050, Begründung in der Paketdatei
- Hängt ab von: —
- Hash: eee50bfc
- Ergebnis: 1 Runde · DOC-035, SEC-004, TEST-027, DOC-049, CONS-050, CFG-016 und die JS-Source-Maps behoben (Reviewer, keine kritischen oder wichtigen Befunde) · Regressionstests vor dem Fix rot: `parseArguments.test.mjs` 0/1 (Modul fehlt), `resolveDependencies.test.mjs` 11/14 (`a workspace: range ships trimmed and as written`, `a workspace: operator with whitespace around it takes the version of the package it names`, neue Zeile `'workspace: '`) · TEST-027 per Negativprobe: `process.exit(1)` in `makePackageJson.mjs:57` auskommentiert → `refuses a specifier npm cannot install` rot, Datei danach unverändert · Maps in `dist/` 268 → 0 · Kommandozeile mit `--dry-run` vorher/nachher belegt, Dry-Run bis zur npm-Abfrage `skip publishing, version 0.21.2 is already released` · `test:scripts` 49 → 59 · klein: 4 Punkte in der Paketdatei
- Nebenbefunde: → Queue (roher Stacktrace bei gescheitertem `npm publish`, fehlende `version`, `ENOENT` ohne `dist/`)
- Folgen: `scripts/publishNpmPkg.mjs:36` und `:70` — `execFile`/`execFileSync('npm', …)` ohne Shell finden unter Windows kein `npm.cmd` (die Shell von `exec` fand es); Deploy läuft auf Ubuntu, betroffen wäre ein lokaler Aufruf unter Windows (low geschätzt) → Paket 7 (Drain, 2026-09-21)
- Schnittstellen: `scripts/publishNpmPkg.mjs <package-dir> [--dry-run]` — jede andere Option, ein fehlendes und ein zweites Verzeichnis → Meldung, Usage-Zeile, Exit 1 vor jedem Datei- und npm-Zugriff; ein wörtliches `--` wird überlesen · Helfer `parseArguments(args)` → `{packageDir, dryRun}` und `USAGE` in `scripts/publishNpmPkg/parseArguments.mjs` · npm-Aufrufe per `execFile`/`execFileSync`, Fehlertext `npm show failed: …` · die Skip-Regel in `scripts/publishNpmPkg.mjs` steht jetzt bei `:31-34` (`-dev`) und `:36-55` (npm-Abfrage), nicht mehr bei `:21-34` · `packages/twopoint5d/tsconfig.build.json` setzt `declarationMap: false`, `sourceMap: false`; Root-`tsconfig.json` unverändert · `resolvePackageVersion` trimmt die `workspace:`-Range an der Entstehung; `workspace: ` (nur Leerraum) bleibt stehen und wird abgewiesen · `scripts/makePackageJson/makePackageJson.test.mjs` startet `makePackageJson.mjs` per `spawnSync` in einem Temp-Projekt mit `dist/`

### [x] 5. Drain: Deploy-Checkout, Repo-Hygiene und Lockfile-Stabilität
- Nebenbefund: aus der Queue — `deploy.yml` checkt ohne `head_sha` aus (low), `twopoint5d-testing` ohne `"private": true` (low), doppelte pnpm-Version in `ci.yml` neben `packageManager` (low), `@emnapi`-Peers schreiben das Lockfile bei nicht eingefrorenem Install neu (low), README-Badge zeigt `deploy.yml` (info), README-Kommentar zu `pnpm cbt` unvollständig (info), tote `build`-Einträge in `.gitignore`/`.prettierignore` (info), tote `/.sass-cache`-Zeile (info)
- Ziel: Deploy veröffentlicht genau den Commit, den CI geprüft hat, das Testpaket ist gegen Publish geschützt, die pnpm-Version hat eine Quelle, ein frisches Install ist reproduzierbar, und README sowie Ignore-Dateien beschreiben den tatsächlichen Stand.
- Detail: docs/remediation/paket-5.md
- Bereich: `.github/workflows/{deploy,ci}.yml`, `packages/twopoint5d-testing/package.json`, `pnpm-lock.yaml`, `package.json`, `README.md`, `.gitignore`, `.prettierignore`, `docs/architecture.md`, `AGENTS.md`
- Bereich in Zug 0 präzisiert (2026-09-21): `pnpm-workspace.yaml` bleibt unberührt — die `@emnapi`-Peers werden als Root-devDependencies deklariert, weil pnpm sie sonst nichtdeterministisch auflöst (10 Läufe von Grund auf: 6× HEAD-Form, 4× gekippt; deklariert 10/10 gleich); `ignoreMissing` dämpfte nur die Warnung · dieselbe pnpm-Duplikation steht auch in `deploy.yml:71` und fällt mit · der Deploy-`if:` bekommt den Guard »Push in dieses Repository«, auf dem `ref: head_sha` beruht · `docs/architecture.md` und `AGENTS.md` beschreiben Deploy-Checkout, pnpm-Quelle und die Peers
- Hängt ab von: —
- Hash: 9974fdb5
- Ergebnis: 1 Runde · alle acht Nebenbefunde behoben (Reviewer: je mit Fundstelle) · kein Regressionstest (kein Testträger); rot vorher: Lockfile-Probe gegen HEAD (Neuauflösung 5/5 und Auflösung von Grund auf gekippt, `missing peer`), jetzt 5/5 je Weg ohne Änderung und Warnung · Lockfile-Diff exakt wie erwartet (+8/−2) · klein: zwei ungleich umbrochene Absätze in `docs/architecture.md` §4/§5, `private` des Testpakets nirgends dokumentiert · offen bis zum ersten Push: `action-setup` ohne `version`, Deploy-`if:` und `head_sha`-Checkout, Badge
- Nebenbefunde: —
- Folgen: —
- Schnittstellen: Root-devDependencies `@emnapi/core`/`@emnapi/runtime` `^1.11.3` (nichts importiert sie, sie halten das Lockfile auf eine Auflösung; Wegfallbedingung in `docs/architecture.md` §5) · `pnpm/action-setup` in `ci.yml` und `deploy.yml` ohne `with.version`, pnpm allein aus `packageManager` · `deploy.yml`: Job `version` läuft nur bei `workflow_run.event == 'push'` aus diesem Repository, beide Checkouts auf `github.event.workflow_run.head_sha` · `twopoint5d-testing` ist `private`

### [x] 6. Drain: Browser-Suite auf Firefox 155/WebGPU und Test-Kommentare
- Nebenbefund: aus der Queue — 13 WebGPU-Browsertests hängen auf Firefox 155 (Playwright 1.63), `GPUInternalError: Buffer with '' label has been destroyed`, Playwright deshalb auf `~1.62.1` gehalten (medium); `waitUntil`-Doc-Kommentar widerspricht dem Reject bei Fristablauf (info); Rückblick-Kommentar in `display-resize.test.js:469` (info); maschinengebundener Rückblick-Kommentar in `vertex-objects-heap.test.js:73-75` (info)
- Ziel: Die Browser-Suite ist auf dem Firefox der aktuellen Playwright-Version grün oder der Hänger ist als Ursache benannt und isoliert, und die Test-Kommentare beschreiben das Verhalten, das sie festhalten.
- Detail: docs/remediation/paket-6.md
- Bereich: `packages/twopoint5d-testing/`, `package.json`, `pnpm-lock.yaml`, `.github/dependabot.yml`, `docs/architecture.md`
- Bereich in Zug 0 präzisiert (2026-09-21): `.github/dependabot.yml` bleibt unverändert — `playwright` bleibt aus der Gruppe `toolchain`, der Kommentar dort begründet das ohne den Halt; neu `packages/twopoint5d-testing/test/support/stopAndDrain.js`, eingebunden in die Teardowns aller 16 Testdateien, die ein Display abbauen
- Grenze: liegt die Ursache des Hängers in Bibliothekscode unter `packages/twopoint5d/src/` statt im Harness, ist das kein Fix dieses Laufs — Befund mit Beleg ins Audit, Playwright bleibt gehalten, Paket endet mit dem Isolations-Ergebnis
- Grenze in Zug 0 eingeordnet (2026-09-21): greift nicht — die Ursache ist Firefox 155, das nach einem `device.destroy()` bei laufender Arbeit einen `GPUInternalError` auf dem zerstörten Device meldet und der ganzen Seite kein `requestAnimationFrame` mehr liefert; die Suite umgeht es im Teardown (`stop()` + `queue.onSubmittedWorkDone()` vor `dispose()`, in der Probe 3/3 grün auf 1.63), Playwright geht auf `^1.63.0`, und was Apps davon trifft, steht als Nebenbefund zu `Display.dispose()` in »Offene Befunde« (→ Audit)
- Hängt ab von: 5 (Lockfile — ohne die dort deklarierten `@emnapi`-Peers schreibt der Playwright-Sprung rund 38 fremde Lockfile-Zeilen samt `missing peer`-Warnung; eingetragen in Zug 0 von Paket 5, 2026-09-21)
- Hash: 9f6897b8
- Ergebnis: 1 Runde · alle vier Nebenbefunde behoben · Regressionstest: die bestehende Browser-Suite, vor dem Fix auf Playwright 1.63 rot (`map2d-tile-upload`, `stage-renderer`: `GPUInternalError`, `Timeout of 20000ms`), danach beide Browser grün, `WebGPU on Firefox/155.0` und `WebGL2 on Chrome/153` je einmal im Log · klein: kein Timeout auf `onSubmittedWorkDone()`, `stopAndDrain` steht vor dem `try` der Teardowns, »still« in `display-resize.test.js:468`
- Nebenbefunde: — (der zu `Display.dispose()` steht seit Zug 0 in der Queue)
- Folgen: —
- Schnittstellen: Playwright `^1.63.0` (Firefox 155, Chromium 153), Root und `@web/test-runner-playwright` lösen 1.63.0; CI-Cache-Key damit `playwright-<os>-1.63.0` (erst nach Push belegbar) · Helfer `stopAndDrain(display)` in `packages/twopoint5d-testing/test/support/stopAndDrain.js` — jeder Browsertest, der ein Display abbaut, ruft ihn vor `dispose()`

### [x] 7. Drain: Fehlerpfade der Publish- und Manifest-Skripte
- Nebenbefund: aus der Queue — gescheitertes `npm publish` endet mit rohem Stacktrace (info), fehlende `version` wirft `TypeError` (info), `makePackageJson.mjs` ohne `dist/` endet mit `ENOENT`-Stacktrace (info) · Folge aus Paket 4: `execFile('npm', …)` ohne Shell findet unter Windows kein `npm.cmd` (low) · in Zug 0 aufgenommen, gleiche Ursache (Verdrahtung lässt Fehler von Dateisystem, Parser und npm ungefangen durch): unlesbares Manifest, leere Meldung bei fehlendem npm, unparsbare `npm show`-Ausgabe, `copyFileSync`, die vier Lesestellen von `makePackageJson.mjs`, verschlucktes stdout von `npm publish` (Default-`stdio`, selbe Zeile `:70`)
- Ziel: Jeder Fehlerpfad der beiden Skripte endet mit einer Meldung, die sagt, was fehlt, und die npm-Aufrufe funktionieren ohne Shell-Interpolation auch unter Windows.
- Detail: docs/remediation/paket-7.md
- Bereich: `scripts/publishNpmPkg.mjs`, `scripts/publishNpmPkg/`, `scripts/makePackageJson.mjs`, `scripts/makePackageJson/`
- Bereich in Zug 0 präzisiert (2026-09-21): neu `scripts/publishNpmPkg/{checkManifest,npmCommand}{,.test}.mjs`, dazu `docs/architecture.md` §4/§6 (beschreibt heute »calls npm without a shell«); `nx.json`, `AGENTS.md`, CHANGELOG bleiben unberührt · Windows startet `npm.cmd` über `cmd.exe` mit einem String aus Literalen (Argumente auf `[\w@./-]` beschränkt, auf jeder Plattform geprüft), nicht über `node npm-cli.js` — das würde den Prefix-Shim von npm umgehen · `npm show .` im Paketverzeichnis statt `npm show <name>`: kein Manifest-Wert mehr in einer Kommandozeile · `makePackageJson.mjs` stoppt ohne `dist/` mit Hinweis aufs Kompilieren, legt es nicht an · kein Test startet `publishNpmPkg.mjs`; seine Fehlerpfade belegt eine Probe mit Fake-npm (in der Paketdatei, gegen HEAD rot)
- Hängt ab von: —
- Hash: 2a278e39
- Ergebnis: 2 Runden · alle vier Aufträge und die sieben in Zug 0 aufgenommenen Fehlerpfade behoben (Reviewer, je mit Fundstelle) · Regressionstests vor dem Fix rot (`test:scripts` 63 Tests, 4 fail): `npmCommand.test.mjs` und `checkManifest.test.mjs` (Modul fehlt), `stops with a message when dist/ does not exist: …` und `stops with a message when package.json is not JSON: …` (Stacktrace) · Fehlerpfade von `publishNpmPkg.mjs` per Probe mit Fake-npm (`paket-7-probe.sh`), gegen HEAD `probe=FAILED`, danach 5/5 Exit 1 mit einer Zeile, `probe=ok` · `test:scripts` 59 → 68 · Runde 1: Doc-Kommentar in `publishedVersions.mjs:2` nannte noch `npm show <name>`, Meldung zu unparsbarer `npm show`-Ausgabe war zweizeilig (`stdout.trim()`) · klein: 3 Punkte in der Paketdatei · offen: Windows-Zweig nur per Unit-Test mit `platform = 'win32'` belegt; Zeile `+ <name>@<version>` im Deploy-Log erst beim nächsten Release
- Nebenbefunde: → Queue (fünfte Lesestelle in `resolveDependencies.mjs`, stummes Überspringen fehlender README/CHANGELOG, ungleiche Umbrüche in `docs/architecture.md`)
- Folgen: —
- Schnittstellen: `npmCommand(args, platform = process.platform)` → `{file, args, options}` in `scripts/publishNpmPkg/npmCommand.mjs` — Argumente nur `[\w@./-]`, sonst `throw` auf jeder Plattform; unter `win32` ein String mit `shell: true` und `args: []` · `checkManifest(pkgJson)` in `scripts/publishNpmPkg/checkManifest.mjs` wirft `the manifest has no "name"`/`"version"`/beide · `publishNpmPkg.mjs` fragt `npm show . versions --json` mit `cwd` = Paketverzeichnis, `npm publish` mit `stdio: 'inherit'`; jeder erwartbare Fehler: eine Zeile auf stderr, Exit 1 · `makePackageJson.mjs` stoppt ohne `dist/` (`… does not exist, compile the package first`) und bei unlesbarer Eingabe (`cannot read <pfad>: …`) · `makePackageJson.test.mjs`: `run(peerDependencies, {dist = true, packageJsonText} = {})` gibt zusätzlich `dir` zurück

### [x] 8. Drain: letzte Fehlerpfade der Skripte und Umbruch in der Architektur-Doku
- Nebenbefund: aus der Queue (Paket 7) — `resolveDependencies.mjs:87-88` liest das Nachbar-`package.json` ohne `catch` und wirft bei fehlender `version` (info); `publishNpmPkg.mjs:106-107` überspringt fehlende README/CHANGELOG stumm (info); `docs/architecture.md:165-170`, `:199-200` brechen ungleich um (info)
- Ziel: Auch der `workspace:`-Pfad und das Kopieren der Begleitdateien enden mit einer Meldung statt Stacktrace oder Stille, und die Architektur-Doku bricht einheitlich um.
- Detail: docs/remediation/paket-8.md
- Bereich: `scripts/makePackageJson/`, `scripts/publishNpmPkg.mjs`, `scripts/publishNpmPkg/`, `docs/architecture.md`
- Bereich in Zug 0 präzisiert (2026-09-21): neu `scripts/publishNpmPkg/releaseFiles{,.test}.mjs`, geändert `scripts/makePackageJson/resolveDependencies{,.test}.mjs`, `scripts/publishNpmPkg.mjs`, `docs/architecture.md`; `scripts/makePackageJson.mjs`, `nx.json`, `AGENTS.md`, CHANGELOG bleiben unberührt · ein unlesbares Nachbar-Manifest oder eines ohne `version`, die semver lesen kann, lässt den Specifier stehen, und `makePackageJson.mjs` verweigert ihn wie jeden anderen — kein neuer Ausstieg im Helfer · `LICENSE` ist Pflichtdatei wie README und CHANGELOG (dieselbe `copyFile`-Stelle), `.npmrc` bleibt optional; geprüft wird erst, wenn wirklich veröffentlicht wird · Umbruch: die ganze `docs/architecture.md` gierig bei 88 Zeichen, der Breite vor dem Lauf — die Pakete dieses Laufs haben 25 Absätze und Listeneinträge ungleich umbrochen (69 Verstöße, an `da095c73` 15), die zwei gemeldeten sind Symptome davon; Prüfskript in der Paketdatei
- Hängt ab von: —
- Hash: 89166b69
- Ergebnis: 2 Runden · alle drei Nebenbefunde behoben (Reviewer, je mit Fundstelle) · Regressionstests vor dem Fix rot: `resolveDependencies.test.mjs` 14/16 (`a workspace: dependency whose package.json is not JSON stays as it is` → `SyntaxError`, `a workspace: dependency whose package has no version semver can read stays as it is` → `TypeError`), `releaseFiles.test.mjs` Modul fehlt · Probe mit Fake-npm (`paket-8-probe.sh`) gegen HEAD `probe=FAILED` (4 statt 2 `npm publish`), danach `probe=ok` · Umbruchprüfung HEAD 68 → 0, Wort-Diff nur die drei Einfügungen · `test:scripts` 68 → 76 · Runde 1: gieriger Umbruch setzte `>=26.3.0` an einen Zeilenanfang (Blockquote), Regel und Prüfung um Blocksyntax geschärft · klein: 3 Punkte in der Paketdatei
- Nebenbefunde: → Queue (`startsWith` auf einem Nicht-String-Specifier)
- Folgen: — (die Schwäche von `createCacheServer.test.mjs:90` ist eine Folge von Paket 1 und steht in »Offene Befunde«)
- Schnittstellen: `releaseFiles({workspaceRoot, projectRoot, packageRoot})` → `Array<{src, dst}>` in `scripts/publishNpmPkg/releaseFiles.mjs` — optional `.npmrc` zuerst, Pflicht `LICENSE`, `CHANGELOG.md`, README (`README-pkg.md`, sonst `README.md`, ausgeliefert als `README.md`); fehlt eine Pflichtdatei, wirft er `<pfade> does not exist`/`do not exist` · `publishNpmPkg.mjs` prüft das erst in `publishPackage` und endet mit `cannot publish <paketverzeichnis>: …`, Exit 1 · `resolvePackageVersion` lässt einen `workspace:`-Specifier stehen, wenn das Nachbar-Manifest unlesbar ist oder keine `version` trägt, die `semver.valid` liest (Warnung `oops.. cannot read workspace package:` bzw. `… has no version:`) · `docs/architecture.md`: Prosa gierig bei 88 Zeichen, keine Folgezeile mit Markdown-Blocksyntax; Prüfskript im Abschnitt »Umbruchprüfung« von `docs/remediation/paket-8.md`

### [x] 9. Drain: Cache-Server-Test ohne geteilten Pfad in /tmp
- Nebenbefund: Folge von Paket 1 — `scripts/ci/nxCacheServer/createCacheServer.test.mjs:90` prüft `<os.tmpdir()>/x` auf Nichtexistenz; eine fremde `/tmp/x` macht `test:scripts` und das Gate rot (low)
- Folge von: Paket 1
- Ziel: Der Traversal-Test belegt dasselbe über einen Pfad, den nur er selbst besitzt, sodass das Gate nicht mehr vom Inhalt des System-Temp-Verzeichnisses abhängt.
- Detail: docs/remediation/paket-9.md
- Bereich: `scripts/ci/nxCacheServer/`
- Bereich in Zug 0 präzisiert (2026-09-21): nur `scripts/ci/nxCacheServer/createCacheServer.test.mjs`; der Server, `docs/`, `AGENTS.md` bleiben unberührt · das bediente Verzeichnis rückt zwei Ebenen unter `root` (`root/parent/cache`), sodass der Hash `..%2F..%2Fx` nur noch `root` erreichen kann und drei Listings (`dir`, `parent`, `root`) den Ausbruch belegen — kein Pfad mehr in `os.tmpdir()` selbst · Beleg per Rot-Probe (fremde `x` in einem privaten `TMPDIR`, gegen HEAD exit=1) und Gegenprobe (präparierter Server schreibt vor dem Guard; gegen HEAD landet die Spur im Temp-Verzeichnis), beide in der Paketdatei
- Begründung für ein Finding: Folge dieses Laufs, die den Gate flaky macht — eigenes Paket, weil die Drain-Runde sonst leer ist
- Hängt ab von: —
- Hash: 320dd067
- Ergebnis: 1 Runde · Nebenbefund `createCacheServer.test.mjs:90` behoben (Reviewer, `:17-21` und `:88-90`) · Regressionstest ist der bestehende Fall `a hash that is not alphanumeric answers 400 and writes nothing outside the directory`: unter der Rot-Probe (fremde `x` im privaten `TMPDIR`) vor dem Fix rot (pass 8/fail 1), danach 9/9 · Gegenprobe vorher `FAILED [x ]`, danach `ok` · klein: 3 Punkte in der Paketdatei
- Nebenbefunde: —
- Folgen: —
