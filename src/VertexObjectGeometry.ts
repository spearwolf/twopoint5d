import {VertexObjectDescription} from './types';

export class VertexObjectGeometry {
  constructor(description: VertexObjectDescription) {
    // eslint-disable-next-line no-console
    console.log('description:', description);
  }
}
