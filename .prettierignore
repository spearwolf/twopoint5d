# Add files here to ignore them from prettier formatting
.vscode
/dist
/coverage
/examples
*.md

apps/*/dist
packages/*/dist
packages/*/coverage

pnpm-lock.yaml
pnpm-workspace.yaml

*.glsl
*.vert
*.frag

# generated by `astro sync`, never hand-edited
apps/*/.astro

# playwright run artifacts
packages/*/test-results

# vendored build output, kept byte-for-byte as shipped; RainbowLine from
# @spearwolf/astro-rainbow-line loads it at runtime, so it is not dead weight
# even though nothing in the repo references it (see AGENTS.md)
apps/lookbook/public/js

# report artifact, rewritten wholesale by its generator
audit.html
