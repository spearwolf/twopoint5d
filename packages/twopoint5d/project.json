{
  "name": "twopoint5d",
  "tags": ["ci"],
  "root": "packages/twopoint5d",
  "targets": {
    "build": {
      "inputs": ["{projectRoot}/src/**/*.ts", "!{projectRoot}/src/**/*.spec.ts", "sharedTsconfigs", "makePackageJson"],
      "outputs": ["{projectRoot}/dist"]
    },
    "test": {
      "inputs": ["vitestDefaults", "{projectRoot}/src/**/*.ts"]
    }
  }
}
